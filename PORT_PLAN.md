# sqlglot-js — Port Plan (final, revision 4 — v0 redefined per Ben's decisions)

**Repo:** `airbrx/sqlglot-js` · **Upstream pin:** `sqlglot @ 91119bc` · **Reference clone (read-only):** `/tmp/sqlglot-ref`

This revision incorporates two independent adversarial reviews, a post-review addition (recursion depth, C1/R11) found by the orchestrator after both reviews had signed off, and **Ben's answers to Q1–Q5 (2026-08-24)** — see §9. The consequential answer is Q2: **v0 now ships at P8, not P6.** Product requires Snowflake, Databricks, DuckDB, and Postgres at launch, so every place the old P6 boundary was load-bearing (§1, §7 P6/P8, §9, §10) has been updated. §2–§6 and §8 are unaffected and stand as written. Every objection from the two adversarial reviews is dispositioned in the Appendix; four are deliberately rejected or narrowed, with rationale stated inline. All closure numbers in this document are recomputed from `/tmp/recon/runtime_calls.json` by the checked-in calculator (§3.2), not asserted in prose.

---

## 1. Objective, constraints, and the definition of done

Port `sqlglot` — SQL parser, transpiler, and optimizer — from Python to JavaScript.

**Hard constraints (Ben):**

1. **Zero runtime dependencies.** `dependencies` and `devDependencies` both empty, enforced by CI lint. No bundler, no polyfill, no transitive anything. Node ≥ 20 and modern browsers, ESM, `.js` only (JSDoc types, `tsc --checkJs` advisory).
2. **Tested against upstream's own tests, in situ**, so the port tracks upstream as it moves. This is the core design constraint. A plan that produces a one-time snapshot and then diverges is a failing plan.
3. **Dialect priority: Snowflake > Databricks > Postgres > everything else.** Confirmed by the data (§9 Q1) — the draft's contrary recommendation was based on an arithmetic error and is withdrawn. Ben's own framing, confirming both the order and DuckDB's inclusion: *"Our product requires Snowflake, Databricks, DuckDB and Postgres. Those go first."* DuckDB was already a forced v0 dependency regardless of priority (§7 P6 — 449 of TestSnowflake's `validate_all` calls write to it), so this confirms rather than changes the existing phase order. **Superseded 2026-09-08 — see the correction immediately below.** All four dialects had real `Parser` subclasses landed under the original order by the time of the correction, so nothing already-shipped needs to be redone; the correction governs which dialect gets the next `Dialect`-settings-class / `Generator` work, not what already landed.
3a. **Corrected dialect priority (2026-09-08): Databricks > Snowflake > DuckDB > Postgres.** Ben's exact words: *"Databricks is the most important engine, followed by Snowflake, then DuckDb and finally Postgres for future reference."* This changes the ORDER, not the v0 set (§9 Q1's four dialects are unchanged) — Databricks now leads. Any session picking the next per-dialect `Dialect`-settings-class or `Generator` work should follow this order, not §10's original table.
4. The output must be executable by a fleet of parallel implementation sub-sessions (§8).

**v0 boundary — decided by Ben (2026-08-24, §9 Q2):** v0 ships at **P8** (Snowflake + Databricks chain + DuckDB + Postgres/Redshift + default; 42.0% atom closure), not at P6 (21.8%). *"We will ship at P8."* No interim release at P5 or P6 (§9 Q3: *"We don't need an interim release"*). This is the only decision that changes the architecture's shape — see §7 P6/P8 and §10 for the restated milestones and effort.

**Definition of done, stated as a number.** Upstream's dialect suite decomposes into **15,540 atoms** — the real, harvested count (§3.2), superseding the 15,642 estimate the plan was originally written against. "Done" for a phase means *every atom whose read dialect and write dialect are both in scope is green*, machine-verified. 100% of the corpus is the terminal state (P9/P10), reached only with a documented `wontfix` list.

**Explicitly out of scope, permanently:** `tests/test_executor.py` and the 201 `# execute: true` execution assertions in the optimizer fixtures. sqlglot's Python executor is not being ported. The SQL-comparison half of those fixtures is kept.

**Byte-exactness claim, stated honestly.** Output SQL is byte-identical to Python sqlglot **for the surface the corpus covers**. Outside that surface — Unicode paths, `unsupported()` messages, the 19 generator methods the dialect suite never exercises — correctness rests on differential fuzzers (§3.4), machine-generated deny-lists (§4.6), and human review (§8.5), not on tests. That distinction is real and is not papered over anywhere in this document.

---

## 2. Strategy: hand-porting, and why the transpiler was rejected

A source-to-source Python→JS transpiler was prototyped and **rejected**. It reached 99.03% syntactic coverage, which sounds decisive and is not: `visit_ClassDef` was `pass`, and sqlglot *is* its class hierarchy — 1,048 expression classes and ~20 dialect derivations built by class-attribute inheritance and metaclass merging. Worse, 2,674 method calls are ambiguous between a Python builtin and a sqlglot method of the same name (`.pop()`, `.copy()`, `.find()`, `.replace()`, `.sql()`); a transpiler must resolve those with types it does not have, whereas a human resolves them by reading two lines of context.

We hand-port, under a **transliteration contract**: same file layout, same class names, same method names, same method order, same control flow, `// py: sqlglot/parser.py:6302` anchors on every method. The goal is that a future upstream diff can be *read* against the JS file. Idiomatic JS rewrites are a lint failure and a review rejection (§4.2, §8.5).

The corresponding cost is that fidelity is enforced by discipline plus lint, not by a compiler. That is R1, the plan's weakest joint, and §8.5/§10 now budget the mitigation explicitly.

---

## 3. Verification architecture

### 3.1 What is harvested from upstream, and where

Harvesting happens at the **`Validator` seam** — `validate_identity` / `validate_all` / `Validator.transpile` — at *call* granularity, not by instrumenting `parse_one` (which sees 52 internal calls and pollutes counts).

Four artifacts, all under `corpus/`, all committed, all regenerable by `make corpus` inside the pinned toolchain container (§5.2):

**(A) Declarative corpus — `corpus/atoms.jsonl`.** 8,415 harvested calls (5,398 identity, 3,005 validate_all, 12 transpile) expanded into an estimated 15,642 atoms; the real harvested count is **15,540** (§1, §3.2 below). Row schema:

```json
{"atom_id":"a1f3c9d2","input_id":"7c02...","expect_hash":"91ab...",
 "cls":"TestSnowflake","read":"snowflake","write":"duckdb",
 "sql":"...","expected":"...","pretty":false,"identify":false,
 "unsupported":[], "py":"tests/dialects/test_snowflake.py:412"}
```

`input_id = H(sql, read, write, pretty, identify)`; `expect_hash = H(expected, unsupported)`. The split is what makes the ratchet safe (§3.3).

**(B) AST oracle — `corpus/ast/<dialect>.jsonl`.** Own lossless dumper (`tools/astdump.py`), **not** sqlglot's `serde`, which drops 34% of arg entries (`None` and `[]`) and would let a wrong AST satisfy the gate. Schema:

```json
{"atom_id":"a1f3c9d2","ast":{"c":"Select","a":[["expressions",[...]],["from",null]],
 "m":{"line":1,"col":7,"start":0,"end":12},"cm":null},
 "repr":"Select(\n  expressions=[...])"}
```

`a` is an **ordered array of `[key, value]` pairs including nulls and empty lists** — this pins `arg_types` insertion order, which is observable in output SQL (§4.6). `repr` is Python's `Expression.__repr__`, byte-exact; asserting it is what makes the AST gate non-tautological and human-readable.

**(C) `astLoad` contract.** `astLoad` constructs with a no-arg constructor and `set()`, bypassing `__init__` — exactly like Python's `serde.load`. It **must not** run `INIT_HOOKS`. Concretely: kwargs-construction of `DateAdd` uppercases the unit; `astLoad` does not. This is frozen in `CONTRACTS.md` and unit-tested at P2.

**(D) Generate oracle — `corpus/gen/<dialect>.jsonl`.** Row = `{atom_id, ast_ref, dialect, flags, sql, unsupported_messages}`.

> **New (fixes review-B O3).** `tests/dialects/test_dialect.py` hardcodes `unsupported_level=ErrorLevel.IGNORE`, and `generator.py:965` returns early — so **497 `unsupported()` calls across the dialect suite are silently discarded** and a port that never calls `unsupported()` is byte-identical under the corpus. But `gen.unsupported_messages` *survives* the IGNORE path. Capturing it is a two-line harvester change that converts 497 invisible calls into hard assertions. It is now part of the row schema and part of P4's exit.

### 3.2 The atom model and the closure calculator

An **atom** is one `(read_dialect, write_dialect, sql, expected, flags)` tuple. `validate_identity(sql)` on dialect `d` yields one atom `(d, d)`. `validate_all(sql, read={r: s}, write={w: t})` on dialect `d` yields one atom per read entry `(r, d)` and one per write entry `(d, w)`. The default dialect is normalized to `""` (upstream stores it as both `None` and `""`). Versioned dialect strings — `"clickhouse, version=23.8"`, `"postgres, version=17.5"` — are distinct dialects and require `compareVersion`.

An atom is **closed** under an implemented dialect set `S` iff both its read and write dialects are in `S`. Because closure is a *pairwise* condition, coverage grows roughly quadratically in the number of implemented dialects; this is the single most important fact about the schedule and it is published at P0 as a curve, not discovered at P6 as a disappointment.

`tools/closure.mjs` computes this from `corpus/atoms.jsonl` and is the source of every phase exit number in §7. Machine-computed, checked in, reproducible:

**Regenerated from the real harvested corpus, 2026-08-25.** The table below is now emitted by `node tools/closure.mjs --markdown` against `corpus/atoms.jsonl`, not estimated. The estimate this plan was written against was ~15,642 atoms; the real count is **15,540**, a 102-atom / 0.65% delta — the same order as the pre-existing 3,417-vs-3,421 footnote below. **Percentages are unchanged to within 0.1pp at every phase**, so the arithmetic and the P8-ships-v0 conclusion stand unaltered. Per-test-class closure and the recomputed P9 greedy ordering are in `CLOSURE.md`.

| after phase | dialect set | atoms closed | % of 15,540 | marginal |
|---|---|---|---|---|
| P4 | `{snowflake}` | 1,595 | 10.3% | +1,595 |
| P5 | `+ default` | 2,000 | 12.9% | +405 |
| P6 | `+ duckdb` | 3,394 | 21.8% | +1,394 |
| P7 | `+ hive, spark2, spark, databricks` | 5,178 | 33.3% | +1,784 |
| **P8 (v0)** | `+ postgres, redshift` | **6,522** | **42.0%** | +1,344 |
| P9 | all 46 dialect keys | 15,540 | 100% | +9,018 |

*(Superseded estimates, for comparison: P4 1,606 · P5 2,022 · P6 3,417 · P7 5,207 · P8 6,552 · P9 15,642.)*

*(v0 boundary per Ben's decision, §9 Q2, 2026-08-24: "We will ship at P8." Bolded row updated accordingly from the original P6.)*

**Two census corrections the real corpus forces**, neither schedule-relevant: the dialect key count is **34 base + 12 versioned = 46**, not "38 + 14"; and the versioned keys contribute **+22**, not +28. Every versioned key appears **only as a write target** (0 occurrences as a read dialect), which narrows `compareVersion` (§7 P8) to the generate path.

*(The independent derivation that produced 3,421 rather than 3,417 at P6 is now moot: the calculator run against the real corpus gives **3,394**, resolving the 12 `Validator.transpile` rows in `TestDAX` as `(dax → write_dialect)`. Settled by running it, as intended.)*

> **Not yet regenerated:** the per-phase **Exit** criteria in §7 still quote the old estimates (e.g. "1,606 atoms closed under `{snowflake}`" at P4). Substitute the table above; `CLOSURE.md` carries the per-class figures those exits also cite.

### 3.3 The ratchet

Three states per atom: `pass`, `todo`, `wontfix`, plus a fourth, `quarantine`.

- **Rule 1.** An atom listed `pass` that fails → build failure.
- **Rule 2.** An atom listed `todo`/`wontfix` that passes → build failure (prevents rot).
- **Rule 3.** `todo` may only shrink for in-scope dialects.
- **Rule 4.** On resync, atoms whose `input_id` is *new* may enter `quarantine` (neither pass nor regression) pending triage. This is what stops the ratchet from deadlocking on the ~1,100 new atoms a large upstream bump brings.
- **Rule 5 (the F1 fix).** An atom whose `input_id` is **known** but whose `expect_hash` **changed** may *never* be quarantined. It hard-fails and requires an explicit human decision recorded in `UPSTREAM-NOTES.md`. Without this split, quarantine silently absorbs changed expectations and the dashboard goes vacuously green — the single most dangerous failure mode available to this design.

### 3.4 Differential fuzzers

Generic fuzzing does not find the hazards that matter here, because **the harvested corpus is 13 non-ASCII strings out of 54,980 — 0.024%**. Five named fuzz targets, each with its own generator, each an explicit phase exit criterion:

1. **`fuzz_decimal.py`** — generative (not sampled) over the *reachable* Decimal operation set (§4.6, B5): `+ - * /` on `Decimal`/`int`, unary neg, `str(Decimal)`, `str(float)`, comparison against `DataTypeParam` bounds.
2. **`fuzz_regex.py`** — validates `_py/re.js` against CPython `re`: group counting, named groups, `\A`/`\Z`, `re.VERBOSE`, `re.sub` template semantics (`\1` vs `$1`), `re.escape` output validity under `/u`.
3. **`fuzz_unicode.py`** — `isprintable`/`islower`/`isupper`/`isspace`/`strip` over the full `sys.maxunicode` range; identifier-quoting decisions (`\w`); error-message column positions with astral characters.
4. **`fuzz_toowide.py` — new, mandatory (fixes review-B O2).** `Generator.too_wide` (`generator.py:4711`) uses Python `len()` (code points); a port writing `.length` (UTF-16 units) passes **15,642/15,642 atoms and is wrong**. Instrumentation shows 118 `too_wide` calls in the dialect suite, 12 returning `True`, **0 with any non-ASCII argument**. The generator must synthesize `SELECT` lists of N multi-byte identifiers whose summed rendered code-point width straddles 80, under `pretty=True` **and** `dynamic=True`. A generic Unicode fuzzer will not construct that.
5. **`fuzz_comments.py` — new, mandatory.** `sanitize_comment` (45 calls, all ASCII in corpus) with `*/`, `/*`, non-ASCII, and astral characters; plus `indent`/`_replace_line_breaks` with astral characters inside string literals.

Targets 4 and 5 exist because M4 and M5 were previously "solved by a lint that no test can confirm." A lint that no test confirms is a comment.

### 3.5 Why the corpus is the gate, and the bridge is not

The dialect suite is ~91% declarative: 4,467 `validate_identity` + 2,755 `validate_all` versus 683 imperative asserts. Those calls are pure data — (input SQL, read dialect, expected output SQL, flags) — so they transfer to a JS runner losslessly and run with zero Python at test time.

Independent instrumentation confirms the corpus is a strong oracle for the generator surface: **405 of 424** base `Generator` `*_sql` methods are exercised by the dialect suite. The 19 misses are `add_column_sql`, `booland_sql`, `boolor_sql`, `characterset_sql`, `check_sql`, `connector_sql`, `createable_sql`, `generatedouble_sql`, `generateint_sql`, `get_put_sql`, `jsoncast_sql`, `kill_sql`, `partition_by_sql`, `schema_columns_sql`, `semicolon_sql`, `tag_sql`, `token_sql`, `uncache_sql`. These get **hand-written native tests** at the phase that ports them — not `wontfix`, because they are reachable from user code even though upstream's suite misses them.

### 3.6 Native hand-ported suites

The corpus tests *transpilation*. It does not test the JS **data model** — `copy`, `eq`, `hash`, `parent`/`depth`, `meta`, mutation APIs. Those are tested by hand-porting `tests/test_expressions.py` into `test/expressions/` as real JS tests.

**Corrected 2026-08-25, verified directly against the pinned `tests/test_expressions.py`** (found by P2, which correctly refused to force a match rather than silently under- or over-counting): the "81 deep `assertEqual`s + 185 `assertIsInstance`es" figure above was never measured against real source and does not correspond to any natural counting of the file — the true totals are **283 `assertEqual`s + 120 `assertIsInstance`es across 71 tests**, and of those, only **6 tests (6 `assertEqual`s + 3 `assertIsInstance`es)** — `test_identifier`, `test_properties_from_dict`, `test_parse_identifier`, `test_literal_number`, `test_update_positions_empty_meta`, `test_pipe_and_apply` — have zero dependency on `parse_one`/`.sql()`. The remaining 65 tests build their fixtures via `parse_one` and/or assert on `.sql()` output, which do not exist until P3 (parser) and P4 (generator) land; almost all use the **default dialect** (2 of 65 explicitly need `bigquery`/`spark`).

**Ben's decision (2026-08-25, §9 Q6):** hand-port only the 6 parser-independent tests at P2 — that is P2's hard gate, not deferrable. The remaining 65 are tracked as an explicit P4 exit-criterion addition (§7 P4), not silently dropped: P4 is the earliest phase where both the base parser and base generator exist to run them for real. **This is a genuine, documented scope reduction from the original (wrong) 81/185 figure, not a quiet skip** — the whole point of §8.5's human-review gate and this section's original "not deferrable" framing was to prevent exactly that failure mode, which is why the deferral is written down here with the real numbers rather than absorbed into a rounder-sounding but fabricated count.

### 3.7 The structure channel: `sync_report.py`

Detecting breakage is not the same as propagating change. `tools/sync_report.py` emits, per upstream bump, a **method manifest diff**: for every ported file, methods added / renamed / deleted / body-changed, with line anchors on both sides. That is the work item generator for maintenance. Corpus diffs tell us *that* something broke; the manifest diff tells us *what to port*.

### 3.8 The bridge — scope, and its proven limits

`tools/bridge/` runs upstream's **imperative core tests** against the JS library via an NDJSON protocol and a Python proxy object. It is a *supplementary* gate for the ~1,419 `assertEqual`s that drive the Python API directly and have no declarative form.

Two corrections from review:

- **`test_build.py` is removed from the bridge list.** It is 228 lambdas, **38 of which apply Python operators to `exp` objects** (`x + 1`, `x // 1 → CAST(x / 1 AS INT)`, `x & 1`, `~x`, `x[...]`). Running these through a proxy would require reimplementing `__add__`/`__floordiv__`/`__and__`/`__invert__`/`__getitem__` inside the bridge — i.e. rebuilding the operator layer we already decided to hand-port. `test_build.py` becomes a hand-ported native suite in P5.
- **Proxy-satisfiability is proven at P0, not assumed at P5.** The bridge was narrowed (M15/M16) to exactly the tests that compare *object graphs* — the workload it is least able to serve. So P0 ships **two end-to-end bridge proofs**: `test_errors.py` (cheapest — 36 of 48 `assertEqual`s are string literals) and `test_transpile.py` (most representative). `CONTRACTS.md` records, per remaining module, whether it is proxy-satisfiable and why. Discovering the proxy's object-model ceiling at P5 would be a four-month-late surprise.

Bridge modules at P5: `test_transpile.py`, `test_errors.py`, `test_generator.py`, `test_time.py`, `test_schema.py`, `test_jsonpath.py`, `test_transforms.py`, `test_diff.py`. An **allowlist** (checked in, size stated in `UPSTREAM-NOTES.md`, one entry per PR justified) records the assertions the proxy cannot serve. 229 of 772 dialect methods are AST-introspecting today, growing ~6.5/month — the bridge is not the dialect gate, so this decay does not threaten conformance, but the count is printed in the weekly sync issue.

---

## 4. Runtime design and code layout

### 4.1 Layout

Mirrors upstream one-to-one:

```
src/
  _py/        num.js str.js re.js sort.js collections.js datetime.js  (Python semantics shims)
  _gen/       expr_meta.js dispatch/ unicode.js  (generated; never hand-edited)
  expressions/  parsers/  generators/  dialects/  optimizer/  typing/
  tokenizer.js tokens.js parser.js generator.js transforms.js schema.js time.js
  helper.js errors.js trie.js jsonpath.js
tools/        harvest/ astdump.py bridge/ closure.mjs ast_diff.mjs lint_*.mjs fuzz_*.py Dockerfile
corpus/       atoms.jsonl ast/ gen/ deny/ PROVENANCE.json
test/         runner.mjs expressions/ build/ ratchet.json
```

### 4.2 Fidelity rules, machine-checked

`tools/lint_fidelity.mjs` enforces: every ported file carries a `// py:` header naming its upstream path and the pinned commit; the exported method-name set equals upstream's; method **order** equals upstream's source order; `cpLen()` is used instead of `.length` inside `generator.js` and `time.js`; and both deny-lists (§4.6) are clean. It cannot check that a *body* is a transliteration — that is §8.5's human gate.

**New rule, added 2026-08-25: no raw control bytes (`\x00`–`\x1F` excluding tab/newline/CR) in committed source.** This recurred **four separate times** during P0 across two independently-developed branches, always via the same mistake — using a literal control byte as a string separator to avoid collision ambiguity (a NUL-separated `frozensetKey`, literal ESC bytes in ANSI color constants, a NUL-separated key in the ratchet's `gapKeyOf`, a NUL-separated cache key in `_py/re.js`). The damage is worse than a wrong value: `file`/`grep` silently classify the whole source file as binary the moment one lands, which neuters every grep-based check in this document (`lint_fidelity.mjs`'s own header/order checks included) without any error — exactly the vacuous-green failure mode §3.3 exists to prevent, just entering through source hygiene instead of test logic. The fix is always the same and always available: `JSON.stringify([...parts])` for a composite key, never raw-byte concatenation. A new standalone `tools/lint_control_bytes.mjs` (P0, wired into `spike/run_all.sh`) scans every source file under `src/`, `tools/`, `spike/` for bytes in that range and hard-fails — the fifth occurrence is a lint failure, not a fifth independent discovery. `lint_fidelity.mjs` (§4.2, a P2+ deliverable once ported files exist to check header/order fidelity against) should absorb this same check when it's built, rather than running two separate lints in parallel.

**Confirmed by the P0 go/no-go spike (2026-08-25):** `\p{...}` runtime regex escapes are banned outright inside `src/_py/` and `src/tokenizer*.js` — not a style preference, a correctness requirement. Measured: the best `\p{...}` candidate for `isprintable` diverges from CPython on 11,130 of 1,114,112 code points, and the corpus is 0.024% non-ASCII, so this would pass every corpus test and ship silently wrong (same hazard class as `too_wide`/R4). `isprintable`/`islower`/`isupper`/`isspace`/`istitlechar` must route through the generated tables in `_gen/unicode.js` (§4.3 item 3), which were verified exact over the full range. `lint_fidelity.mjs` denies any `\p{` literal in those paths.

### 4.3 Code generation, correctly scoped

An earlier draft claimed 12–18 kLOC of codegen savings. Measured: **~4.3 kLOC**, because 76% of dialect dict entries (and 94% in `generators/`) are *callables*, not literals. Codegen is therefore scoped to exactly four things:

1. `_gen/expr_meta.js` — the 1,048 expression classes: ordered `argTypes`, `requiredArgs`, traits, `initOwner`.
2. `_gen/dispatch/` — the resolved method-dispatch table per generator class (prototype walk, sorted, TRANSFORMS-beats-`*_sql`), used to *assert* the runtime builder, not to replace it.
3. `_gen/unicode.js` — full-range `isPrintable` / `isLowercase` / `isUppercase` / `isSpace` tables.
4. Derived lookup tables that are pure literals.

Behaviour tables are hand-written. `tools/seed_static.py` drafts the literal portion of each so the human starts from a filled skeleton.

### 4.4 The class-attribute inheritance model

sqlglot dialects override class-level dicts and sets (`TOKENIZER` settings, `FUNCTIONS`, `TRANSFORMS`, `PROPERTY_PARSERS`), merged by Python's MRO and by metaclass `__init_subclass__` logic. JS reproduces this with an explicit `registerDialect` / `initTokenizerSubclass` derivation performed once at module init, in upstream's declaration order, with the final state asserted against a generated snapshot (parity probe #4). Import-time mutation of `TRANSFORMS` in `dialect.py:304-309` is reproduced including its aliasing behaviour (R7).

### 4.5 Equality, hashing, and value-keyed containers

`Expression.__eq__`/`__hash__` are not structural equality: Python skips falsy args and conditionally lowercases. The relation is **consumed by `.index()` inside `_move_ctes_to_top_level`, which runs on every generate**, so getting it wrong is not academic. We reproduce Python's exact projection (falsy-skip + conditional lowercase), at **true 64 bits via two 32-bit lanes** — a 32-bit hash truncates and silently changes `while_changing()`'s fixpoint in `simplify`.

`_py/collections.js` provides `ExprSet`, `ExprMap`, and `frozensetKey` for the value-keyed containers upstream relies on. A dedicated differential harness harvests every `(a, b, a == b)` triple the Python dialect suite produces and asserts the JS relation matches.

`_py/sort.js` provides `pySortedTuples`, reproducing Python's tuple comparison including the `exp.Xor` branch where `Expr.__lt__` returns a *node* rather than a bool. The exact permutation on a tie is a CPython Timsort artifact; if it diverges, that surfaces as a corpus failure and goes to `wontfix` with a citation.

### 4.6 The hazard census and the deny-lists

Every hazard below was found by targeted probing, never by the test suite. Each is either shimmed in `_py/` or machine-listed in `corpus/deny/` with a CI lint requiring the site to route through the shim.

**Numeric.** `_py/num.js` provides `pyInt`/`pyFloat`/`pyDecimal` tagging, BigInt for arbitrary precision, `pyFloatToStr` (4 known divergences from `String(x)`), and `Decimal`. Scope (per review-B O5, verified): numeric literals are **echoed verbatim as strings** and never reformatted — `'SELECT 1.10' → 'SELECT 1.10'`, `'SELECT 1e300' → 'SELECT 1e300'` — so `Decimal` is required only where arithmetic happens: `optimizer/simplify.py:1254-1270`, reached on the v0 path via `generator.py:5387` (`_simplify_unless_literal`, gated on `LIMIT_ONLY_LITERALS`), plus `dialect.py:2457`, `generators/postgres.py:63`, `generators/teradata.py:22`. Measured: **63** such invocations across the whole dialect suite, not the 11,921 previously cited. `_py/num.js` is therefore scoped to `+ - * /`, unary neg, comparison, and `str()` — **not** a general CPython `decimal` clone. It stays in P0 and stays a go/no-go gate (the mixed-case `E+18`/`e+18` literal asserted at `test_snowflake.py:367` is real), but the budget drops from 60–100h to 40–70h. BigInt sites: `clickhouse:491`, `postgres:563`, `hive:131/508`, `dialect:2242`, `parser:10211`.

**Strings.** `pyStrip`/`pyLstrip`/`pyRstrip` and `isSpace` from full-range tables — Python `isspace()` and JS `\s` differ on exactly 6 code points (Python-only `{1c,1d,1e,1f,85}`, JS-only `{feff}`), and `generator.js` has 23 strip sites reaching `sanitize_comment` and `generate()`. `cpLen()` for `too_wide`. `pyRepr` for list/tuple/set/dict rendering inside error and warning messages. `islower`/`isupper` via `\p{Lowercase}`/`\p{Uppercase}` full-range tables (naive approximations are wrong on 292/187/984 code points depending on the approximation chosen). `isPrintable` gates `ESCAPED_SEQUENCES`.

**Three sites added by review B, all on the v0-or-near path, all corpus-invisible:**

- **UTF-8 byte length.** `parsers/clickhouse.py:63` — `len(sep_value.encode("utf-8")) == 1`. Corpus coverage is `splitByChar('', x)` only. `splitByChar('é', x)` must produce `Anonymous`, not `Split`; a `.length` port produces `Split`. Fix: `utf8Len()`; deny-list `.length` on strings inside `parsers/`.
- **`chr()` beyond the BMP.** `generators/singlestore.py:25` — `chr(int(m.group(1), 16))`. `String.fromCharCode` truncates above `0xFFFF`, and Python `chr()` also accepts lone surrogates. Fix: `pyChr()`; deny-list `String.fromCharCode` outright.
- **Format-spec mini-language.** `parsers/dremio.py:65` — `f"{int(year.this):04d}-..."`. `padStart` diverges from `:04d` on negatives (`0-01` vs `-001`). Only 3 sites repo-wide. Fix: `pyFormatInt(n, width)`.

**Regex.** `_py/re.js` with a hand-written Python-regex group counter and validity oracle (needed because `generator.py:1667` and `bigquery.py:127` build regexes at *runtime*, so build-time rewriting is impossible), `pyReEscape` producing `/u`-valid output, and `re.sub` template translation. `\w` maps to `[\p{L}\p{Nd}\p{Nl}\p{No}_]` — this gates identifier quoting, so it is directly output-visible.

**Verified by a dedicated differential corpus (2026-08-25, `p0-regex-corpus`): 2,867/2,867 on every pattern sqlglot actually reaches, 0 fail, 0 skip.** `[\p{L}\p{Nd}\p{Nl}\p{No}_]` is confirmed the correct `\w` mapping — 0 CPython-only code points on a full-range sweep, where the more obvious `\p{Alphabetic}` and `[\p{L}\p{M}\p{Nd}\p{Pc}]` both fail — and it is genuinely output-visible: `exp.select(exp.column('café')).from_('t').sql()` is `SELECT café FROM t`; a naive `\w` port quotes it (`SELECT "café" FROM t`). Two residuals, both accepted:
- **Unlike the four `_py/str.js` predicates, `\w`/`\d` genuinely cannot route through a static generated table** — they're embedded inside dynamically-constructed, dynamically-compiled regexes (`generator.py:1667`, `bigquery.py:127`), so they must use a real runtime `\p{...}` JS regex at match time. The residual divergence measured is 100% attributable to Unicode-DB version skew (Node's ICU 16.0 vs the harvesting interpreter's `unicodedata` 13.0.0) — the same axis as R6/§5.2's Unicode finding, but for a construct that can't be worked around the way the static predicates were. `corpus/PROVENANCE.json` records both `unidata_version` (harvesting interpreter) and the consuming runtime's `process.versions.unicode`, so the gap is measured and visible, not silently absorbed.
- **`\N{NAME}` (Unicode character-name escapes) is accepted as an unsupported gap, not built.** Exact validity requires shipping a full Unicode character-name table for a construct sqlglot's real patterns don't reach (BigQuery, the one dialect with runtime-constructed patterns using this feature space, uses RE2 semantics regardless). Recorded in `CONTRACTS.md`'s `_py/` surface notes rather than implemented.

Four other divergences were found and closed, each worth naming because a competent port gets them wrong silently: `re.escape` output that doesn't compile under `/u`; a lone `]` (valid Python regex syntax, a JS `SyntaxError` — reachable via `UESCAPE ']'`); CPython's `must_advance` empty-match scan protocol (reachable via `UESCAPE '|'`); and match offsets being code points, not UTF-16 units — that last one is corpus-invisible upstream, the same hazard class as `too_wide`.

**Datetime — promoted to P0 (fixes review-B O1, the most serious finding in either review).** `sqlglot/time.py:65-85`:

```python
parsed = datetime.datetime.fromisoformat(timestamp_literal)
subsecond_digit_count = len(str(parsed.microsecond).zfill(6).rstrip("0"))
```

The docstring says it: *"Python prior to 3.11 only supports 0, 3 or 6 digits in a timestamp literal."* The `except ValueError: return 0` swallows the difference. Measured on this box (CPython 3.9.25):

```
'2023-01-01 12:13:14.1234'   -> 0     # 3.11+ returns 6
'2023-01-01 12:13:14.123456' -> 6
'2023-01-01 12:13:14Z'       -> 0     # 'Z' rejected pre-3.11
```

This reaches output SQL through `dialect.py:1796` (`timestrtotime_sql(..., include_precision=True)`) → `CAST(x AS TIMESTAMP(6))` vs `CAST(x AS TIMESTAMP)`, consumed by `generators/trino.py:45`, `mysql.py:203`, `tsql.py:111`, `postgres.py:397`, and it is live in the corpus at `test_dialect.py:848,856`.

**So the golden expectations encode the interpreter version that harvested them.** Q5 previously framed the 3.9 floor as a tooling-lifetime problem; it is a *corpus-provenance* problem, and when upstream bumps its floor, re-harvesting silently changes expected SQL and the ratchet either reports a false regression or absorbs it — the exact F1 mode we claim to have closed. Three-part fix: (a) `corpus/PROVENANCE.json` records the exact CPython **patch** version, and the runner hard-fails on mismatch without `--rebaseline`; (b) `pyFromIsoFormat` in `_py/datetime.js` implemented against a *named, pinned* CPython grammar — 3.9's restricted subset, not ISO-8601, and emphatically not `new Date()` — moved from P10 to **P0**; (c) a targeted differential test over fractional-digit counts 0–9 × {`Z`, `+00:00`, none} × {`T`, space}.

**Deny-lists, all machine-generated, all CI-linted:**

- `corpus/deny/operators.json` — **115 operator-overload sites inside `sqlglot/` itself** (corrected 2026-08-25 from an earlier estimate of 68 — see below), e.g. `dialect.py:1782` (not `:1778` as earlier cited — the one example used to justify this mechanism had already drifted from upstream) builds AST with `exp.Length(...) - exp.paren(expression.expression - 1)`; `((value - 1) // 32768) + 1` on an `exp.Expr` builds nodes, not numbers. The agent contract ("transliterate; never use `Number` for SQL literals") *actively produces wrong code* at these exact lines, which is why they must be listed rather than reasoned about.
- `corpus/deny/implicit_str.json` — **58 SQL-rendering implicit `__str__` sites** (+2 `__repr__`) that render with the default dialect (corrected 2026-08-25 from an earlier estimate of 26).

**Deny-list census corrected 2026-08-25** (`p0-regex-corpus`, a real Python-AST walk over `sqlglot/`, not tests): `operators.json` **115** (not 68, 1.7×), `implicit_str.json` **58 + 2** (not 26, 2.2×), `py_builtins.json` **17 confirmed sites** (the plan previously cited 3 illustrative examples, not a full count — all 3 present, 14 more found). The plan's own stated justification for `py_builtins.json` — that the `_py/` surface was derived from recon briefs rather than an exhaustive census — turned out to apply to the other two lists as well. **Confidence is stratified, not uniform:** 24/115 operator sites and 13/58 implicit-str sites are labeled `low` confidence, because `.this` holds a plain `str` on some node classes and static analysis alone can't always distinguish an `Expr` operand from a scalar one. A companion `instrument_operators.py` (patches all 26 node-building dunders and runs upstream's suite to turn static verdicts into ground truth) is written and committed but **has not yet been run** — it hits the same auto-mode permission gate the harvester did, and needs the same kind of explicit authorization before it can upgrade `low`→`confirmed`. Until then, `low`-confidence sites get an acknowledgement marker in the lint (proves a human/agent looked at the line) rather than a hard guarantee.
- **`corpus/deny/py_builtins.json` — new (fixes review-B O4).** The `_py/` API surface was previously derived from recon briefs rather than from an exhaustive census, which is how the three sites above were missed. This file is a *generated* enumeration of every call to `encode`, `chr`, `ord`, `zfill`, `rjust`/`ljust`, `partition`, `splitlines`, `rsplit`, `startswith`/`endswith` with tuple args, and every f-string format spec, with a lint requiring each site to route through `_py/`. Same mechanism as the operator and implicit-`__str__` lists; it just needed to cover builtins too. Regenerated on every upstream bump.

**Indexing.** The tokenizer returns `{tokens, codePoints}`; `errors.js` and `parser.js` slice the **code-point array**, never the JS string, so error columns and `highlight_sql` are correct under astral characters. Frozen in `CONTRACTS.md` at P0 because three components depend on it.

**Determinism.** Verified: `name_sequence` (`helper.py:260`) is per-`Generator`-instance (`generator.py:928`) and a fresh generator is constructed per call, so output is intra-process deterministic — confirmed by three identical repeat transpiles across three dialects. No hidden global counter.

### 4.7 Recursion depth — a structural gap, not a shim

*Added after the adversarial reviews; missed by all four architects and both reviewers.*

sqlglot is a recursive-descent parser with a recursive generator, and **Python and JS differ here in kind, not degree**:

| | Python | JS |
|---|---|---|
| limit | `sys.setrecursionlimit`, tunable at runtime | fixed per engine, **not tunable from library code** |
| failure | `RecursionError` — a normal, catchable exception | `RangeError` — catchable, but unwinding mid-generate is not a supported recovery path |
| headroom | commonly raised to 10k–100k | measured below |

**Measured on this box (Node v22.12.0):** a trivial frame overflows at depth **9,160** (single self-call, one real stack frame per recursion level); a frame resembling a real generator method (5 args, several locals, string building) overflows at **4,225**.

**Re-measured 2026-08-25 via `spike/fuzz_depth.mjs` (P0 item 7's actual fuzzer, worst-of-three trials): trivial 4,125, generator-like 3,350, parser-like 3,575.** These are lower than the figures above, and the gap is fully reconciled, not a discrepancy left open: `fuzz_depth.mjs`'s `measure()` driver calls each frame shape as `fn(run, d)` — a separate closure invocation that itself calls `run` again — so every logical recursion level costs **two** real V8 stack frames (`run` → `fn` → `run`) instead of one. Replicating that exact structure against the original trivial shape reproduces the lower number almost exactly (4,228 vs 4,125, the residual being ordinary run-to-run JIT/stack variance, consistent with worst-of-three sampling). **This does not change the conclusion — it strengthens it.** The harness's extra indirection frame makes its numbers an *underestimate* of the bare engine ceiling, which is the conservative direction for setting `DepthLimitError` thresholds: the gap to upstream's N=10,000 test is larger than the original single-shot measurement suggested, so the trampolines in item 3 below remain non-optional either way. `CONTRACTS.md` §9.1 records both figures rather than overwriting one, and the suggested `DepthLimitError` threshold (0.6× the harness-measured generator-like ceiling) is deliberately built on the more conservative number.

**Upstream has a live test that exceeds this.** `tests/dialects/test_redshift.py:524` builds a 10,000-element `VALUES` clause — comment: *"to ensure we don't get RecursionError"*. And `generator.py:2742-2746` documents the hazard explicitly:

```python
if self.pretty:
    # This may result in poor performance for large-cardinality `VALUES` tables, due to
    # the deep nesting of the resulting exp.Unions. If this is a problem, either increase
    # `sys.setrecursionlimit` to avoid RecursionErrors, or don't set `pretty`.
    query = reduce(lambda x, y: exp.union(x, y, distinct=False, copy=False), selects)
```

Note the asymmetry that makes this tractable: the **non-pretty** path (`generator.py:2750`) is an iterative `" UNION ALL ".join(...)` and is safe at any width; only the **pretty** path builds the left-nested `Union` chain. `test_redshift.py:529` asserts the non-pretty path at N=10,000, so **the corpus passes without ever touching the recursive path** — the same corpus-invisibility pattern as `too_wide` (§4.6, R4).

**The dangerous class is recursion that grows linearly with input size, not with query nesting:** left-associative binary chains (`a OR b OR c …` → nested `Or`), `UNION` chains, and the `VALUES`→`UNION` pretty conversion. Depth ∝ N, so a 5,000-term `IN`-to-`OR` rewrite overflows a realistic JS frame. Subquery/paren nesting is depth ∝ *nesting depth*, and is not the concern.

**Mitigation, in order:**

1. **`fuzz_depth` — a sixth mandatory P0 fuzz target.** Generates linear-growth inputs (`OR` chains, `UNION` chains, wide `VALUES`, `IN` lists) at increasing N against parse, generate, and generate-`pretty`, and reports the empirical max safe N per path per dialect. That number goes in `CONTRACTS.md`; it is measured, never assumed.
2. **An explicit depth counter in `Parser` and `Generator` raising a structured `DepthLimitError` before the engine's `RangeError`.** This restores Python's contract — a catchable, typed error at a known threshold — instead of an engine artifact at an unknown one. Threshold set from (1) with margin, configurable via generator options, which is the closest honest analogue to `setrecursionlimit`.
3. **Trampoline the two paths where depth ∝ N, and only those:** `Generator.sql()` dispatch over left-nested binary chains, and the `VALUES`→`UNION` pretty reduction. An explicit work-stack there is a *local* deviation from transliteration, so it is listed in `CONTRACTS.md` with its Python anchor and exempted from `lint_fidelity.mjs` **by name** — not by an agent's judgement.

**Explicitly rejected:** `node --stack-size`. It is a V8 flag unavailable to browser consumers and to anyone importing this as a library, and raising it past the OS thread stack converts a clean `RangeError` into a **segfault**. A zero-dependency library cannot require a runtime flag to be correct.

**Residual:** the pretty/`VALUES` path may retain a lower max-N than CPython at default settings. If so it is documented in the README with the measured number rather than papered over — matching upstream, which also documents rather than solves it.

---

## 5. Upstream tracking

### 5.1 Pinning policy

Pin `91119bc` through **P4**; **continuous resync from P5**. Upstream ships every ~7.3 days, ~33% of commits carry a breaking-change marker, and net drift is ~85 LOC/day, ~98% additive. Pinning through the whole of v0 accumulates ~10,000 lines of drift into one catch-up cliff; resyncing from day one costs ~20% of an agent while the base layer is still churning under us. Splitting at P5 — where the base layer stabilizes and upstream hunks start applying meaningfully — halves the cliff at modest cost. Needs Ben's sign-off (Q4).

Weekly: `make resync` produces (a) corpus diff → ratchet quarantine per §3.3 rule 4/5, (b) manifest diff → work items per §3.7, (c) regenerated deny-lists, (d) an issue with counts, allowlist size, and `NEEDS_BASE` queue depth.

### 5.2 Toolchain container — built from upstream, not pinned against it

The runtime artifact has zero dependencies. The **verification** toolchain needs python3 and a readable upstream clone. This box runs CPython **3.9.25**; upstream declares `requires-python = ">=3.9"` — zero headroom — and has already landed 3.9 deprecation (`1a10806`, `9bd7e7c8`).

The earlier mitigation ("containerize with a pinned CPython") solved the wrong direction: the container's job is to *run upstream's suite*, so the moment upstream's floor moves past the pin, the container can no longer do its job. Pinning would protect today's corpus and destroy upstream tracking — the thing Ben actually asked for.

Corrected: `tools/Dockerfile` builds CPython from **the pinned upstream tag's own declared `requires-python`**, is rebuilt as part of every resync, and the resync job fails loudly with `toolchain Python floor moved 3.9 → 3.10` rather than silently emitting a stale corpus. `corpus/PROVENANCE.json` records the exact patch version (CPython patch releases have changed float `repr` and `Decimal` behaviour before, and this corpus is byte-exact) **and the `unicodedata` version** — the P0 go/no-go spike (2026-08-25) found the generated Unicode tables (§4.3 item 3) are only exact for the interpreter that harvested them; Node 22's ICU ships Unicode 16.0 against this box's CPython 3.9.25 / `unicodedata` 13.0.0, an independent version axis from the CPython patch with an 11,130-code-point blast radius on `isprintable` alone if conflated or ignored. The regex differential corpus (also 2026-08-25) hit the same axis from the runtime side — `\w`/`\d` inside dynamically-compiled patterns must use a live JS `\p{...}` regex rather than a generated table (§4.6 Regex), so `PROVENANCE.json` also records the *consuming* runtime's `process.versions.unicode` alongside the harvesting interpreter's `unidata_version`, so both ends of that gap stay measured.

### 5.3 Licensing and attribution — before the first ported line

This is unambiguously a derivative work: line-by-line transliteration with `// py:` anchors, plus a corpus harvested verbatim from upstream's test files. The target repo's `LICENSE` currently reads `MIT License / Copyright (c) 2026 Airbrx` with no mention of sqlglot; upstream is `MIT License / Copyright (c) 2026 Toby Mao`. MIT requires the upstream copyright and permission notice in all copies and substantial portions.

**Required before any port PR merges:** `LICENSE-sqlglot` (verbatim upstream MIT); `NOTICE` stating derivation from sqlglot at commit `91119bc`; README attribution; a CI check that the commit in `NOTICE` matches `UPSTREAM.txt`; and explicit confirmation in `NOTICE` that the redistributed corpus is covered. This is a P0 blocker, not a launch-day task.

---

## 6. Optimizer scope: three tiers

Not all of `optimizer/` is optional, and the previous plan's tiering was wrong in one place.

- **Tier A (P3, parser-path).** The small helpers the parser itself calls.
- **Tier A′ (P4, generate-path, mandatory).** `optimizer/simplify.js` (1,880 LOC). Previously deferred; it runs on the **plain generate path** via `generator.py:5387` (`_simplify_unless_literal`, `LIMIT_ONLY_LITERALS`). v0 cannot ship without it.
- **Tier B (P5).** `scope.js` (1,111), `annotate_types.js` (1,112), `schema.js` (813), `typing/index.js` (379), `typing/snowflake.js` (564). Required for the last ~18% of `test_snowflake.py`.
- **Tier C (P10).** `qualify_columns` (1,319), `merge_subqueries` (557), `pushdown_*` (568), `unnest_subqueries` (345), `canonicalize*` (589), `normalize*` (329), `optimize_joins`, `eliminate_*`, `optimizer.js`, plus `lineage.js`, `diff.js`, `anonymize.js`, and the full `_py/datetime.js` (`relativedelta` with month-end clamping).

---

## 7. Phase plan

Every exit criterion below is a number emitted by `tools/closure.mjs` and the test runner, not a claim in prose. This is the direct fix for the two arithmetically unachievable exits the review found.

### P0 — Foundation, tooling, and the go/no-go. *1 agent, serial. ~150–225 agent-hours.*

**Ordered, because the first three items decide whether the project is the right shape:**

1. **Day 1–3: the byte-exactness spike.** `Decimal` `+ - * /` + `str(Decimal)` + `str(float)`, fuzzed against CPython; and a full `sys.maxunicode` sweep comparing `isprintable`/`islower`/`isupper`/`isspace` against candidate `\p{...}` classes. Green → proceed. **Red → re-plan on day 3, not on day 40.** Previously this signal sat at the *end* of a 4–6 week serial phase; that was the worst scheduling decision in the plan.
2. **Day 3–6: the calibration spike.** Port one real file to green and publish measured LOC/agent-hour before committing to §10's numbers. Target: `sqlglot/time.py` (688) + `helper.py` — chosen over the reviewer's suggestion of `tokens.py` because these are pure functions that can be differential-tested directly at P0, whereas `tokens.py` has no oracle until P1. Re-baseline after P1 and again after P3.
3. **Day 6: licensing** (§5.3). Blocks all port PRs.
4. `CONTRACTS.md` frozen (§8.2).
5. `_py/num.js` (scoped per §4.6), `_py/str.js`, `_py/re.js`, `_py/sort.js`, `_py/collections.js`, **`_py/datetime.js::pyFromIsoFormat`**.
6. Harvester (Validator seam), `astdump.py`, generate oracle **including `unsupported_messages`**, `closure.mjs`, runner, ratchet, 6 parity probes, `seed_static.py`, `sync_report.py`, toolchain container.
7. **Six** fuzzers (§3.4) including the two new targeted ones and **`fuzz_depth`** (§4.7).
8. Generated deny-lists: operators, implicit-`__str__`, **py_builtins**.
9. **Two end-to-end bridge proofs**: `test_errors.py` and `test_transpile.py` (§3.8).

**Exit:** all **six** fuzzers green; **`fuzz_depth`'s measured max-N per path recorded in `CONTRACTS.md` and `DepthLimitError` thresholds set from it** (§4.7); calibration LOC/hour published; closure curve and the full §3.2 per-phase table published; both bridge proofs green; deny-lists generated; container reproducible; licensing landed.

### P1 — Tokenizer. *2 agents. 1,809 LOC.*
`tokenizer_core.js` (1,217) + `tokens.js` (592) + `trie.js`, with code-point threading per the frozen contract. **Exit:** parity probe #1 (token streams byte-exact across all corpus inputs); `fuzz_unicode` green over tokenization.

### P2 — Expressions. *2 agents. 11,882 LOC.*
The 1,048 expression classes: trait mixins + `TRAITS`, `_gen/expr_meta.js` wiring, `defineExpr`, the 24-entry `INIT_HOOKS` table, `equals`/64-bit `hash`/`ExprSet`/`ExprMap`/`frozensetKey`, `copy()` (carrying `_hash` per `core.py:1015`, sharing non-Expr scalars by reference), `_to_s`/`repr`, `update_positions`, `add_comments` + `sqlglot.meta` directive extraction, `astLoad`. `builders.js` (1,148) is the third-largest piece; give it a strong agent.

**Exit:** (1) probes #2/#3 green — 1,048 `EXPR_CLASSES`, 629 `FUNCTION_BY_NAME`, 563 `ALL_FUNCTIONS`, ordered `argTypes` + `requiredArgs` + `traits` + `initOwner` for every class (5,240 checks). (2) **Hand-ported `test/expressions/` native suite green — hard gate, scope corrected 2026-08-25 (§3.6): the 6 tests with no `parse_one`/`.sql()` dependency (`test_identifier`, `test_properties_from_dict`, `test_parse_identifier`, `test_literal_number`, `test_update_positions_empty_meta`, `test_pipe_and_apply`), not the originally-cited 81/185, which was never a real measurement. The other 65 tests are P4's problem (below), tracked explicitly, not dropped.** (3) For all AST-oracle rows, `astDump(astLoad(row.ast))` deep-equals `row.ast` **and** `_to_s(astLoad(row.ast))` byte-matches `row.repr`. (4) `DATE_ADD` unit-normalization test (kwargs uppercases; `astLoad` does not). (5) `equals` reproduces Python's relation on the four falsy/case-insensitivity cases.

### P3 — Parser base + Snowflake parser. *1 blocking agent, then 6–8 through the stub queue. 11,920 LOC.*

Blocking step (~500 LOC + seeding): `SENTINEL_NONE` falsy token, cursor primitives, semicolon chunking, `ErrorLevel`/`raise_error`/`highlight_sql` (byte-exact ANSI over the code-point array), `_try_parse` backtracking, the 20+ dispatch table shapes, optimizer Tier A, **plus a ~200-LOC minimal generator kernel** — non-negotiable because `parser.py:5491` (`fld.sql()`) and `parser.py:8069` (`default.this.sql().upper() == "END"`) call the generator on the parse path. Then seed **364 `_parse_*` stubs** (7,567 LOC) **and the class-level tables** (§8.1 Rule 2′).

**Exit:** AST oracle exact match — including `meta` line/col/start/end — on 100% of Snowflake identity + read SQLs, with no full generator in existence. The 18 `check_command_warning` log strings byte-exact (via `error_message_context`, not a hardcoded 100). `ParseError.errors` structure matches `test_errors.py`. `fuzz_unicode` green over error-message column positions. **The minimal generator kernel's `Select`-clause coverage at the pivot-subquery call site is corpus-complete but not grammar-complete (R12) — broadening it to match upstream's actual unrestricted call is stub-queue follow-up, not a P3 blocking-step gate.** **"Snowflake parser" in this phase's title has so far meant base `parser.js` exercised against Snowflake SQL, not `parsers/snowflake.py`'s own 1,440 LOC `SnowflakeParser(parser.Parser)` subclass (`STATEMENT_PARSERS`/`FUNCTIONS`/`RANGE_PARSERS`/`NO_PAREN_FUNCTIONS` overrides, `SHOW`, stage refs, `FLATTEN`, dialect function-name mappings, ...) — that component has never been scoped or started (R15). The 100%-exact exit criterion is therefore not reachable until it lands; base-parser progress was merged ahead of it by explicit decision, not silently declared done.**

### P4 — Generator base + Snowflake generator. *1 blocking agent, then 6–8. 10,573 LOC.*

Blocking step: `_buildDispatch` (prototype walk, sorted, TRANSFORMS-beats-`*_sql`, asserted against `_gen/dispatch/`), module-level dispatch cache, `sql()` exact-class dispatch with only `Func`/`Property` fallbacks, `sep`/`seg`/`indent`/`expressions`/`maybe_comment`/`sanitize_comment` (using `pyStrip`), `unsupported()`/`ErrorLevel`, `SENTINEL_LINE_BREAK` pretty machinery, `function_fallback_sql` iterating `argTypes` in declaration order, `too_wide` using `cpLen`. Then seed 432 `*_sql` stubs (4,107 LOC) + `TRANSFORMS` (143 entries) + 126 settings. Then `generators/snowflake.js` (1,222) + `transforms.js` (1,083) + `jsonpath.js` (238) + **`optimizer/simplify.js` (1,880, mandatory)**.

**Exit:** **1,606 atoms closed under `{snowflake}`**, including all 82 `pretty=True` calls, the 5 `UnsupportedError` sentinels, and `identify=True`. **`unsupported_messages` matches the oracle on every generate-oracle row** (the 497-call fix). Probe #4 green (base 560, Snowflake 656). `RANDOM()` emits the exact mixed-case `E+18`/`e+18` literal asserted at `test_snowflake.py:367`. `fuzz_toowide`, `fuzz_comments`, **and `fuzz_depth`** green. **Trampolines landed on the two depth-∝-N generator paths and recorded in `CONTRACTS.md`; `test_redshift.py:524`'s N=10,000 `VALUES` case green on both the iterative *and* the `pretty` path** (§4.7). Deny-list lints green. **The 65 `test_expressions.py` tests deferred from P2 (§3.6) go green here — this is the earliest phase where both a base parser (P3) and base generator (this phase) exist to run them; 63 of 65 use the default dialect, 2 need bigquery/spark and may need to wait for those P9 dialects instead.**

### P5 — Slice complete: Snowflake + default + optimizer Tier B + bridge. *3–4 agents. 6,754 LOC.*

`dialects/dialect.js` (2,685: registry, `registerDialect` with all ~20 derivations, `get_or_raise` with the `"name, k=v"` grammar, `compareVersion` with a non-2⁶³ sentinel, `dialectEquals`, ~90 shared generator helpers). Optimizer Tier B. `tools/bridge/` full + `allowlist.json`. Hand-ported `test/build/` native suite (replacing `test_build.py` on the bridge).

**Exit:** **2,022 atoms closed under `{snowflake, default}`.** `identity.sql` 980/980; `pretty.sql` 29/29; `partial.sql` 7/7; `jsonpath/cts.json` 526/526. Bridge green on the eight modules in §3.8 with the allowlist checked in and its size stated. `test/build/` native suite green.

*No interim release here — Ben confirmed (§9 Q3, 2026-08-24): "We don't need an interim release." v0 ships at P8.*

### P6 — DuckDB. *8 agents (shard count). 5,394 LOC.*

`generators/duckdb.js` alone is 4,788 LOC — the largest file in the repo, 28% of all `generators/`; budget it as 4 sub-shards. **Not** a priority reshuffle: 449 of TestSnowflake's 572 `validate_all` calls write to DuckDB, and TestSnowflake cannot progress without it.

**Exit:** **3,417 atoms (21.8%).** `test_snowflake.py` closes **2,086 of 2,437 atoms (85.6%)** — *not* 100%, which was arithmetically impossible; the 351 open atoms need spark (55), bigquery (40), postgres (35), presto (23), hive (22), databricks (16), mysql (14), sqlite (10), redshift (9), trino (8), tsql (8), plus 84 read-entries from unimplemented dialects. `APPROX_QUANTILES(x, 3)` emits the 28-significant-digit array. `fuzz_decimal` green over `generators/duckdb.js`.

**Does not ship as v0.** Ben confirmed (§9 Q2, 2026-08-24): *"We will ship at P8."* DuckDB is still built here regardless — 449 of TestSnowflake's `validate_all` calls require it as an intermediate write target, so Snowflake conformance cannot progress past P4's 1,606 atoms without it. Building continues straight through to P8; there is no pause or release event at P6.

### P7 — Databricks chain. *4 sequential sub-phases (hive → spark2 → spark → databricks), 3 agents each. 2,357 LOC.*

Ben's priority #2, **confirmed as the correct #2 by the corrected data** (§9 Q1). Cannot start before P5: `dialects/hive.py:14` and `dialects/databricks.py:11` import `TypeAnnotator` at module level. Databricks' own churn is *majority typing* (14 of 39 touches).

**Exit:** **5,207 atoms (33.3%), +1,790.** TestDatabricks closes **203 of 222** (the remaining 19 need tsql/clickhouse/mysql/teradata — again, *not* 222). `test_hive.py`, `test_spark.py`, `test_spark2.py` atoms closed to their in-scope maximum. TestSnowflake rises to 2,202 (90.4%).

### P8 — Postgres + Redshift. *3 agents. ~1,742 LOC.*

Self-contained; no dialect-to-dialect inheritance. Reproduce `generators/postgres.py:311`'s explicit parent-`TRANSFORMS` filter rather than "fixing" it. Requires `compareVersion` for the four `postgres, version=…` keys.

**Exit:** **6,552 atoms (41.9%), +1,345.** TestPostgres closes 700 of 803.

**v0 SHIPS HERE.** Ben confirmed (§9 Q2, 2026-08-24): *"We will ship at P8."* v0 is Snowflake + Databricks chain (Hive/Spark2/Spark/Databricks) + DuckDB + Postgres/Redshift + default — the set the product requires at launch (§9 Q1: *"Our product requires Snowflake, Databricks, DuckDB and Postgres. Those go first."*). See §10 for the restated v0 effort table (P0–P8).

### P9 — Long tail. *1–2 agents per dialect. 17,680 LOC — 59% of the dialect layer, ~58% of remaining atoms.*

Ordered by **greedy marginal value** from the P8 base (machine-computed, not guessed): bigquery (+1,677), tsql (+1,101), presto (+1,027), mysql (+1,000), clickhouse (+792), exasol (+618), oracle (+573), sqlite (+356), trino (+338), singlestore (+336), starrocks (+276), teradata (+202), doris (+179), drill (+111), dremio (+100), athena (+72), materialize (+63), fabric (+49), dune (+44), tableau (+44), druid (+41), prql (+30), risingwave (+16), dax (+13), **solr (added 2026-08-25 — missing from this list in earlier revisions; found by direct enumeration of the real corpus, not the plan's prose)**, then the **12** versioned keys (**+22 total**, not the originally-cited 14/+28 — see the corrected census below). Each dialect also fills stubs in the shared base files (v0 leaves ~148 parser + ~252 generator methods unimplemented); §8.1 handles the contention.

**Dialect census corrected 2026-08-25, verified directly against the real corpus** (`corpus/atoms.jsonl`, computed by enumerating every distinct `read`/`write` value): **46 total dialect keys = 34 base** (33 named dialects + the `""` default) **+ 12 versioned** (`clickhouse` ×2, `duckdb` ×4, `postgres` ×4, `spark` ×2 — version strings that normalize to the same `compareVersion` bucket collapse, e.g. `duckdb, version=1.1` and `duckdb, version=1.1.0`), not the 38 base / 14 versioned cited earlier in this document. Also confirmed directly: **every versioned key appears only as a write target in the corpus, never as a read target** — so `compareVersion` is exercised exclusively on the generate path, never the parse path, which narrows where it needs to be correct.

**Exit:** 15,540/15,540 (§1's real harvested atom count, superseding the 15,642 estimate this line originally cited) minus a documented `wontfix` list. Quarantine empty. Native tests written for the 19 never-exercised generator methods (§3.5).

### P10 — Optimizer Tier C + remainder. *~9,000 LOC.*

**Exit:** 3,476 optimizer fixture pairs green (the 201 `# execute: true` *execution* assertions are `wontfix`; the SQL-comparison half is kept). Bridge green on `test_optimizer.py`, `test_lineage.py`, `test_anonymize.py`.

---

## 8. Parallel execution

**Assumed concurrency: 2.** `nproc = 2`, so agent-count ≈ wall-clock. Every mechanism below is correct at 1, at 2, and unchanged at 40. Phase agent-counts are *shard counts* (how work divides), not simultaneity claims; at concurrency 2 they drain as a queue.

### 8.1 Ownership

**Rule 1 — one file, one owner, one branch, one PR — enforced in CI, not by a hook.** The previous design (a local pre-push hook plus `.claims/*.json` committed to `main`) was not an enforcement mechanism: hooks are not distributed by clone, are not installed by default, and are bypassed by `--no-verify`. Worse, committing a claim per file meant every claim was a push to `main` — a serialization point generating exactly the rebases Rule 6 exists to avoid. **Corrected:** claim checking is a **required CI check** that computes changed-file overlap against the changed-file lists of currently-open PRs via the GitHub API. Zero repo writes, actually enforced. The pre-push hook survives only as fast local feedback, installed by `make setup`.

**Rule 2 — stub-seeding makes intra-file method conflict structurally impossible.** For `parser.js` (10,480), `generator.js` (6,370), `generators/duckdb.js` (4,788), the phase's blocking step emits **every method as a stub in upstream source order**:

```js
/** @param {Token} token @returns {Expr|undefined} */
// py: sqlglot/parser.py:6302
_parse_bitwise(token) { throw new NotPorted("_parse_bitwise", "sqlglot/parser.py:6302"); }
```

One task replaces exactly one stub; two agents produce non-adjacent hunks that git merges cleanly. These files become **method-owned**: claims are `.claims/parser.js#_parse_bitwise`. `grep -c NotPorted src/parser.js` is a free burndown.

**Rule 2′ — seed the class-level tables the same way (new).** Rule 2 as written covered method bodies and claimed intra-file conflict was "structurally impossible." It isn't, for the part that matters most. Measured: `parser.py` is 405 methods / 8,143 LOC, leaving **2,337 LOC of class body** — `FUNCTIONS` (648 entries), `STATEMENT_PARSERS`, `EXPRESSION_PARSERS`, `PROPERTY_PARSERS`, `ALTER_PARSERS`, `RANGE_PARSERS`, eight precedence maps, ~162 class attributes. `generator.py` is 484 methods / 4,996 LOC, leaving **1,374 LOC** of `TRANSFORMS` (143 entries) + 126 settings. The DDL shard adds to `PROPERTY_PARSERS`; the types shard adds to `TYPE_TOKENS`; the functions shard adds to `FUNCTIONS` — all single contiguous literals, and in P9 all 24 remaining dialect efforts touch them.

**Corrected:** seed these tables **one entry per line in upstream declaration order, each with its own `// py:<file>:<line>` anchor**, so an addition is a single non-adjacent line. Table **order** is CI-asserted against a `_gen/` snapshot, because §4.6 establishes that insertion order is observable in output SQL.

**Rule 3 — shared foundation frozen after each phase's blocking step, with a named owner.** `src/_py/*`, `helper.js`, `errors.js`, `trie.js`, and the registration helpers are edited only by a serialized foundation task. **Corrected (the queue previously had no owner and no latency bound, and P9 is exactly where its arrival rate spikes):** (a) a **named foundation owner per phase**; (b) the `NEEDS_BASE` queue **must be drained at the start of every work session** before any port task is pulled; (c) **strictly-additive** changes — a new exported function, no signature change to an existing one — may be made in place by the requesting agent under a `foundation-additive` label that forces human review; (d) queue depth is measured and published in the weekly report.

**Rule 4 — generated files: regenerate, never merge.** The previous mechanism (`.gitattributes merge=ours`) was a **no-op**: `ours` is not a built-in merge driver (git ships `text`, `binary`, `union`), it requires `[merge "ours"] driver = true` in each clone's local config, and config is not cloned — verified unset on this box (git 2.50.1). Worse, under Rule 6's rebase-only policy, "ours" is the *upstream* side, so even when configured it would keep the incoming file.

**Corrected:** delete the merge-driver line. The real mechanism is the CI gate that already exists — `make codegen && git diff --exit-code src/_gen` — plus a committed `_gen/.manifest.sha256` that CI verifies. `corpus/**` uses `merge=union` (a genuine built-in) since the JSONL files are append-ordered.

> **Deliberately rejected:** the reviewer's primary remedy — make `src/_gen/**` and `corpus/**` untracked build outputs. That would mean a JS-only contributor cannot run `node --test` without Python and an upstream clone, which contradicts Q5's stated goal. We keep them committed and rely on the regeneration gate for freshness.

**Rule 5 — shard on upstream's seams, never invented ones.** (a) One dialect = 4 files (`dialects/X`, `parsers/X`, `generators/X`, `typing/X`) = 1–2 agents, independent except along an inheritance chain (hive→spark2→spark→databricks is strictly sequential); (b) one `expressions/` file = 1 agent; (c) one grammar area within `parser.js`/`generator.js` = 1 agent, where areas are query/modifiers, DDL, DML, types, functions, properties, joins/set-ops — chosen because upstream's methods already cluster that way in source order, so an area is a **contiguous stub range**.

**Rule 6 — rebase, never merge.** Stub-seeded files rebase cleanly by construction.

### 8.2 Interface contracts, frozen at P0 in `CONTRACTS.md`

Freezing these on day one decouples the three serial lanes (tooling / runtime / port):

1. **`_py/` API surface** — every helper's name, arity, and Python-semantics reference, written *before* implementation, so port agents can call functions the runtime agent hasn't written. Now includes `utf8Len`, `pyChr`, `pyFormatInt`, `pyFromIsoFormat`.
2. **Tokenizer output shape** — `{tokens, codePoints}`.
3. **`_gen/` schema**, including the table-order manifest.
4. **AST oracle JSON shape** (`astdump.py` ↔ `astDump()`/`astLoad()`), including the `astLoad` no-`INIT_HOOKS` rule.
5. **Bridge NDJSON protocol**, plus the per-module proxy-satisfiability table.
6. **`NotPorted` error contract** and the stub JSDoc template.
7. **Corpus provenance schema**, including `python_version`, `unidata_version`, and the consuming runtime's `process.versions.unicode` (added 2026-08-25 per the P0 go/no-go spike and the regex differential corpus — see §5.2, §4.6). **Also record: `\N{NAME}` regex escapes are an accepted unsupported gap** — no faithful JS encoding without shipping a full Unicode character-name table, and unreached by any real sqlglot pattern (2026-08-25, `p0-regex-corpus`).

### 8.3 Task briefs carry their own oracle

Every task is a triple: (i) the Python `file:line-range`, (ii) the JS stub(s) to fill, (iii) **the exact command that proves it**:

```
node --test --test-name-pattern='^(a1f3c9d2|b2e4f001|...)$'
node tools/ast_diff.mjs --dialect snowflake --filter 'DATE_TRUNC'
python3 tools/parity/check.py --probe argtypes
```

An agent that cannot turn its command green does not open a PR. Because the AST oracle and the generate oracle both exist from P3/P4, parser agents and generator agents never block each other and never argue about whose bug it is — `ast_diff.mjs` prints a side-by-side `repr` diff and the runner labels the failure `PARSE MISMATCH` or `GENERATE MISMATCH`.

### 8.4 The failure queue *is* the scheduler

`node --test --json` groups failures by signature (error class + top JS frame + the `// py:` anchor on that line). `make triage` buckets them into `runtime-fixable` / `stub-missing` / `body-wrong` / `unknown` and emits one work item per bucket-file pair. At concurrency 2 the queue drains slower; no step assumes fan-out that doesn't exist.

### 8.5 Review gates

**CI, every PR:** (1) `node --test` — no unlisted atom fails, no `todo`/`wontfix` atom passes; (2) ratchet monotonicity + quarantine rules 4/5; (3) `lint_fidelity.mjs` — headers, method-name set, method order, table order, `cpLen`, all three deny-lists; (4) `lint_nodeps.mjs`; (5) `make codegen && git diff --exit-code src/_gen` + `_gen/.manifest.sha256`; (6) claim-overlap check via GitHub API; (7) parity probes for the touched area; (8) corpus provenance match; (9) NOTICE/UPSTREAM.txt commit match; (10) advisory `tsc --checkJs --noEmit`.

**Human review gate — the only thing CI cannot do.** Every PR touching `parser.js`, `generator.js`, or a dialect file is reviewed **against the Python source side-by-side**, and the reviewer's sole question is "is this a transliteration?" — not "is this good JS."

**Corrected sizing.** The previous claim ("median `_parse_*` is 11 LOC, so 60 seconds per method") used a statistic that hides the cost. Measured on `parser.py`: 364 `_parse_*` methods, median 11, **mean 20.8, p90 42, max 285**, and the **72 methods larger than 30 LOC hold 4,240 LOC — 56% of all parser method LOC**. Generators genuinely are small (432 `*_sql`, median 5, mean 9.5, only 20 methods >30 LOC holding 1,001 LOC).

So review is budgeted **by LOC, not by method count**: ~250 LOC/human-hour for the small tail, ~100 LOC/hour for methods >30 LOC. The 72 large parser methods are named explicitly in the task briefs as **two-reviewer items**. §10 now carries a human-hours row.

If Ben's answer is that an agent performs this review, say so plainly: R1's mitigation is then an LLM checking an LLM, the residual rises materially, and the honest statement is that transliteration fidelity is defended by lint plus the resync signal — with divergence discovered at the first resync rather than at review time.

### 8.6 Practical sequencing at concurrency 2

P0 is one agent alone (~4–5 weeks; unavoidable serial bottleneck, budgeted as such). The second agent is **not** idle: it writes `_py/` differential-test case tables, which require no JS to exist — that is exactly why P0's contracts are frozen first. P1–P2 run two agents on disjoint files. P3/P4/P6 each begin with one blocking agent (~2–4 days: primitives + stub-and-table seed) after which two agents cycle the stub queue. P7's sequential chain runs one agent deep while the second works P8/P9 dialects — after P5 there is always independent long-tail work, so the second slot is never idle.

---

## 9. Risks and questions for Ben

### Decisions — answered by Ben, 2026-08-24

All five resolved. Q2 changes the architecture (v0's boundary moves from P6 to P8); Q1/Q3/Q4/Q5 confirm the plan as written.

**Q1 — Priority order: confirmed, including DuckDB.** Ben's exact words: *"Our product requires Snowflake, Databricks, DuckDB and Postgres. Those go first."* This settles the one narrow open question (whether BigQuery should lead over Postgres at position #3 — see the marginal-atom table below): **Postgres stays at P8 as written; no swap.** It also confirms DuckDB's place in the build order, which was already forced by test dependency regardless of priority (§7 P6 — 449 of TestSnowflake's `validate_all` calls write to DuckDB) — Ben's answer independently corroborates the existing design rather than changing it.

*Supporting data (unchanged from the draft):* the previous plan draft had argued against Ben's stated order using a number that was 5.4× too small — `databricks +333 atoms`, the marginal for adding `databricks` alone, when Databricks cannot be added without Hive/Spark and P7 delivers the *chain* as its unit of work. Recomputed correctly from the P6 base `{snowflake, default, duckdb}`:

| addition | marginal atoms | LOC (4 trees) | atoms/kLOC |
|---|---|---|---|
| **`{hive, spark2, spark, databricks}`** | **+1,790** | 2,357 | **760** |
| `{bigquery}` | +1,403 | 2,139 | 656 |
| `{postgres, redshift}` | +1,259 | ~1,742 | 723 |
| `{tsql}` | +833 | 1,706 | 488 |
| `{databricks}` alone | +333 | 288 | — |

Databricks-chain is #1 by marginal atoms and best-in-class per kLOC — Ben's priority #2 was already the data-confirmed correct choice before he answered.

**Q2 — v0 ships at P8, not P6.** Ben's exact words: *"We will ship at P8."* v0 is now Snowflake + Databricks chain + DuckDB + Postgres/Redshift + default — **42.0% atom closure (6,522/15,540)**, not the 21.8% at P6. This adds P7 (Databricks chain, 80–130 agent-hours) and P8 (Postgres+Redshift, 50–80 agent-hours) to the v0 critical path — see the restated effort table in §10. Calendar impact: v0 moves later by the 2–4 months this question flagged; the restated wall-clock estimate is in §10.

**Q3 — No interim release.** Ben's exact words: *"We don't need an interim release."* No release event at P5 or P6; the optimizer Tier B work at P5 (~3,400 LOC) still happens exactly as scheduled — it was never optional, only the question of shipping an intermediate build around it was open. Moot in practice now that v0 already extends to P8 regardless.

**Q4 — Pin through P4, continuous resync from P5. Confirmed.** Ben's exact words: *"Sounds good. Pin first."* Plan proceeds exactly as written in §5.1 — no change.

**Q5 — Build-time Python dependency for verification tooling: confirmed acceptable.** Ben's exact words: *"This is acceptable (and needed)."* Plan proceeds exactly as written in §5.2 — no change. Runtime remains zero-dependency, guaranteed.

### Decisions made during execution

**Q6 — P2's `test/expressions/` native-suite scope, 2026-08-25.** The originally-cited "81 deep `assertEqual`s + 185 `assertIsInstance`es" (§3.6) was found by P2 to not match any real measurement of the pinned `tests/test_expressions.py` (true totals: 283/120 across 71 tests; only 6 tests have zero `parse_one`/`.sql()` dependency). Options presented: (1) hand-port only the 6 parser-independent tests now, deferring the rest as a tracked P4 exit criterion; (2) triage all 71 and hand-port whichever can be faithfully rewritten via direct construction; (3) full hand-port including parser-dependent fixtures. **Ben chose (1).** §3.6 and §7 P2/P4 updated with the real numbers and the explicit P4 tracking addition.

### Risks, ordered by expected damage

**R1 — Transliteration fidelity is enforced by discipline, not by machine.** Lint checks names, order, headers, tables; it cannot check that a body is a transliteration rather than an idiomatic rewrite. One agent "cleaning up" a 40-line if/elif into a lookup table produces a file that no longer diffs against upstream, invisible until the first resync. *Mitigation:* §8.5's human gate, now correctly sized and budgeted. *Residual:* real, and larger than previously stated because 56% of parser method LOC sits in a 72-method tail that the median-based estimate hid. This is the price of hand-porting and the plan's weakest joint.

**R2 — Effort. ~1,850 agent-hours + ~230 human review-hours for v0 (P0–P8) at the midpoint** (revised from ~1,310/~200 for the old P0–P6 boundary, now that Q2 extends v0 through P7 and P8 — see §10). At concurrency 2 that is roughly ~5 months at 24/7, ~13 months at an 8-hour day (linear extrapolation from the P0–P6 wall-clock table pending recalibration — see §10). **This estimate rests on an LOC/hour constant that is now measured on day 3–6 of P0 rather than asserted** — a 2× miss is the difference between roughly 6.5 and 13 months at 24/7, so the calibration spike gates the schedule, not just the plan's credibility.

**R3 — the "morale cliff" risk is substantially reduced by Q2, but a smaller internal one remains.** The original concern was that v0 shipping at 21.8% (P6) would read as a letdown relative to "sqlglot in JS." Ben's Q2 answer (v0 ships at P8, 41.9%) resolves the *public-facing* version of this risk — the released v0 is a materially more complete transpiler (Snowflake, Databricks, DuckDB, Postgres). The residual: P6 (21.8%) still exists as an *internal* milestone en route to P8, and TestSnowflake only reaches 85.6% there — teams watching progress mid-build should not mistake P6 for a release. *Mitigation:* publish the quadratic closure curve **and the full §3.2 per-phase table** at P0, and be explicit in status updates that P6 is a checkpoint, not a ship. The previous secondary metric — "% of `test_snowflake.py` closed, which hits 100% at P6" — **was false** (it is 85.6% at P6, 90.4% at P7, 100% only at the end of P9) and is replaced by two honest ones: **"% of in-scope atoms closed" (100% by construction at every phase)** and **"atoms closed / total," reported against the published curve**.

**R4 — Unicode and numeric hazards are a long tail of silent wrong answers.** Every one found so far was found by targeted probing, not by tests, because the corpus is 0.024% non-ASCII. Two of them — `too_wide` and `sanitize_comment` — are provably *unreachable* by the corpus: a `.length` port passes 15,642/15,642 and is wrong. *Mitigation:* **six** named fuzz targets with purpose-built generators (§3.4, §4.7), each a phase exit criterion. *Residual:* the fuzzers cover what we thought to fuzz. Assume more exist — **R11 was found after both adversarial reviews had signed off, which is the concrete demonstration of this residual, not a hypothetical one.** **Byte-exactness outside the corpus-covered surface rests on fuzzers, deny-lists, and review — stated plainly, not implied.**

**R5 — 115 operator-overload sites and 58+2 implicit-`__str__` sites inside `sqlglot/` itself** (corrected 2026-08-25 from earlier estimates of 68/26 — see §4.6). The agent contract actively produces wrong code at those exact lines. *Mitigation:* machine-generated deny-lists + CI lint, now extended to Python builtins (`corpus/deny/py_builtins.json`, 17 confirmed sites vs 3 illustrative examples originally cited). *Residual:* 24/115 operator and 13/58 implicit-str sites are `low`-confidence pending a runtime instrumentation pass (`instrument_operators.py`, written, blocked on the same permission gate as the harvester) that would upgrade them to ground truth; the census also derives from `tests/dialects` coverage, so a site reachable only from an unported dialect could be missed, and re-runs on every bump.

**R6 — Corpus provenance is interpreter-version-dependent.** `subsecond_precision` returns different values on CPython 3.9 vs 3.11 for the same literal, and that difference reaches output SQL through four generators. *Mitigation:* `PROVENANCE.json` + hard-fail on version mismatch + `pyFromIsoFormat` against a pinned CPython grammar. *Residual:* other version-dependent surfaces may exist; float `repr` and `Decimal` have changed across CPython patch releases before, which is why the **patch** version is recorded.

**R7 — P9 contention on the shared base files and tables.** 24 remaining dialect efforts filling stubs in `parser.js`/`generator.js` (~3,801 method-LOC after v0) plus the shared class-level tables. *Mitigation:* method-level claims + Rule 2′ per-line table seeding + CI-enforced claims. *Residual:* at concurrency 2 this is a throughput ceiling, not a correctness problem.

**R8 — `dialect.py`'s import-time `TRANSFORMS` mutation is import-order dependent** (`dialect.py:304-309`). Today all 10 generators with `SUPPORTED_JSON_PATH_PARTS` also redeclare `TRANSFORMS`, so the mutation is local; we reproduce the aliasing. A future upstream dialect setting one without the other diverges, and probe #4 compares *final state*, not order. Documented; accepted.

**R9 — Bridge allowlist growth.** 229 of 772 dialect methods are AST-introspecting, growing ~6.5/month. Not the dialect gate, so it does not threaten conformance, but it is the mechanism most likely to quietly grow. CI requires an `UPSTREAM-NOTES.md` entry per addition; the count is printed weekly.

**R10 — `Expression.equals` subtleties.** If §4.5's projection is wrong anywhere, the failure is a wrong `.index()` inside `_move_ctes_to_top_level`, which runs on *every* generate. *Mitigation:* a differential harness over every `(a, b, a == b)` triple the Python dialect suite produces.

**R11 — Recursion depth is a hard JS ceiling where Python has a tunable one (§4.7).** Measured: a realistic generator frame overflows at depth **4,225** on Node v22 (single-shot), **3,350 via the P0 item 7 fuzzer's worst-of-three harness** (lower because that harness adds one extra real stack frame of indirection per level — reconciled in §4.7, not a discrepancy); upstream ships a test at N=**10,000** (`test_redshift.py:524`) and documents the hazard in `generator.py:2742-2746`. Either figure lands well short of 10,000, so the conclusion is unaffected by which one is used. Recursion depth grows **linearly with input size** on binary/`UNION` chains, so this is reachable by ordinary machine-generated SQL, not just pathological input. *Mitigation:* `fuzz_depth` as a sixth mandatory P0 target to measure the real ceiling per path; a structured `DepthLimitError` raised before V8's `RangeError`; trampolines on the two depth-∝-N paths, named in `CONTRACTS.md` and lint-exempted by name. `--stack-size` rejected (unavailable in browsers; converts a clean error into a segfault). *Residual:* max-N on the pretty/`VALUES` path may stay below CPython's, and gets documented with its measured number. **Corpus-invisible** — `test_redshift.py:529` asserts the *iterative* path, so 15,642/15,642 can pass with the recursive path broken. Found after both adversarial reviews, which is itself evidence for R4's thesis that the corpus does not defend this class of hazard.

**R12 — P3's minimal generator kernel (§7 P3) only covers the `Select` clauses the corpus happens to exercise at its two-turned-seven mid-parse call sites, not the clauses upstream's grammar actually allows there.** `parser.py:5481-5492`'s pivot `IN (...)` handling calls `fld.sql()` on an unrestricted subquery; the kernel allow-lists only `expressions`/`distinct`/`from_`/`where`/`order`. Confirmed against the real corpus: 41 atoms contain `PIVOT(`, **0 exercise a pivot subquery with `LIMIT`/`GROUP BY`/`HAVING`/an aliased column** — real Snowflake syntax upstream parses successfully (verified directly against the pinned commit), but the JS kernel throws `NotPorted` for all four. *Mitigation already in place:* fails loud, not silently wrong — no atom regresses, and no incorrect SQL is emitted. *Residual, tracked here rather than left implicit:* this is real user-reachable incompleteness outside the corpus, the same hazard class as R4/R11, found by an independent review adversarially constructing inputs adjacent to what the PR's own probes covered (§8.5 in miniature — this is exactly what that gate is for). Not a blocker for the P3 blocking-step PR, whose exit criteria are explicitly corpus-scoped ("100% of what parses," not "100% of the grammar"). Follow-up: broaden `generator_kernel.js`'s `Select`-clause coverage to match upstream's actual unrestricted call, owned by whoever picks up the stub-queue phase next, not silently assumed complete by the "real recursive generator" framing in PR #6's description.

**R13 — `tools/closure_parser.mjs`'s demand tracer only sees `_parse_*`-prefixed methods, so it is structurally blind to non-`_parse_*` helper stubs that gate coverage just as hard.** Found 2026-08-28 mid-stub-queue: after a PR ported 25 `_parse_*` methods and `closure_parser.mjs` reported closure jumping 49.68% -> 93.99%, the honest probe (`spike/p3/fuzz_ast_coverage.mjs`, which actually parses each row) showed EXACT/STUB completely unchanged — because `trace_parse_demand.py` never wrapped `_can_parse_limit_or_offset` or `_can_parse_named_window`, two tiny non-`_parse_*` helpers that gate nearly every `SELECT`/alias parse. Porting those two (plus `_match_l_paren`/`_match_r_paren`/`_advance_any`/`_identifier_expression`, also invisible to the tracer, 37/7/2 existing call sites respectively) collapsed STUB from 15,440 to 516 and surfaced a second, larger dormant bug: `Parser.FUNCTIONS` had only 2 entries instead of 629+, because the seed only ever wrote upstream's hand-override half of the dict (`parser.py:376`) and never the bulk `**{name: func.from_arg_list for name, func in exp.FUNCTION_BY_NAME.items()}` population — every ordinary function call (`RAND()`, `LOG(...)`) was silently falling through to `exp.Anonymous`. Neither gap was visible to `closure_parser.mjs`, `fuzz_from_arg_list.mjs` (tests `from_arg_list` builders directly, not through `_parse_function`'s lookup), or any merged PR's own verification, because every prior stub-queue PR's honest-probe run legitimately showed "STUB is large, MISMATCH/ERROR are 0" — the accepted state for a blocking step — right up until enough `_parse_*` progress accumulated to actually reach these methods at runtime for the first time. *Mitigation in progress:* the two `_can_parse_*` stubs, the four small primitives, and the `FUNCTIONS` population are fixed and verified (native suite 61/61, EXACT 38->306) on branch `p3-primitives-and-functions-fix`, dispatched onward rather than merged, because fixing them unmasked a further ~7,995 MISMATCH / ~6,635 ERROR that were always latent and are now finally reachable. *Residual, same hazard class as R4/R11/R12:* `closure_parser.mjs` should be extended to trace every `Parser` method actually called during a demand trace, not just `_parse_*`-prefixed ones, or its "rows closed" number should carry an explicit caveat next to it. Until then, treat `closure_parser.mjs`'s percentage as an upper bound on real coverage, never the real number — only `fuzz_ast_coverage.mjs`'s EXACT/MISMATCH/STUB/ERROR counts are ground truth.

**R14 — 24 of `Parser`'s class-level dispatch tables were seeded as fully-commented-out skeletons and never wired, even where the `_parse_*` methods they should dispatch to are already ported.** Found 2026-08-28 while root-causing R13's residual MISMATCH/ERROR set: `Parser.FUNCTION_PARSERS` (which routes `CAST(x AS INT)`, `EXTRACT(...)`, `TRIM(...)`, `SUBSTRING(...)`, `STRING_AGG(...)` etc. to their dedicated custom-syntax parsers instead of generic comma-separated-args parsing) is a `Map` with **zero entries** — every line in its seed is `// py:NNNN ["CAST", /* TODO lambda */]`, commented out. A systematic sweep (`Object.getOwnPropertyNames(Parser)` filtered to empty `Map`/`Set` statics) found **23 more in the same state**: `STATEMENT_PARSERS` (routes `CREATE`/`ALTER`/`DROP`/`GRANT`/... at the top level), `RANGE_PARSERS` (routes `BETWEEN`/`IN`/`IS`/`LIKE`/`GLOB`/...), `EXPRESSION_PARSERS`, `NO_PAREN_FUNCTION_PARSERS`, `PLACEHOLDER_PARSERS`, `ALTER_PARSERS`, `ALTER_ALTER_PARSERS`, `QUERY_MODIFIER_PARSERS`, `SET_PARSERS`, `SHOW_PARSERS`, `TYPE_LITERAL_PARSERS`, `TYPE_CONVERTERS`, `PROCEDURE_OPTIONS`, `ODBC_DATETIME_LITERALS`, `ANALYZE_EXPRESSION_PARSERS`, `LAMBDAS`, `JSON_OPERATORS`, `EXPONENT`, `SHOW_TRIE`, `SET_TRIE`, plus the two empty `Set`s `JOIN_HINTS`, `QUERY_MODIFIER_TOKENS`, `OPERATION_MODIFIERS`. Every one of these gates real, common SQL: the concrete symptom that surfaced it was `CAST(x AS INT)` throwing `ParseError: Required keyword: 'to' missing for Cast` — `_parse_cast` is already ported and correct, but nothing ever routes a `CAST(...)` call to it, so it falls through to the generic function-args path instead. *Same hazard class and same root cause as R13:* `closure_parser.mjs`'s demand tracer has no visibility into class-level table population at all (it only tracks whether a *method* is a stub), so a fully-implemented `_parse_cast` sitting behind an unwired dispatch table reads as 100% progress. *Scope, not yet measured precisely:* each table's real upstream population (`parser.py:1157-1594` region alone spans ~150 entries across the first 4 tables) is mostly a mechanical "look up upstream, confirm the callee `_parse_*` method is already ported (a large fraction now are, per the stub queue's progress), write the real `Map`/`Set` entry" task — some entries need a genuinely new lambda (`binary_range_parser(...)` factories, a few `_parse_*` methods still stubbed) rather than a bare uncomment. *Mitigation landed 2026-09-03 (branch `p3-primitives-and-functions-fix`, PR #21):* all 24 tables resolved — 12 genuinely wired to their upstream lambdas, 8 confirmed correct-as-empty (upstream's own base-class default, not a seeding gap — the naive `size === 0` sweep above overcounted), the remaining 3 self-derived from the others. Also fixed along the way: `Parser.FUNCTIONS`'s missing bulk `exp.FUNCTION_BY_NAME` population, `astDump`'s Python/JS empty-array truthiness bug, a `DataType` lookup using `.name` on integer `TokenType`s, a second `null`-vs-`[]` crash, `except`/`from` reserved-word arg renaming missed at 405 call sites, `::` casts not parsing at all (`COLUMN_OPERATORS`'s `DCOLON` was itself an unwired `TODO`), 11 more base-parser `FUNCTIONS` builders, and a mis-ported `_parse_decode`. Net effect measured by `fuzz_ast_coverage.mjs`: **EXACT 38 -> 7,046** (full corpus), **ERROR 6,635 -> 1,942**. **Merged to `main` with `spike/run_all.sh` NOT fully green — the first exception to this project's standing "must be green" rule, made deliberately: see R15.**

**R15 — full Snowflake AST-oracle exactness is not reachable from base `parser.js` alone; it needs `parsers/snowflake.py`'s `SnowflakeParser` subclass, a component this project has never scoped or started.** Found while chasing R14's fix toward `spike/run_all.sh` green: after every reachable base-parser bug was fixed (R13, R14, and the round above), Snowflake closure plateaued at 975/2,516 exact (38.8%). Triaging the remaining 344 Snowflake `ERROR` rows by construct found the large majority are `SHOW ...` (64), stage references `@...` (27), policy/tag DDL (15), `COPY INTO`/`PUT`/`GET` (12), `FLATTEN` (8), `IDENTIFIER(...)` (7), `RELY`/`NORELY` (4), plus dialect-specific function-name mappings (`TRY_TO_DATE`, `ZEROIFNULL`, `REGEXP_SUBSTR`, `TO_TIMESTAMP`, ...) that dominate the 1,111 Snowflake `MISMATCH` rows — all confirmed to live in `sqlglot/parsers/snowflake.py` (verified: `class SnowflakeParser(parser.Parser)`, overriding `STATEMENT_PARSERS`/`FUNCTIONS`/`RANGE_PARSERS`/`NO_PAREN_FUNCTIONS` and adding methods like `_parse_show_snowflake`), **not** `sqlglot/dialects/snowflake.py` (a separate, much smaller 192-LOC file holding only the `Snowflake(Dialect)` settings class — `TIME_MAPPING`, `DATE_PART_MAPPING`, `NULL_ORDERING`, etc. — no parsing logic at all) and not base `sqlglot/parser.py`. An earlier version of this paragraph named the wrong file (`dialects/snowflake.py`/`src/dialects/snowflake.js`) — corrected 2026-09-03 after a direct question surfaced the mismatch; upstream's own `parsers/snowflake.py` is measured at **1,440 LOC** (already the correct figure in §10's effort table, just not cross-referenced here originally), the real basis for this component's size, not the earlier "~2,000+ LOC" estimate derived by subtraction. This is a real, separate component (the literal second half of P3's own title, "Parser base + **Snowflake parser**") that no PR or stub-queue dispatch has ever targeted — `src/parsers/snowflake.js` does not exist yet, distinct from `src/dialects/dialect.js` (R13's `VALID_INTERVAL_UNITS` note, base Dialect settings) and the eventual `src/dialects/snowflake.js` (Snowflake's own Dialect settings, ~192 LOC, also unstarted but not what gates AST-oracle parsing exactness the way the parser subclass does). *Not the same as the already-documented "100% of `test_snowflake.py` only at P9" fact (§3.2, R3)* — that one is about atoms whose *write* dialect isn't yet implemented; this one is about Snowflake's own *read*-side grammar. *Decision (Ben, 2026-09-03):* merge the real, verified base-parser progress now rather than block on scoping and building the new dialect-parser component in the same pass — "let's merge and move on." `parsers/snowflake.js`'s parser portion is tracked here as explicitly deferred, not silently dropped; whoever picks it up next should start from this paragraph's construct list rather than re-triaging from scratch. Until it lands, `spike/run_all.sh`'s "P3: AST oracle coverage (honest)" probe is expected to stay red — that is not a regression signal, it is this exact gap, and should not be "fixed" by loosening the probe's own MISMATCH=ERROR=0 standard. *Landed 2026-09-03 (branch `p3-snowflake-parser`): see R16.*

**R16 — `parsers/snowflake.py` is ported; the R15 construct list is closed, and porting it exposed four base-parser defects that read as ported and were silently wrong.** Landed 2026-09-03 on branch `p3-snowflake-parser`, closing R15. `src/parsers/snowflake.js` now carries all 38 methods, all class-table overrides (including the two SECOND assignments — `FUNCTIONS` minus `PREDICT`, `FUNCTION_PARSERS` minus `TRIM` — that look like duplicate definitions in Python and are easy to drop), and the 20 module-level builders; `src/dialects/dialect.js` is new and holds ONLY the module-level builders `parsers/snowflake.py` imports plus `DATE_PART_MAPPING` (the `Dialect` class itself stays P5). Every construct R15 triaged is now green on the AST oracle: `SHOW` 72/72, stage `@...` 27/33, policy/tag DDL 29/29, `FLATTEN` 24/24, `IDENTIFIER(...)` 7/9, `TRY_TO_*` 83/83, `REGEXP_SUBSTR` 37/37, `TO_TIMESTAMP`/`TO_DATE` 80/80, `ZEROIFNULL`/`DIV0` 36/36, `SEMANTIC_VIEW` 18/18, `UNDROP` 15/15, `DATE_PART`/`DATEADD` 72/72 — **zero MISMATCH in any of those classes**. Snowflake overall: **EXACT 975 -> 2,192 of 2,516** (46.7% -> 96.7% of REACHED rows), MISMATCH 1,111 -> 75, ERROR 344 -> 109, STUB 86 -> 140. Full corpus EXACT 7,046 -> 8,353, MISMATCH 5,731 -> 4,719, ERROR 1,942 -> 1,668.

*The hazard worth recording is not the port itself but what it uncovered.* R13 and R14 are about code that ANNOUNCES its incompleteness — a `NotPorted` stub, an empty dispatch table. This round found the next layer down: four sites that look finished, carry no TODO, and are wrong. `_parse_null` (py:8880) was `return this.expression(exp.null())` — it returned a `Null` **without consuming a token**, so `x IS NULL` left the cursor on `NULL`, `_parse_csv` stopped at the following comma, and `IFF(c IS NULL, 0, c)` reached `If.from_arg_list` with ONE argument; `IS UNKNOWN` never matched at all. `_parse_extract` (py:8117) read a `_parse_function_parameter()` where upstream reads `_parse_function() or _parse_var_or_string(upper=True)`, yielding an `Identifier` where the oracle has a `Var` (70 Snowflake rows), and matched a closing paren that `_parse_function_call` already owns. `_parse_format_name` (py:10140) built an `exp.FormatNameProperty` — a class that **does not exist in upstream `parser.py` at all** (`grep` returns nothing); upstream returns a plain `Property(this=Var("FORMAT_NAME"), value=...)`. `Expr.error_messages` (core.py:1322) dropped the `isinstance(self, Func)` guard on its arity check, so any builder wrapping its function node in a non-`Func` — `build_like` returning `Escape(Like(...))` — was measured against the wrapper's two `arg_types` and rejected. Plus one missing member: `Identifier.quoted` is a `@property` returning `bool(args.get("quoted"))`, absent here, so upstream's `isinstance(k, exp.Identifier) and k.quoted` (Python `False`) became JS `undefined` and dumped `quoted: null` against an oracle `false` — 155 rows. *None of these are findable by any counter this project has:* `closure_parser.mjs` sees a non-stub method, `fuzz_from_arg_list.mjs` tests builders directly, and `fuzz_ast_coverage.mjs` only says "MISMATCH" without naming the method. **A method being non-stub is not evidence it was ported; it is evidence someone wrote a body.** The generalisable check is a per-method diff against the pinned ref, not a per-method existence test — until such a sweep exists, expect more of these to surface one dialect subclass at a time, because a subclass is what first sets the flags (`COLON_IS_VARIANT_EXTRACT`, `TYPED_LAMBDA_ARGS`) that make dead base-parser paths live.

*Residual, all measured and all base-parser scope, not Snowflake's:* the 75 remaining Snowflake MISMATCH are entirely base `FUNCTIONS`/method gaps whose names appear nowhere in `parsers/snowflake.py` (`Pad` 16, `Floor`/`Ceil` missing `to` 13, `GroupConcat.on_overflow` 7, `ArrayAgg.nulls_excluded` 10, `Array*.null_propagation` 8, `Trim` 6, `AlterColumn` kwarg ORDER 8, `Count.big_int` 2, `JSONExtractScalar` 2, `VECTOR` DataType 2). The 109 remaining ERROR are `MATCH_RECOGNIZE` (75 — `_parse_match_recognize` is a stub AND its `QUERY_MODIFIER_PARSERS` entry at py:1598 is unwired, R14's class), `QUALIFY` (12), `COPY INTO` (12, `_parse_copy_parameters` stub), inline `FOREIGN KEY REFERENCES` (3), `{fn CONVERT}` (2) — each verified to fail identically under the BASE `Parser`, i.e. none is introduced by the subclass. The 140 STUB are `_parse_cte` 35, `annotate_types` 32 (the type annotator is P6+; `build_trunc`/`build_timetostr_or_tochar` announce it rather than guessing a type), `_parse_using_identifiers` 16, `_kv_to_prop_eq` 16, `_parse_pivot` 14, `DataType.from_str` on a parameterised string 10 (needs `parse_one(into=DataType)`; nothing calls `registerParser` yet), `Dialect.to_json_path` 9 (needs the unported jsonpath module), `_parse_connect` 4, `_parse_json_object` 3, `_replace_lambda` 1.

*On CONTRACTS.md §8:* the harness picks `SnowflakeParser` from the corpus row's OWN dialect metadata — the `corpus/ast/<dialect>.jsonl` filename — via `parserClassFor(dialect)` in `spike/p3/dialect_tokenizer.mjs`, as a plain ESM import with no registry in between. `Parser._resolveDialect` still throws on a dialect NAME and was not touched. This is the same argument `tokenizerFor` already makes one layer down, and the extension point for the next dialect subclass is one entry in that map.

**R17 — R16's "base-parser residual" is largely R14's job left half-done at ENTRY granularity, and the pipe-syntax third of it is blocked outside the parser entirely, on a `registerParser` hook that is exported and never called.** Landed 2026-09-08 on branch `p3-base-parser-residual`, chasing R16's residual note. Full corpus **EXACT 8,353 -> 9,156, MISMATCH 4,719 -> 4,170, ERROR 1,668 -> 1,440, STUB 738 -> 712**; snowflake **96.7% -> 98.7%** of REACHED rows (2,192 -> 2,355); default-dialect ERROR **66 -> 6**; 61/61 unit tests throughout, and no row anywhere moved EXACT -> anything worse.

*What the residual actually was.* R14 wired 24 dispatch TABLES but left individual ENTRIES inside them as `/* TODO lambda */` placeholders, and the stub queue's convention ("a stub's dispatch entry stays commented until the stub is ported") kept 11 of `QUERY_MODIFIER_PARSERS`' 21 entries dark. Neither is visible to any existing counter: `closure_parser.mjs` tracks whether a METHOD is a stub, and R14's own `size === 0` sweep only catches a table that is entirely empty, never one that is 90% populated. Closed in this pass: `QUERY_MODIFIER_PARSERS` is now **21/21** (`_parse_prewhere`, `_parse_having`, `_parse_qualify`, `_parse_cluster`, `_parse_sort`, `_parse_locks`, `_parse_window_clause`, `_parse_named_window`, `_parse_connect`, `_parse_connect_with_prior`, `_parse_match_recognize` + `_parse_match_recognize_measure` all ported), and 33 of the 43 remaining `TODO lambda` entries are wired. Also ported: `_replace_lambda` (which gated `LAMBDAS[ARROW]`, so NO function-call lambda `f(arr, x -> pred)` parsed at all), `_parse_merge` + `_parse_when_matched` + `_parse_star` + `_parse_using_identifiers`, `_parse_cte` + `_values_to_select` (the single largest stub, 312 rows), `_kv_to_prop_eq` + `_to_prop_eq`, and `_parse_name_as_expression`. `MATCH_RECOGNIZE` went **0 -> 78/78 EXACT**; `MERGE` 0 -> 33/35.

*Two more R16-class defects — non-stub, finished-looking, silently wrong.* (1) `_parse_string_as_identifier` (py:8835) and `_parse_field` (py:8790) both called `.update_positions(...)`, the PYTHON spelling, on an `Expr` whose JS method is `updatePositions`; a `TypeError` at runtime, not at load. Swept every camelCase method on `Expr` for the same slip — these two were the whole class. (2) `PIPE_SYNTAX_TRANSFORM_PARSERS["ORDER BY"]` was written `(self) => self._parse_order(...)` where upstream (py:1293) is `lambda self, query: query.order_by(...)`: it dropped the `query` parameter and returned a bare `Order`. A populated-table sweep sees both as green. **The counter this project still lacks is one that checks a wired entry's SHAPE against upstream's, not merely its presence.**

*The pipe-syntax blocker, which is not in the parser.* `expressions/core.js:585 maybeParse` dispatches to a `PARSE` hook installed by `registerParser` (core.js:513), falling back to what its own comment calls a "P2-safe leaf fallback ... P3 replaces the whole branch through registerParser". **`registerParser` is exported and called from nowhere in `src/`, `test/` or `spike/`**, so `PARSE` is permanently null and the stopgap is permanently live: `exp.select("a")` is correct, but `exp.select("*")` yields `Column(Identifier('*', quoted=True))` where CPython yields `Star()`, and `exp.select("a + 1")` yields a quoted identifier where CPython parses an `Add`. Every pipe method funnels through `_build_pipe_cte`/`_parse_pipe_syntax_query`, both of which call `exp.select("*")`. All 9 remaining pipe methods (py:10170-10333) were ported to TEST this rather than assume it: 103 STUB rows became reachable and 97 landed in MISMATCH differing *only* by that one node. That trades loud stubs for silent near-misses at zero EXACT gain, so **the pipe port is deliberately not landed** — only the `ORDER BY`/`WHERE`/`TABLESAMPLE` entry fixes above are. Wiring `registerParser` needs a `parse_one` equivalent, which needs a resolved base `Dialect` — P5 scope, and exactly why the harness had to build `standInDialect`; making the stopgap announce itself instead would change shared P2 machinery under all 61 tests. Both are real options; neither belongs in a base-parser pass. *This is the same hazard family as R13/R14 one layer out: not a stub and not an empty table, but a registration hook nothing ever calls.*

*Harness change, same argument as its two precedents.* `tools/parser/extract_dialect_attrs.py` gains 6 `PARSER_ATTRS` (`ARRAY_AGG_INCLUDES_NULLS`, `ARRAY_FUNCS_PROPAGATES_NULLS`, `CONCAT_WS_COALESCE`, `HEX_LOWERCASE`, `JSON_EXTRACT_SCALAR_SCALAR_ONLY`, `LOG_BASE_FIRST`) and a new `PARSER_CLASS_ATTRS` list for `LOG_DEFAULTS_TO_LN`, which `build_logarithm` reads off `dialect.parser_class` rather than the Dialect and which splits 14 True / 21 False across dialects. **Two of the six are TRUE at the base Dialect**, so wiring their `FUNCTIONS` entries before extending the harvest would have silently taken the opposite branch from CPython on every row — the precise failure mode that file's own header was written about. Harvest first, then wire.

*Residual, measured.* The 1,440 remaining ERROR are now ~96% dialect-specific: tsql 322, mysql 223, clickhouse 144, bigquery 90, postgres 89, duckdb 84, oracle 62, singlestore 60 — the `parsers/<dialect>.py` subclasses R15/R16 established as their own components. Only **6 are default-dialect** (4 pipe-syntax, 2 chained `AT TIME ZONE`). Three specific rows regressed into ERROR/MISMATCH from base entries that are FAITHFUL to upstream and shadowed there by an unported dialect override: `parsers/duckdb.py:120` subtracts `GLOB` from the inherited `FUNCTIONS`, `parsers/singlestore.py:102` replaces `JSON_KEYS`, and `parsers/trino.py:73` + `parsers/clickhouse.py:659` override `_parse_cte` (which is why the base py:4219 is typed `-> exp.CTE | exp.FunctionSpecification | None` while its body only ever returns a CTE). Left wired rather than omitted, so the gap sits where upstream puts it. Of the remaining MISMATCH, 184 are `Dialect.to_json_path` (P4 jsonpath) and a large block is `annotate_types` (P6+) — both already-tracked components, now reached loudly rather than approximated silently.

*Probe status.* `spike/run_all.sh` has two red probes. "P3: AST oracle coverage (honest)" is red for the R15 reason and stays red until the dialect parsers land. "P3: check_command_warning strings" reports `0/0` against an expected `18/18` **for an environmental reason, not a code one**: `spike/p3/gen_command_warning_ref.py:83` calls `unittest.loader.discover` on `/tmp/sqlglot-ref/tests/dialects`, and this sandbox's copy of the pinned tree contains only `test_dialect.py` with no `__init__.py`, so the loader raises `ImportError: Start directory is not importable`. The reference corpus comes out empty and the gate compares against nothing. Nothing in this branch touches that probe or its inputs; it needs a complete `/tmp/sqlglot-ref` checkout, and should be re-checked on a full tree before being read as a regression.

**R18 — the Databricks chain (`Hive <- Spark2 <- Spark <- Databricks`) is ported, and the next defect layer down from R16/R17 is ARGUMENT SHAPE at the CALL site: a keyword-only parameter passed positionally, which every existing counter reads as green.** Landed 2026-09-08 on branch `p3-databricks-chain` (PR #26), taking the second dialect in §10's priority order. All four upstream files — `parsers/hive.py` (282 LOC, 7 methods), `parsers/spark2.py` (123, 2), `parsers/spark.py` (144, 3), `parsers/databricks.py` (76, 3) — are ported into `src/parsers/{hive,spark2,spark,databricks}.js`, each `extends`ing the previous exactly as upstream does; all 15 methods, 6 module-level builders and 21 class-table overrides, nothing stubbed or skipped. `src/dialects/dialect.js` grows by the four shared builders these files import (`build_regexp_extract`, `pivot_column_names`, `build_date_delta`; `build_trim` was already ported in `parser.js` and merely needed exporting), keeping its documented narrow scope. Measured: **hive EXACT 273 -> 489, MISMATCH 198 -> 2, ERROR 23 -> 0; spark2 20 -> 41 EXACT with MISMATCH/STUB/ERROR all 0 (41/41); spark 323 -> 618, MISMATCH 303 -> 24, ERROR 28 -> 3; databricks 168 -> 309, MISMATCH 123 -> 2, ERROR 32 -> 8. Full corpus EXACT 9,156 -> 9,888, MISMATCH 4,170 -> 3,498, ERROR 1,440 -> 1,364.** A per-dialect sweep over all 34 corpora against a stashed baseline confirms no dialect lost an EXACT row.

*The new hazard class.* R13 found stubs invisible to the tracer, R14 empty dispatch tables, R17 wired entries with the wrong shape. This round found the layer below: **a CALL whose arguments are the wrong shape.** `_match_text_seq(*texts, advance=True)` takes `advance` keyword-only after a var-args list, so the port accepts it as a trailing options object — and `src/parser.js:2244`'s own docstring warns that "passing it positionally would make it the next TEXT to match". Five call sites did exactly that (`"LOCATION"` py:9081, `"TO"` py:9189, `"FILESTREAM_ON"` py:9211, `"MODE"` py:9456, `"COLUMNS","("` py:10006): `false` is not an object, so it stayed in `texts`, was compared against `this._curr.text`, and every one of those calls returned `false` unconditionally. Three were dead branches, one inverted an `ALTER ... RENAME` condition, and one left `AddPartition.location=False` while leaking the `LOCATION` into `Alter.options`. **A grep for `_match_text_seq\([^)]*,\s*(true|false)\)` finds all five in one line and is the cheapest counter this project has ever added — the generalisable form is a lint that flags a boolean literal in any position a var-args signature does not accept.** The same sweep discipline applies to every other `*args, kw=...` method the port exposes as a trailing options object; only `_match_text_seq` was checked here.

*Three more R16-class defects — non-stub, no TODO, wrong.* (1) `_parse_row` (anchored `py:3759`) had an entirely UNRELATED body, building `exp.Row({expressions: _parse_wrapped_csv(_parse_assignment)})` where upstream is three lines: `if not self._match(TokenType.FORMAT): return None` then `return self._parse_row_format()`. Upstream `parser.py` never constructs an `exp.Row` (`grep 'exp.Row(' ` is empty) and no such class exists in either tree, so it would have raised `TypeError` had it ever got past `_parse_wrapped_csv` — it never did, because `_parse_wrapped_csv` raised "Expecting (" first. It dates to the original seed commit `7fc1338` and gated `PROPERTY_PARSERS["ROW"]` (py:1385), i.e. every `ROW FORMAT SERDE`/`DELIMITED` in Hive and Spark. **A method whose body has nothing to do with its own `// py:` anchor is not detectable by any counter here; only reading the two side by side finds it.** (2) `DataType.fromStr` omitted `expressions: null`; upstream's `from_str` returns the PARSER's node via `parse_one(dtype, into=cls)`, whose args are `['this', 'expressions', 'nested']`, and `astDump` serialises in INSERTION order, so this was a POSITIONAL diff, not merely an absent key. Its in-code justification — "invisible to the AST oracle, which only ever reaches DataType nodes through astLoad" — was true when written and falsified by this port, because `build_as_cast` (spark2.py:17) calls it for eight type names. **A correctness argument that rests on "nothing reaches this yet" expires silently the moment something does; it needs re-checking whenever a new caller lands, and nothing enforces that.** `test/expressions/review_regressions.test.mjs:147` asserted the OLD key order, i.e. the test was pinning the port's own shortcut rather than upstream — corrected against the pinned tree, with every other assertion in it unchanged and still passing. (3) `_parse_grant_principal` (py:10045): `kind = self._match_texts(("ROLE","GROUP")) and self._prev.text.upper()` — a Python `and` yields the FALSE OPERAND, so an unmatched kind is `False`, not `None`. The port wrote `: null`, a silent one-token diff on every GRANT/REVOKE row without an explicit ROLE/GROUP; fixing it moved rows in six dialects (postgres +31, oracle +12, redshift +6, clickhouse +4, tsql +4, databricks +8). **The `X and Y` -> false-operand idiom is a mechanical grep against upstream and has now bitten at least twice; it deserves the same one-line sweep as `_match_text_seq`.**

*Harness gap, same class as R17's attr additions.* `tools/parser/extract_dialect_attrs.py` never harvested `REGEXP_EXTRACT_DEFAULT_GROUP`, which `build_regexp_extract` (dialect.py:2488) reads for the `group` default — Hive/Spark set it to 1 where the base `Dialect` uses 0. The miss did NOT fail loudly the way that explicit list is supposed to make it: `dialect.REGEXP_EXTRACT_DEFAULT_GROUP` read back `undefined` and built `Literal(this=undefined)`, which the oracle reported as an ordinary MISMATCH. `assert_covers` only checks that a listed name still exists upstream, never that a name the port READS is listed. The mechanical check — grep `dialect\.[A-Z_]+` across `src/` and diff against `PARSER_ATTRS` — is exactly what that file's own header comment already describes as the derivation, but nothing runs it.

*Blockers hit, diagnosed, and deliberately not forced.* (a) **20 of spark's 24 residual MISMATCH are `/*+ ... */` hints, blocked on R17's `registerParser` hook, not on any parser gap.** `_parse_hint` (py:4435) is faithful — it calls `exp.maybe_parse(comment, into=exp.Hint)`, and with `PARSE` permanently null the P2-safe leaf fallback yields `Hint(this=Identifier(' MERGE(table) ', quoted=True))` where CPython yields `Hint(expressions=[JoinHint(...)])`. `_parse_join_hint` was ported (it is the callee of 8 `Spark2Parser.FUNCTION_PARSERS` entries and wiring an entry to a throwing stub is R14/R17's pattern) and is correct, but unreachable through hint comments until `parse_one` lands at P5. **This is the second independent construct family — after pipe syntax — that traces to that one uncalled hook; its cost is now demonstrably not confined to pipes.** (b) `DataType.from_str("string"/"TIMESTAMP_LTZ"/"TIMESTAMP_NTZ")` (6 spark STUB) are keyword ALIASES resolved by the tokenizer/parser type tables upstream, so they need the same `parse_one`; an invented alias map would trade a loud stub for a silent near-miss and was not written. (c) `FROM_UTC_TIMESTAMP(x::TIMESTAMP, tz)` (1 databricks MISMATCH) is not a parser gap at all: `exp.cast` (builders.js:188) collapses a redundant cast by comparing both types through `dialect.generator_class.TYPE_MAPPING`, which `standInDialect` does not supply (the generator is P7), so it falls back to `BASE_TYPE_MAPPING`; Databricks maps TIMESTAMP -> TIMESTAMPTZ there. Harvesting it is the same mechanism already used for `parser_class`, but it changes a shared expression builder for all 34 dialects to fix one row. (d) `_parse_overlay` (py:10123) is an announced stub with a correspondingly commented-out `FUNCTION_PARSERS["OVERLAY"]` entry, costing databricks 1 MISMATCH + 3 ERROR — stub-queue scope, left announced.

*Probe status, verified rather than assumed.* `spike/run_all.sh` still has exactly the two red probes R17 recorded, and **both were confirmed pre-existing by running them against a clean `origin/main` worktree rather than by inheriting the claim**: "P3: check_command_warning strings" fails identically there with `ImportError: Start directory is not importable: '/tmp/sqlglot-ref/tests/dialects'` (this sandbox's pinned tree has only `test_dialect.py`, no `__init__.py`), and nothing in this branch touches that probe or its inputs. "P3: AST oracle coverage (honest)" stays red for the R15 reason — the residual is now ~96% the remaining `parsers/<dialect>.py` subclasses, with postgres and duckdb next in §10's order. `run_all.sh` regenerates every corpus from CPython and left the working tree clean, so the committed corpora are reproducible.

**R19 — `parsers/postgres.py` is ported, and the defect it surfaced is a DISPATCH TABLE THE PORT NEVER READS: `JSON_OPERATORS` was declared, wired by no one, and invisible precisely because every dialect that populates it was still unported.** Landed 2026-09-08 on branch `p3-postgres-parser`, taking the third dialect in §10's priority order. `sqlglot/parsers/postgres.py` (363 LOC) is ported whole into `src/parsers/postgres.js` — all 4 module-level builders, all 13 class-table overrides, all 9 methods, nothing stubbed or skipped — extending the base `Parser` directly the way `snowflake.js` does rather than chaining. `src/dialects/dialect.js` grows by the two shared builders it imports (`build_json_extract_path`, `build_timestamp_trunc`), keeping its documented narrow scope; `binary_range_parser` was already ported and merely needed exporting (R18's `build_trim` shape again), and the three module-level `build_jsonb_*` functions (`parser.py:318-331`) were genuinely absent — upstream defines them as named functions AND separately inlines the same bodies as lambdas in `COLUMN_OPERATORS`, and the port had only the lambdas. `dialects/postgres.py`'s 143-LOC `Postgres(Dialect)` settings class stays P5, the same split every prior dialect port used. Measured: **postgres EXACT 640 -> 883, MISMATCH 184 -> 11, ERROR 88 -> 7, STUB 34 -> 45** (883 of 901 REACHED rows, 98.0%); **full corpus EXACT 9,888 -> 10,169, MISMATCH 3,498 -> 3,335, ERROR 1,364 -> 1,235**.

*The new hazard class: a table with no reader.* R13 found stubs invisible to the tracer, R14 empty dispatch tables, R17 wired entries with the wrong SHAPE, R18 call sites with the wrong ARGUMENT shape. This round found the layer beside them: **`Parser.JSON_OPERATORS` was declared at `parser.js:1110` with a correct `py:1113` anchor and a correct "empty on the base parser upstream" comment — and was read by nothing.** Upstream reads it in exactly one place, `_parse_bitwise` (`parser.py:6330-6331`), and the port's `_parse_bitwise` simply lacked that `elif` branch. R14's sweep could not catch it: R14 asked "is this table populated?", and the honest answer for `JSON_OPERATORS` was "correctly empty, because upstream's base is empty too". No probe asks the *other* question — **is this table ever read?** — and the answer stayed invisible for as long as every dialect that populates it (Postgres, DuckDB, MySQL) was unported. Postgres is the first, and without the fix its five `JSON_OPERATORS` entries would have been a silent no-op: populated, correct, never consulted. *Residual, cheap and mechanical:* for every `static X = new Map/Set` on `Parser`, grep `src/` for a read of `X` that is not its own declaration; a table with zero readers is either dead upstream too or a missing call site. Nothing runs that check today.

*Three more R16-class defects — non-stub, no TODO, correct `py:` anchor, wrong body.* All three were surfaced by Postgres rows reaching further, and all three were verified line-by-line against the pinned reference and against the oracle's expected AST before being touched. (1) **`_parse_unique` never called `_parse_unique_key`** — which made `PostgresParser._parse_unique_key`'s `return None` override (postgres.py:329) DEAD CODE. The port had `_parse_unique_key` building a `UniqueColumnConstraint` with an invented `is_key` arg, and `_parse_unique` calling `_parse_wrapped_id_vars()` directly, which REQUIRES parens — so every bare `col INT UNIQUE` raised "Expecting (". It also invented a tri-state `nulls` via a non-existent "NULLS DISTINCT" branch and dropped `index_type`/`on_conflict`/`options` entirely, where the oracle wants all five keys present and ordered. (2) **`_parse_references` had four defects in five lines**: no `match` guard and no `match = true` default (so `_parse_foreign_key`'s bare call never consumed REFERENCES), `_parse_table_parts()` where upstream has `_parse_table(schema=True)`, a paren-REQUIRING `_parse_wrapped_csv` that upstream does not have at all, and a `match` kwarg that is not in `exp.Reference.arg_types`. (3) Both are the same **body-vs-`py`-anchor mismatch** the R18 note records as greppable, and both sit in the constraint-parsing neighbourhood — worth sweeping the rest of `_parse_*_key` / `_parse_*_constraint` as a unit rather than one oracle diff at a time.

*One R14-class unwired entry, five years of context later.* `STRING_PARSERS[TokenType.UNICODE_STRING]` (`py:1211`) was the last of that table's five entries, left as an anchored `/* TODO lambda */` with an explicit rationale: its lambda needs `_match_text_seq` and `_parse_string`. Both have been ported for a long time, so the entry was simply never revisited — an unwired entry behind a table that otherwise reads as fully populated. It was the single largest Postgres ERROR cluster (17 rows of `U&'...'`): the tokenizer already emits UNICODE_STRING from harvested dialect settings, so no P5 work was needed; nothing routed the token. **A TODO whose stated blocker has since been cleared does not un-TODO itself, and no probe re-checks them.**

*Python `and` keeps yielding the FALSE OPERAND, in three new places.* R18 recorded this as a greppable class; it recurred three times in one round and every instance was confirmed against the oracle rather than assumed — `_parse_jsonb_exists`'s `path` (postgres.py:336), `_parse_unique`'s `index_type` (which the oracle dumps as `index_type: false`, not null), and `UNICODE_STRING`'s `escape` (`UnicodeString(this=\FE01, escape=False)`). The grep is `\b(and)\b` in upstream against `&&`/ternary in the port, at any site whose left operand is a `_match*` call.

*Blocker hit, diagnosed, and deliberately not forced.* `_build_generate_series` calls `exp.to_interval`, which routes through `maybe_parse` and therefore needs R17's `registerParser` hook — still exported and never called, still genuinely P5. Left bare it raises a plain `TypeError`, and `_parse_function_call` (`parser.js:4870`) catches `TypeError` as its `builder(args)` / `builder(args, dialect)` ARITY signal — so the missing component would have been silently retried into a confusing double-call instead of a counted stub. It is re-raised as `NotPorted`, which is why postgres STUB rises 34 -> 45 while ERROR falls: 10 of those rows are this, honestly counted. Ordinary two-argument `GENERATE_SERIES(a, b)` never touches that path. **This is the trade the round refused to fake: a loud stub over a silent near-miss.**

*No dialect lost a row, checked rather than assumed.* Each of the four base-parser fixes touches shared code, so every one was measured with a 34-dialect sweep against a worktree pinned at the pre-change commit. Across the whole branch, seven dialects moved and **every one gained EXACT rows**: postgres +243, presto +18, mysql +9, databricks +4, snowflake +4, sqlite +2, tsql +1. The sqlite (+4) and tsql (+6) MISMATCH increases are rows that previously CRASHED and now parse to a near-miss — strictly forward, the same unmasking R14 documented, and no row anywhere moved EXACT -> anything worse.

*Postgres residual, triaged for whoever picks it up.* 7 ERROR and 11 MISMATCH remain, none of them in `parsers/postgres.py`'s own surface: `OVERLAY(a PLACING b FROM 1)` (2, base `FUNCTION_PARSERS`), `EXCLUDE USING gist(...)` index-constraint bodies (2), `COPY ... WITH (FORMAT ...)` (3), a missing `_type=DataType(DType.UNKNOWN)` annotation on `SELECT col[1]` (5, wants the P6 type annotator), `XMLELEMENT` (2), plus one each of `ALTER COLUMN ... TYPE`, `IS JSON`, and two LATERAL shapes. The 45 stubs are 26 `_parse_recursive_with_search`, 10 `to_interval`, 4 `_normalize_collate`, 2 `annotate_types`, 2 `Dialect.to_json_path`, 1 `_parse_xml_namespace`.

*Scoping note for the next dialect, with the arithmetic actually done.* `redshift.py` (160 LOC), `materialize.py` (45) and `risingwave.py` (66) all `extend PostgresParser` upstream — the same "looks small but pulls in a base" shape as the Databricks chain — and this round unblocks all three, since the base they need now exists. It is tempting to take them next for that reason, but the measured trade says otherwise: they are 271 LOC against 220 currently-non-EXACT rows (redshift 205 of 326, materialize 12 of 37, risingwave 3 of 9), ~0.8 rows per LOC, whereas **DuckDB is 414 LOC against 494 non-EXACT rows of 1,053 (~1.2 rows/LOC) and is standalone** — it extends the base `Parser` directly, so it carries no chain risk either. §10's original order (DuckDB ahead of the Postgres descendants) survives contact with the numbers; the descendants are the better *follow-on*, not the better next pick. An earlier draft of this paragraph asserted the opposite from a guessed "DuckDB is 1,000+ LOC" figure — it is 414, measured — which is worth recording as its own small lesson about sizing a component by impression.

**R20 — the `Dialect` class lands, and the defect layer below R19 is CROSS-LANGUAGE LOOKUP: a faithful `self.X` that resolves through Python's MRO and reads `undefined` in JS.** Landed 2026-09-08 on branch `p5-dialect-class`. `src/dialects/dialect.js` gains `class Dialect` (105 settings, 20 methods), `registerDialect`, `get_or_raise` with the `"name, k=v"` grammar, and `parse`/`parse_into`/`tokenize`/`generate`; `src/parsers/base.js` ports `parsers/base.py`'s `BaseParser`, which `Dialect.parser_class` points at and which is NOT `parser.Parser` (four `NO_PAREN_FUNCTIONS` entries; `SELECT LOCALTIME` parsed as a `Column` without it). Registration is explicit rather than upstream's lazy `importlib.import_module`, because `get_or_raise` is called from `Tokenizer`'s constructor and a JS constructor cannot `await import(...)` — CONTRACTS.md §8 now carries eight new rows for this file alone. Measured against `origin/main` AFTER merging R19's Postgres port, so the figures are the marginal effect of this branch alone: **EXACT 10,169 -> 10,242, MISMATCH 3,335 -> 3,261, ERROR 1,235 -> 1,235 (flat), STUB 739 -> 740**. A 34-dialect sweep against a worktree pinned at `origin/main` confirms **zero rows that were EXACT there are not EXACT here**; 61/61 unit tests throughout.

*R17's `registerParser` blocker is closed, and closing it cost three latent defects.* `exp.select("*")` now yields `Star()` rather than `Column(Identifier('*', quoted=True))`, and spark's `/*+ ... */` hints — R18's deferred blocker (a), "20 of spark's 24 residual MISMATCH ... blocked on R17's registerParser hook" — go **+20 EXACT**, the whole cluster. R19's own deferred blocker goes with them: its 10 `to_interval` stubs ("needs R17's `registerParser` hook — still exported and never called, still genuinely P5") are **postgres +10 EXACT / -10 STUB** here, without a line of postgres-specific work. Two independently-diagnosed blockers, two sessions apart, one uncalled hook. But turning `PARSE` on immediately broke `exp.select("a").from_("t")`, because `query_methods.js` had dropped the `prefix` argument at all nine `_apply_*_builder` call sites (`core.py:2635/2661/2708`): `Select.from_` must parse `"FROM t"`, not `"t"`. The P2-safe leaf fallback ignores `prefix`, so for as long as `PARSE` was null the omission produced the same node shape either way and nothing could have seen it. **This is R18's "a correctness argument that rests on nothing-reaches-this-yet expires the moment something does", except the expiring shortcut was in a DIFFERENT file from the change that expired it** — no per-file sweep would have paired them. Two more of the same shape: `astDump` had no `isinstance(node, Dialect)` branch (`tools/astdump.py:110`) and had been serialising a Dialect held on an arg as `[object Object]`; and `registerAstDialects` was a third exported-and-never-called hook, the same family as R17's `registerParser`.

*The new hazard class.* R13 found stubs, R14 empty tables, R17 wired entries with the wrong shape, R18 calls with the wrong argument shape, R19 tables with no reader. This round found: **a read that is faithful, of a member that exists, which returns `undefined` because the two languages disagree about where class attributes live.** `src/parser.js` reads ~40 settings as `this.dialect.X`; every one of those lines is a correct transliteration of `self.dialect.X`. Python resolves it through the MRO to the class attribute. JS `static` fields live on the CONSTRUCTOR and are not on the prototype, so against a real class every one of them is `undefined` — and 24 of the base defaults are falsy, so two thirds of them behave correctly by accident and the rest silently flip a branch. The harness never saw it because `standInDialect` returns a plain object whose settings are OWN properties. Fixed by mirroring the settings onto `klass.prototype` in `registerDialect`, which is the same lookup Python performs. **The generalisable check is a grep for `\bthis\.[A-Z_]+\b` and `\.[a-z_]+\.[A-Z_]+\b` against the set of names declared `static`** — the same one-line shape as R18's `_match_text_seq` sweep, and nothing runs it today. Run against this branch it returns two hits, both inside `static format_time`, where `this` IS the class — so the sweep needs to exclude static method bodies or it reports upstream's own `@classmethod`/`cls` reads as defects.

*Two findings handed over rather than fixed, both with the exact edit written down.* (1) `Literal.string` is `String(value)` where upstream is `str(string)` (`core.py:1768`); they differ on `None` -> "None" vs "null", reachable via `TO_DATE(x, '')`. The shim now exists (`pyStr`, `_py/str.js`) but landing it ALONE regresses two bigquery rows, because `pyStr` also honours this repo's convention that a Number models a Python float, and the one caller passing a bare number is `src/parser.js:370`, whose upstream (`parser.py:411`) passes a Python INT. **The complete fix is two edits — `pyStr` in `focused_methods.js` AND `1` -> `1n` in `parser.js` — and half of it is a net regression**, so neither is landed and the pair is recorded at the call site and in `spike/p5/fuzz_dialect_defaults.mjs`'s GAP output. (2) `src/parser.js:5269` reads `this.dialect.FORMAT_MAPPING || this.dialect.TIME_MAPPING`, but upstream's `or` falls through on an EMPTY dict while an empty JS `Map` is truthy, so every dialect with no `FORMAT_MAPPING` takes the wrong operand; `pyTruthy` is already imported there. Both are in `src/parser.js`/`src/parsers/`, which this dispatch was scoped out of.

*R19 sweep on this file's own output, stated rather than assumed.* 32 of the 105 settings have exactly one occurrence in `src/` — their own declaration — because their consumers are the P4 generator and the P6 optimizer. That is upstream's structure reproduced, not a gap: every one of them is DIFFED against CPython by `spike/p5/fuzz_dialect_defaults.mjs`, so they are verified-but-unconsumed rather than unverified. The distinction matters, because R19's hazard is a table nothing reads AND nothing checks; these are checked.

*Explicitly deferred, not dropped.* `src/dialects/<name>.js` settings classes (the ~192-LOC `Snowflake(Dialect)` and its 33 siblings) — `spike/p5/fuzz_dialect_parse.mjs` measures their absence rather than estimating it: routing a name through the registry to `SnowflakeParser` scores **1,917/2,232 REACHED rows exact (85.9%)** against the harness's 98.7%, and the difference is exactly that file plus a Snowflake `Tokenizer` subclass. `compareVersion`/`dialectEquals` (`version` is exact BigInt, so the sentinel problem §7 P8 anticipated does not arise). `normalize_identifier`, announced as `NotPorted` because it needs a `pyLower` shim that would otherwise be written with `toLowerCase()` — the `\p{...}`/`too_wide` hazard class, corpus-invisible at 0.024% non-ASCII. The ~90 shared generator helpers, which depend on P4's conventions.

**R21 — P4's blocking step is landed, and the defect layer it exposes is DATA rather than code: 126 class-level settings that no counter in this repo had ever compared against upstream, plus a `**spread` that reads as one seeded item and is really ten.** Landed 2026-09-08 on branch `p4-generator-blocking-step`. `src/generator.js` exists: `_buildDispatch` + `_DISPATCH_CACHE`, `sql()` exact-class dispatch with only the `Func`/`Property` fallbacks, the pretty/comment/indent primitives, `unsupported()`, `function_fallback_sql` over `argTypes` declaration order, `too_wide` via `cpLen` — plus the seeded skeleton: **432 `*_sql` methods of which 6 are wired and 426 throw `NotPorted`**, 133 anchored `TRANSFORMS` lines (+10 behind the spread, = upstream's 143, `TRANSFORMS.size === 0`), and 126 settings. `src/_gen/dispatch/index.js` is generated by the new `tools/gen_dispatch.mjs` from the already-harvested `corpus/parity/dispatch.json`, flipping parity probe 4 from `NOT_BUILT` to `PASS`. **The runtime builder's 417 method-derived entries match CPython's own `_build_dispatch` output entry-by-entry — same classes, same handler names, 0 wrong, 0 missing, 0 extra — with the remaining 143 of upstream's 560 accounted for exactly by the unseeded `TRANSFORMS`.** Deliberately NOT done, per §7 P4's sequencing: `generators/snowflake.js`, `transforms.js`, `jsonpath.js`, `optimizer/simplify.js`, and the stub queue itself.

*The new hazard class: a setting with no oracle.* R13 found stubs invisible to the tracer, R14 empty dispatch tables, R17 wired entries of the wrong shape, R18 wrong argument shape at the call site, R19 a table with no reader. This round found the one below: **a hand-transcribed class-level VALUE that nothing anywhere compares against upstream.** `parser.py`'s ~162 attributes and `generator.py`'s 126 were seeded per §8.1 Rule 2′ and then never checked — `corpus/parity/` carries dispatch, exprs, functions, parser tables and tokens, but no generator settings at all, so a `static FOO = true` that is `False` upstream would have been invisible until some dialect finally read it, and would then have surfaced as an ordinary output MISMATCH far from its cause. Closed by `spike/p4/gen_generator_base_ref.py` + `fuzz_generator_base.mjs`, which compares **123 of the 126 by value** (the other 3 are the deliberately-deferred tables), plus 76 machinery primitives and 42 generate cases at both `pretty` settings. It classifies EXACT/SKELETON/MISMATCH the way `fuzz_ast_coverage.mjs` does and names each SKELETON item individually, so "green" cannot absorb them. **The same gap remains open for `parser.js`'s ~162 attributes; the mechanical form is the same and this file is the template.**

*A `**spread` is not one entry.* `TRANSFORMS`'s seed contained the line `// py:137 SPREAD: JSON_PATH_PART_TRANSFORMS — merge manually`, which reads as a single anchored TODO and in fact stands for **ten** entries (`JSONPath{Filter,Key,Recursive,Root,Script,Selector,Slice,Subscript,Union,Wildcard}`). 133 explicit + 10 = the 143 that `Generator.TRANSFORMS` resolves to, so a burndown counting anchored lines would have reported "134 to go" and finished 10 short. Every `**` spread the seeder emits has this property; `tools/seed_static.py` should print the expansion count rather than the name alone.

*Three defects in the 27 hand-ported methods, all found by an adversarial second read rather than by any test.* (1) `property_sql` interpolated `undefined=` where CPython emits `None=` — Python's `dict.get` miss yields `None` and an f-string renders it as four literal characters. Reachable and observable: `StorageHandlerProperty`, `SerdeProperties`, `PartitionByListProperty` and `RowFormatSerdeProperty` have neither a `PROPERTY_TO_NAME` entry nor a `*_sql` method, so they land in `property_sql`, one of `sql()`'s two fallbacks. **`unsupported_messages` matched exactly in both, so the §3.1(D) message assertion — the one thing P4's exit criteria names for this path — would never have caught it; only the SQL string diverged.** (2) `sql()` raised a JS `TypeError` where upstream raises `ValueError`, i.e. not a `PyException` and invisible to `instanceof PyValueError`, against 15 existing sites using the convention. (3) `_trailingOptions` silently SWALLOWED a trailing enum argument: `exp.DType.*` and `Properties.Location.*` are plain frozen objects, so `constructor === Object` accepted them as the options bag and dropped the argument with no error. Unreachable from any upstream call site today — and recorded anyway, because that is exactly the "safe because nothing reaches it yet" argument R18 says expires silently, with 426 stubs still to be written against those signatures. **`expressions/core.js:541 trailingOptions` has the identical gap and is shared P2 code, so it is reported here for the foundation owner rather than changed on this branch (§8.1 Rule 3).**

*New counter, and the cheapest one added since R18's grep.* `tools/lint_anchors.mjs` checks that every `// py: <file>:<line>` anchor points at the line that actually DEFINES the member below it. R18's `_parse_row` finding ended "a method whose body has nothing to do with its own `py:` anchor is not detectable by any counter here" — this is the machine-checkable half. It cannot judge a body (R1's residual, §8.5's human gate), but a wrong anchor makes that gate *worse than useless*, because the reviewer diffs against the wrong source. It also catches the likelier failure: at ~85 LOC/day of upstream churn every resync shifts line numbers, and 484 silently-wrong anchors are indistinguishable from 484 right ones. **Result across every ported file today — `generator.js`, `parser.js`, all six `parsers/*.js` — 883 member anchors, 0 bad**, so it lands green rather than with a backlog. Self-verified against an injected off-by-one and an injected past-EOF anchor. Wired into `make lint` and `run_all.sh`.

*The `registerGenerator` hook now has a caller — R17's twin, closed.* `expressions/core.js:515` exported `registerGenerator` from P2 and **nothing ever called it**, so `Expr.sql()` was permanently the throw `"No SQL generator registered (available in P4)"` — R19's shape exactly. Importing `src/generator.js` installs it. Verified safe before wiring rather than assumed: nothing asserts the old message (`grep` finds only the throw itself), and the parse path reaches the generator through `kernelSql`, not `Expr.sql()`. `exp.Literal.string("it's").sql()` now returns `'it''s'`, byte-matching CPython. **`registerParser`, its twin, was still uncalled when this branch was written; R20 closed it independently on `p5-dialect-class`. Both hooks now have callers — but they were found and fixed by two separate rounds, months apart, and neither round's counters would have found the other's, which is the argument for a sweep over EXPORTED-BUT-UNCALLED registration hooks rather than a third discovery.**

*Design decisions worth not re-litigating.* (a) `_buildDispatch` stores method NAMES, not bound functions — name lookup on the instance at call time is the same resolution `getattr(cls, name)` performs, keeps subclass overrides winning, and makes the runtime table directly comparable against the `_gen/` snapshot. (b) `Generator.__init__`'s 12 kwargs became one options object (`TokenizerCore`'s 26 set the precedent), and `func`/`format_args` take a trailing options object because their params are genuinely keyword-only after `*args` — R18's shape. `expressions`/`indent`/`maybe_comment` do too, but on measurement rather than necessity: **across upstream's own 169/14/7 call sites not one passes an argument beyond the 2nd positionally**, so a 12-parameter positional signature would be the deviation. `sql()` stays positional because 579 of its 708 call sites pass `(expression, key)` that way. (c) `SUPPORTED_JSON_PATH_PARTS` — the single setting whose value is a call into the unported `jsonpath.py` — is a **throwing static getter**, chosen over an empty `Set` (membership silently false, the worst option) and over absence (reads `undefined`, R18's `REGEXP_EXTRACT_DEFAULT_GROUP` failure). All seven deviations are in `CONTRACTS.md` §8.

*Probe status, verified rather than inherited.* `spike/run_all.sh` has exactly the two red probes R17 and R18 already recorded, both confirmed pre-existing and untouched by this branch: "P3: check_command_warning strings" (the environmental `ImportError` on an incomplete `/tmp/sqlglot-ref/tests/`) and "P3: AST oracle coverage (honest)" (the R15 dialect-parser gap). The new "P4: base Generator" probe is green. 76/76 unit tests (61 pre-existing + 15 new), all 7 lints green.

**R22 — `parsers/duckdb.py` is ported, closing the four dialects §10 names for v0, and the defect it surfaced is a STUB THAT OUTLIVED ITS OWN STATED BLOCKER.** Landed 2026-09-08 on branch `p3-duckdb-parser`, taking the fourth and last dialect in §10's priority order. `sqlglot/parsers/duckdb.py` (414 LOC) is ported whole into `src/parsers/duckdb.js` — all 7 module-level builders, all 17 class-table overrides (2 of them plain flags), all 14 methods, nothing stubbed or skipped — extending the base `Parser` directly the way `snowflake.js` and `postgres.js` do rather than chaining. `dialects/duckdb.py`'s 128-LOC `DuckDB(Dialect)` settings class stays P5, the same split every prior dialect port used. `src/parser.js` grows by the `build_json_extract` / `build_json_extract_scalar` pair (py:295/305), genuinely absent: R19 landed the `build_jsonb_*` TRIO because Postgres's `JSON_OPERATORS` names those three by module path, but Postgres builds its own ARROW/DARROW entries from `build_json_extract_path` inline, so nothing referenced the non-B pair until DuckDB — R19's "declared, read by nothing" shape one level down. `build_extract_json_with_path` and `build_array_concat` were already ported and faithful and merely needed exporting (R18's `build_trim` shape again); `_pivot_column_names` (py:5521, one line) was a stub that `DuckDBParser`'s override calls `super()` on. DuckDB uses all three table shapes in one file — EXTEND ×8, REPLACE ×3, FILTER ×4 — and its two FILTER/REPLACE *pairs* are the trap: `^` must be removed from `BITWISE` **and** added to `EXPONENT`, and `ARROW`/`DARROW` must move out of `COLUMN_OPERATORS` **and** into `JSON_OPERATORS`; either half alone silently mis-binds precedence instead of erroring. Measured: **duckdb EXACT 570 -> 973, MISMATCH 366 -> 17, ERROR 77 -> 9, STUB 40 -> 54** (973 of 990 REACHED rows, 98.3%); **full corpus EXACT 10,242 -> 10,677, MISMATCH 3,261 -> 2,902, ERROR 1,235 -> 1,169**. A ROW-LEVEL sweep over all 34 corpora against a worktree pinned at `origin/main` (ba46c8d) confirms **zero rows that were EXACT there are not EXACT here**; 435 gained, of which 32 are outside duckdb (snowflake +15, default +7, spark +6, oracle +2, teradata +1, tsql +1). 77/77 unit tests throughout.

*The new hazard class: a stub's stated blocker is a claim with a timestamp, and nothing re-evaluates it.* R13 found stubs invisible to the tracer, R14 empty dispatch tables, R17 wired entries of the wrong shape, R18 wrong argument shape at the call site, R19 a table with no reader, R21 a setting with no oracle. This round found: **a `NotPorted` stub whose comment correctly named its blocker, two rounds after that blocker landed.** `DataType.from_str`'s parameterised-string path (py:386) threw with the rationale *"needs `parse_one`, which needs `registerParser`, which nothing calls yet"* — and R17 wired `registerParser` (`dialects/dialect.js:1455`). Deleting the throw and calling `maybeParse` was the whole fix, worth **+15 snowflake and +8 duckdb rows**: Snowflake's `build_default_decimal_type(38, 0)` had been dead-ending on that same stub since R16. DuckDB is only what *surfaced* it, via `TYPE_CONVERTERS[DECIMAL] = build_default_decimal_type(18, 3)` and a corpus table with a column literally named `number` (a DECIMAL type keyword), so `_parse_types` speculatively fires the converter. `fuzz_ast_coverage.mjs` counts such a row as STUB, which is the *accepted* state for a blocking step — so it reads as "known work queued" rather than "unblocked work forgotten", and the gap grows monotonically as each round lands infrastructure (R17 `registerParser`, R20 `Dialect`, R21 `registerGenerator`). **Sweep, one line, not yet a probe:** `grep -rn "NotPorted" src/ | grep -iE "needs|requires|pending|until|not yet|nothing calls"`, then check whether each named dependency now exists.

*Two more R18s, and the reason enumerating call sites beats fixing the one the corpus reached.* `_parse_var(any_token, tokens, upper)` takes `upper` THIRD, so upstream's `self._parse_var(upper=True)` written as `_parse_var(true)` gets **both** parameters wrong from one dropped keyword — it enables `any_token` and leaves `upper` off. Found at `parser.js:3773` (py:4421) and `parser.js:4138` (py:5291); the visible symptom was `USING SAMPLE 10% (system, 377)` producing `Var(this=system)` against CPython's `Var(this=SYSTEM)`. All 14 upstream `_parse_var(..., upper=...)` sites were then checked rather than just the two the corpus reached: 9 were already correct, 2 wrong, 1 (py:7658 `AutoRefreshProperty`) not ported at all, 2 in unported `doris.py`. Separately, `DataType.build`/`from_str` take `dialect` as a NAMED parameter upstream that never reaches `cls(...)`; the port destructured `udt`/`copy` but left `dialect` in `...kwargs` and spread it into the node, so every UDT carried a surplus arg that dumped as the JS string coercion `dialect=[object Object]` — R21's None/undefined rendering hazard as the visible surface of an R18 bug underneath. All five call sites were themselves faithful, which is why no call-site sweep would have found it.

*Deliberately deferred, with the residual triaged rather than estimated.* DuckDB's remaining **17 MISMATCH are all `annotate_types`** — every one carries `_type=` in its oracle repr, i.e. the row is optimizer output (P6+), not a parse. The **9 ERROR are all constructs absent from `parsers/duckdb.py` entirely**: `AT TIME ZONE` inside/around `CAST` (4), `COPY ... WITH (FORMAT JSON, ...)` (2, `_parse_copy`/`_parse_copy_parameters` are base stubs), DuckDB's `WITH RECURSIVE ... USING KEY` (1, which upstream's base `parser.py` has no handling for either), and 2 more in the same base-residual class — R17's category, not this file's. `_parse_pivot` (py:5404) stays a base stub, which means the newly-ported `_pivot_column_names` and DuckDB's override of it are correct-but-not-yet-reachable.

*Probe status, verified rather than inherited.* `spike/run_all.sh` was run on this branch AND on a worktree pinned at clean `origin/main`, and both produce **exactly the same two red probes** — "P3: check_command_warning strings" and "P3: AST oracle coverage (honest)". The claim that these two are pre-existing is therefore measured here, not inherited from R21's paragraph. Every other probe is green in both trees; the full `diff` of the two runs is confined to temp-directory paths, `+1` scanned file, one fewer stub in the inventory, and the coverage numbers improving.

**R23 — the generator's demand-closure tool lands, and the first thing it did was reproduce R13 inside itself: it scored 1,029 rows closed of which only 27 could be generated at all.** Landed 2026-09-08 on branch `p4-generator-closure-tool`. `tools/harvest/trace_generate_demand.py` traces, per generate-oracle row, what CPython's generator actually executes (15,540 of 15,540 rows reproduce byte-for-byte; 2,507 distinct units), and `tools/closure_generator.mjs` computes marginal closure over it with `--brief`/`--add`/`--curve`/`--selftest`. **`closure_parser.mjs` has no `--selftest` despite the brief saying to mirror one; the precedent followed instead is `tools/closure.mjs --selftest`, "verify the maths on a synthetic corpus", and the maths functions are exported so the selftest exercises the real ones.** *(Numbered R23, not R22: `p5-snowflake-dialect` had already pushed an R22 when this was written. Three branches are landing R-numbers concurrently and the register has no allocator — the previous round hit the same thing and renumbered on merge. Checking `git show origin/<branch>:PORT_PLAN.md | grep -oE '^\*\*R[0-9]+' | tail -1` across live branches before picking a number costs one command.)*

*The conjunctive-closure lesson, as sharply as this corpus can state it.* `identifier_sql` blocks **10,870 of 15,540 rows — the single largest blocker in the corpus — and porting it opens ZERO**. `column_sql` + `column_parts` together also open zero. All three together open **779**. The per-unit blocking counts sum to 1,090,846 against 15,540 real rows, a 70.2x over-count, so any queue ranked by "blocks N rows" mis-sizes every brief here exactly as it did on the parser side. Those three are the keystone group and were ported: measured on the honest probe, generate-oracle **EXACT 3 -> 27, STUB 125 -> 101, MISMATCH 0, ERROR 0** in the current tree, and **+779 (250 -> 1,029, 1.61% -> 6.62%) once P5 registers dialect names**. Both numbers are real and they are not the same number; reporting only the second would be the over-claim this entry is about.

*The tool's own R13.* The first version scored units — methods, `TRANSFORMS` entries, dialect-generator overrides, `transforms.py` functions — and got 1,029. The new probe `spike/p4/fuzz_generate_oracle.mjs` was written to check that claim rather than quote it, and **1,002 of the 1,029 could not be generated at all**: they name a dialect, and the port registers none but the base, so `Dialect.get_or_raise("snowflake")` throws before any generator method runs. The trace was measuring which units RUN and had no way to express a precondition that gates whether the row can start. Fixed by recording `dialect:<name>` as a derived unit and resolving it against the real registry, so the count corrects itself the day P5 lands rather than the day someone notices. **This is R13's exact shape — a demand tracer blind to a gating dependency — found this time by the probe that was written alongside the tool instead of three weeks later, which is the only reason it is a paragraph here and not a regression.**

*Three divergences from `trace_parse_demand.py`, each one a recorded defect class.* (1) It wraps EVERY generator method, not just `*_sql`: 52 of `Generator`'s 484 functions are non-`*_sql` hot-path helpers (`expressions`, `func`, `column_parts`, `query_modifiers`, `table_parts`, ...) and a prefix filter is literally R13. (2) It records `TRANSFORMS` entries as first-class units, because a row routed through `TRANSFORMS[Union]` needs a table entry and not a method — R14/R17/R19's family. (3) It attributes each unit to the class that DEFINED it, so a dialect override is `SnowflakeGenerator.select_sql` and never bare `select_sql`; scoring those against `src/generator.js` would claim closure `src/generators/` cannot deliver, since that directory does not exist. `transforms.py` demand is reached by mutating the transform list inside `preprocess`'s closure in place — wrapping the module after import is invisible to closures captured at class-creation time. **The boundary that remains (`helper.py`, `expressions.py`, jsonpath internals) is written into the docstring, because R13's cost came from a blindness nobody had stated, not from one that had.**

*An unwired `TRANSFORMS` entry cannot announce itself, and that is a real gap in the generator's error taxonomy.* When `sql()` finds no handler it raises `PyValueError: Unsupported expression type Union` — **byte-identical to what upstream raises when it genuinely has no handler**, so the port's "this entry is not ported" and upstream's "this is unsupported" are indistinguishable from the exception alone. Three rows hit it here (`Union`, `Except`, `Operator`), and a naive probe reports them as "3 ERROR, must be 0". The probe disambiguates them from the oracle's recorded demand — if the row's unit set contains `TRANSFORMS[Union]`, upstream HAD a handler and the port simply has not wired it — but that is the probe compensating for the runtime, not a fix. **`sql()` itself still cannot distinguish the two, and with `TRANSFORMS` at 0 of 143 that will mislabel every unported entry the stub queue touches.**

*Two burndown claims in `src/generator.js`'s own header were wrong, in the file whose header exists to be the burndown of record.* The non-`*_sql` stub count read **46** and was **31** at the blocking step (30 now); and `grep -c NotPorted src/generator.js`, which the header names as the method burndown, **over-counts by exactly 3** — `constructor`, `preprocess` (py:981 `ENSURE_BOOLS`) and `_move_ctes_to_top_level` are ported bodies carrying a guarded throw for a branch the base class cannot reach. Found by the tool's `--selftest`, which asserts the source grep and the loaded module classify every method identically; the loose predicate "the body mentions `NotPorted`" scored all three as stubs. Both corrected, with the exact commands that check them.

*`pyLower` lands, which is R20's deferral coming due rather than scope creep.* `dialects/dialect.js:1028` left `normalize_identifier` an announced stub with the reason recorded — "`pyLower` is a `_py/` addition with its own differential gate ... P4/P6 is when it must be real" — and `identifier_sql` is the caller that made it real. Measured over the full range: **Node's `toLowerCase()` maps 67 code points CPython 3.9.25 does not**, the same 67 `pyUpper` exists for, and identifier normalization is output-visible, so `toLowerCase()` would pass all 15,540 corpus rows (0.024% non-ASCII) and still be wrong — R4's `too_wide` class exactly. **Unlike `str.upper()`, `str.lower()` IS context-sensitive**, so the generator's existing `upper_context_sensitive` assertion would fire: a full-range sweep in five contexts measures **U+03A3 as the only such code point** (CPython's `handle_capital_sigma` emits final sigma at end of word). `pyLower` is exact on every other code point and REFUSES on that one, with the two tables needed to finish it (Cased, Case_Ignorable) named in the docstring — R19's "loud stub over a silent near-miss". The sweep quantifies over the whole range rather than a probe list because the table's claim is "exact except for the code points listed here", and a probe list cannot support that quantifier. `tools/lint_unicode.mjs` then caught Unicode property escapes in the new docstring; the comment was reworded rather than the lint weakened.

*The 27-row probe passed while the method was wrong twice, which is R4 generalised.* `identifier_sql` is demanded by **10,870 of 15,540** corpus rows and those rows still exercise almost none of it: 0.024% non-ASCII, no empty identifier, and one of the eight `normalize` x `identify` combinations. `spike/p4/fuzz_identifier_sql.mjs` diffs it against CPython over **1,216 cases (38 names x normalize x identify x quoted x pretty), 1,160 EXACT, 0 unexplained**, and found both defects in one run. (1) `text[:1]` was written with `cpAt` — an INDEX, which this port faithfully makes raise `PyIndexError` out of range — where `text[:1]` is a SLICE and Python slices clamp; `Identifier(this="")` crashed. (2) **`src/expressions/core.js:525` `SAFE_IDENTIFIER_RE = /^[_a-zA-Z][\w]*$/u` against upstream's `re.compile(r"^[_a-zA-Z][\w]*$")`: Python's `\w` on a str pattern is UNICODE-aware, JS's is ASCII-only even under `/u`.** That is shared P2 code with two readers and it is **not generator-only** — `toIdentifier("café").args.quoted` is `true` here and `False` upstream, a live AST divergence on the parse side today. Reported rather than patched (§8.1 Rule 3, R21's precedent), and counted by the probe as one narrowly-defined GAP that cannot absorb a new defect: a case qualifies only if the port's regex verdict for that NAME differs from CPython's. **For a method whose whole job is a decision over arbitrary text, corpus coverage is not method coverage** — the same argument `too_wide`'s `.length` port lost.

*The stand-in dialect could not serve the new reads, in both of R18(d)'s modes at once.* `BASE_DIALECT_GENERATOR_SETTINGS` is the frozen object `_resolveDialect(null)` returns, and its own docstring already demanded the process: "a field a stub needs later must be ADDED here and re-read from CPython, never guessed". Three settings were missing (`IDENTIFIERS_CAN_START_WITH_DIGIT`, `SUPPORTS_COLUMN_JOIN_MARKS`, `PROJECTION_ALIASES_SHADOW_SOURCE_NAMES`) and would have read `undefined` — **all three are False at the base `Dialect`, so they would have been correct BY ACCIDENT and silently wrong for the first dialect that flips one**, which is R20's finding restated. The fourth read, `can_quote`, is a METHOD, and the stand-in cannot host one honestly: a second copy of real `Dialect` behaviour (`case_sensitive` + `SAFE_IDENTIFIER_RE`) is R18(d) with extra steps. It announces `NotPorted` instead. **The consequence is that "wired" is now two different numbers — 8 `*_sql` methods have a real body, 7 of them also run against the stand-in — and `test/generator_dispatch.test.mjs` asserts both plus the name of the one that differs, because collapsing them would hide the more interesting one.** Wiring `Dialect.generator_class` is the P5 line that merges them back.

*Probe status.* `spike/p4/fuzz_generate_oracle.mjs` is new and wired into `run_all.sh` alongside the closure selftest — the generator's counterpart to "P3: AST oracle coverage (honest)", wired for the same reason: without it the closure percentage is the only number anyone would quote. It buckets EXACT/MISMATCH/STUB/ERROR/**PARSE**, the last so a parser gap is never counted as a generator defect (97 rows here), and it fails if the closure tool over-claims. Baseline measured in a worktree pinned at the pre-change commit rather than inherited: **generate-oracle EXACT 3 -> 27, STUB 125 -> 101**. `spike/p4/fuzz_identifier_sql.mjs` and the closure selftest are wired in beside it. A full `run_all.sh` leaves exactly the two red probes R17/R18/R21 already recorded, both environmental/R15 and untouched here. 77/77 unit tests; the burndown assertion in `test/generator_dispatch.test.mjs` updated on purpose 6 -> 8, which is what it exists for.

**R24 — `dialects/snowflake.py` lands and the real `get_or_raise("snowflake").parse()` path closes its measured gap exactly as predicted; the defect below it is a REGISTRY WRITE that silently downgrades a correct entry to a plausible one.** Landed 2026-09-08 on branch `p5-snowflake-dialect`. `src/dialects/snowflake.js` ports the 192-LOC `Snowflake(Dialect)` settings class whole — 19 setting overrides, the 55-entry `TIME_MAPPING`, the `DATE_PART_MAPPING` merge, `PSEUDOCOLUMNS`, `can_quote`'s DUAL exception, and the nested `Tokenizer` subclass (33 added keywords, `$$` raw strings, `x'…'` hex strings, `//` comments, `\` escapes, `KEYWORDS.pop("/*+")`, `COMMANDS` minus `SHOW`) — plus the `registerDialect(Dialects.SNOWFLAKE, Snowflake)` call that both resolves the name and, per R20, mirrors the settings onto the prototype so `src/parsers/snowflake.js`'s ~40 `this.dialect.X` reads resolve against a real instance. Verified rather than assumed: a real `new Snowflake()` reads `NULL_ORDERING` `"nulls_are_large"`, `NORMALIZATION_STRATEGY` `UPPERCASE`, `VALID_INTERVAL_UNITS` 117 (base 116). Measured on `spike/p5/fuzz_dialect_parse.mjs`, the real `Dialect.get_or_raise(name).parse(sql)` entry point: **snowflake 1,917/2,232 (85.9%) -> 2,359/2,390 (98.7%), MISMATCH 315 -> 31, ERROR 175 -> 17.** That probe's header had predicted the 85.9%-vs-98.7% gap was "exactly the size of that missing file, measured instead of estimated"; it was, and the prediction is now settled rather than left standing. The real path in fact scores four rows ABOVE the harness-routed figure (2,359 vs 2,355 at R17, 2,359 vs 2,359 today) because `fuzz_ast_coverage.mjs` builds token streams from P1's harvested tokenizer SETTINGS and deliberately has no per-dialect `Tokenizer` SUBCLASS. `Generator = SnowflakeGenerator` and `class JSONPathTokenizer` are the two deliberate omissions — `generators/snowflake.js` and `sqlglot/jsonpath.py` are P4 gaps, and `registerDialect` already throws `NotPorted` on any `generator_class`, so both are holes with live guards rather than silent absences.

*R21's hazard class, closed for this file rather than re-inherited.* R21 found "a hand-transcribed class-level VALUE that nothing anywhere compares against upstream" and left the same gap open for `parser.js`'s ~162 attributes. `dialects/snowflake.py` is almost nothing BUT such values, so it got its own oracle in the same pass: `spike/p5/gen_snowflake_dialect_ref.py` + `fuzz_snowflake_dialect.mjs`, **451 checks green**, covering all **105 RESOLVED** settings (resolved via `getattr`, not own via `vars` — so a missing override AND an *invented* one both fail, and no "did we transcribe every line?" review catches the second), the nested tokenizer's declared settings and its six `__init_subclass__`-derived ones, the 347-entry `KEYWORDS` map, and 168 `can_quote` cases of which 24 are the DUAL exception. **A first-run pass was treated as suspect rather than as evidence** (R16's rule applied to a probe instead of a method): seven mutations of the ported file each drive it red — dropped `NULL_ORDERING` (2 failures), one flipped `TIME_MAPPING` entry (2), omitted `KEYWORDS.pop("/*+")` (4), omitted `initTokenizerSubclass` (10), dropped `can_quote`'s DUAL clause (6), an invented setting (1), `COMMANDS` keeping `SHOW` (2).

*The new defect: a registry entry overwritten by a worse one, through a path nothing asserts on.* `spike/p3/dialect_tokenizer.mjs`'s `dialectClassFor` synthesises a throwaway `class Snowflake extends Dialect { static Parser = SnowflakeParser }` and **ends by calling `registerDialect(dialect, klass)`** — correct while no real dialect class existed, and a silent downgrade the moment one does. Confirmed by running it both ways rather than by reading it: with a real `src/dialects/snowflake.js` imported, touching the harness moved `Dialect.get_or_raise("snowflake")` from `NULL_ORDERING` `nulls_are_large` / `TIME_MAPPING` 55 back to `nulls_are_small` / 0. **`parser_class` stayed `SnowflakeParser` throughout, which is what makes it invisible** — every routing assertion still passes and only the settings rot, so the failure would have surfaced as ordinary output MISMATCHes in whichever probe imported both first. This is R19's "a correct value replaced by a plausible one" and R20's "wrong by lookup, not by transcription" in a third location: not a stub, not an empty table, not a wrong argument shape, but **a WRITE to a shared registry from test scaffolding, ordered by import side-effects**. Fixed by returning an already-registered class instead of synthesising over it. The fix imports no real dialect, so `fuzz_ast_coverage.mjs` is byte-identical against a pristine `origin/main` worktree (**EXACT 10,242, snowflake 2,359/2,390**) — measured, not asserted, because duckdb and the generator closure tool were landing on `main` in parallel. *The generalisable check, and nothing runs it today:* grep test/spike scaffolding for calls to the `register*` hooks `src/` owns (`registerDialect`, `registerParser`, `registerGenerator`, `registerExprClasses`, `registerAstEnums`, `registerAstDialects`). R17/R21 found those hooks EXPORTED-AND-UNCALLED twice; this is the same family from the other end — **called, by the wrong caller, with the wrong argument.**

*A base-layer defect the new oracle found on its second run, in a file this branch never set out to touch: `\w` means different things in the two languages.* `expressions/core.js:525` had `SAFE_IDENTIFIER_RE = /^[_a-zA-Z][\w]*$/u` — a character-for-character transliteration of upstream's `re.compile(r"^[_a-zA-Z][\w]*$")`, and wrong twice. **(1)** Python's `\w` on a `str` pattern is UNICODE-aware ("alphanumeric as defined by `str.isalnum()`, plus underscore"); JS's `\w` is *always* `[A-Za-z0-9_]`, and **the `u` flag does not change that** — it governs `\p{...}` and surrogate handling only. So `"Straße"` and `"DUAＬ"` are safe identifiers upstream and were unsafe here. **(2)** Python's `$` matches before a single trailing newline, so `"abc\n"` matches upstream (`"abc\n\n"` does not) and did not here. Fixed as a frozen object with a `.test()` method — keeping upstream's name and leaving both call sites (`toIdentifier`'s `quoted` default, `Dialect.can_quote`) byte-identical — with the `\w` equivalence established the way §4.6 requires rather than from the docs: a full `sys.maxunicode` sweep of Python's `\w` against `str.isalnum() or ch == "_"` returns **0 mismatches over all 1,114,112 code points**, so the table-generated `pyIsAlnum` is exact and no runtime `\p{...}` is needed. *Why no counter in this repo could have found it, and why it is not merely academic:* the corpus stayed at **EXACT 10,242 with the bug and without it** — the divergence needs an ASCII FIRST character (both languages' `^[_a-zA-Z]` is ASCII-only, which is what makes `"ÄÖÜ"` correctly unsafe in both and the rule non-obvious) followed by a non-ASCII word character, and not one of 15,540 atoms has that shape. But `toIdentifier`'s `quoted` default is `not SAFE_IDENTIFIER_RE.match(name)` and is live on the build path today, so any user calling `exp.to_identifier("Straße")` got `quoted=true` where CPython gives `false`. **This is the R20 class — a faithful read that means something else across the language boundary — but one layer lower than a class-attribute lookup: the REGEX DIALECT itself.** Guarded in `test/expressions/core.test.mjs` rather than in a probe, because the corpus cannot reach it; both new tests were confirmed to go red against the old regex. *Generalisable check, and nothing runs it today:* grep `src/` for `\w`, `\d`, `\s`, `\b` and `$` inside regexes ported from Python `str` patterns — each is ASCII-only or end-anchored in JS and Unicode-aware or newline-tolerant in Python.

*Probe status, verified rather than inherited.* `spike/run_all.sh` has exactly the two red probes R17/R18/R21 recorded, both re-confirmed on this branch and untouched by it: "P3: check_command_warning strings" (the environmental `ImportError` on this sandbox's incomplete `/tmp/sqlglot-ref/tests/`) and "P3: AST oracle coverage (honest)" (the R15 gap, whose residual is now the remaining `parsers/<dialect>.py` subclasses). The new "P5: Snowflake dialect vs CPython" probe is green and wired into `run_all.sh`. 79/79 unit tests (77 pre-existing + 2 new), `fuzz_dialect_defaults.mjs` 425/425, `fuzz_snowflake_dialect.mjs` 667/667.

**R25 — the P4 "render a basic SELECT statement" keystone group lands, and it exposes a second blind spot in the same tool R23 built: `closure_generator.mjs`'s "closed" prediction assumes the row can be PARSED, an axis its GENERATE-time demand trace never checks.** Landed 2026-09-08 on branch `p4-generator-keystone`. `src/generator.js` gains the "render a basic SELECT statement" keystone cluster — `select_sql`, `query_modifiers`, `after_limit_modifiers`, `offset_limit_modifiers`, `options_modifier`, `prepend_ctes`, `table_sql`, `table_parts`, `from_sql`, `datatype_sql`, `cast_sql` — plus the connective glue `tools/closure_generator.mjs --add`/`--curve` showed was co-required to make any of them actually open a row rather than sit next to ten still-missing siblings (this project's "oracle closure is conjunctive" lesson, applied): `with_sql`, `cte_sql`, `tablealias_sql`, `binary`, `alias_sql`, `where_sql`, `join_sql`, `group_sql`, `order_sql`, `ordered_sql`, `limit_sql`, `offset_sql`, `subquery_sql`, `filter_sql`, `distinct_sql`, `tuple_sql`, `paren_sql`, `not_sql`, `connector_sql`, `and_sql`, `or_sql`, `eq_sql`, `gt_sql`, `lt_sql`, `neq_sql`, `mul_sql`, `mod_sql`, `sub_sql`, `add_sql`, `dpipe_sql`, `is_sql`, `between_sql`, `all_sql`, `any_sql`, `in_sql`, `attimezone_sql`, `localtime_sql`, `localtimestamp_sql`, `op_expressions` — 42 real bodies, taking bodied `*_sql` methods from 8 to 50 (`test/generator_dispatch.test.mjs` updated with both new numbers). Deliberately OUT: `create_sql`/`createable_sql`/`truncatetable_sql` (DDL, not "a basic SELECT"), `set_operation`/`set_operations` (UNION family, a separate feature), `pivot_sql`/`tablesample_sql`/`window_sql`/`lateral_sql` (advanced FROM-clause features), and anything gated on `transforms.py` or a dialect's own generator subclass — all still `NotPorted`, named in a stub-queue comment where their upstream line numbers already point.

Two genuine infra gaps surfaced while porting, fixed rather than routed around, because the alternative was a plausible-looking wrong port: **(1)** `Binary.left`/`Binary.right` — upstream `@property`s defined once on `Binary` (`core.py:1628`) and inherited for free by every binary-op subclass (`Add`, `And`, `EQ`, ...) — did not exist ANYWHERE in `expressions/query_methods.js`; `connector_sql` (needed for `AND`/`OR`) reads `expression.left`/`.right` directly. Fixed by installing the getters generically on every class whose `bases` names `Binary`, since — as the file's own docstring already states — generated classes here do not form a JS inheritance chain, so a getter installed once on `Binary` itself would not reach its subclasses (this is R20's mechanism, applied to a P2 file rather than P3/P4 this time). **(2)** `BASE_DIALECT_GENERATOR_SETTINGS` (`generator.js`'s stand-in `Dialect`, R18(d)'s naming) was missing five fields this group's methods read — `ALIAS_POST_TABLESAMPLE`, `ALIAS_POST_VERSION`, `NULL_ORDERING`, `STRICT_STRING_CONCAT`, `UNNEST_COLUMN_ONLY` — added and re-read from the pinned CPython base `Dialect()` per the object's own documented rule ("a field a stub needs later must be ADDED here and re-read from CPython, never guessed"), not guessed or left to resolve `undefined`.

Measured: `tools/closure_generator.mjs`'s base-method-only achievable ceiling this round is **225 of 15,540 rows** (everything else needs a `dialect:X` name — 0/45 resolvable today even for the dialects with a landed `src/dialects/*.js` settings class, since none has a `generator_class` wired — or a `transforms.py` function, unstarted, `src/transforms.js` does not exist), confirmed with a `--curve` run restricted to base-only rows; this group closes it from **27 to 127** (56% of the true ceiling). `--curve`'s unrestricted top-of-queue is now entirely `dialect:X`/`transforms.X` names, zero base methods — the honest confirmation that base-`Generator` stub-queue work is no longer the gating step for generate-oracle closure. `spike/p4/fuzz_generate_oracle.mjs` (the honest, differential-against-CPython probe): **EXACT 27 -> 57, MISMATCH 0, ERROR 0** — the number to cite for this branch's real, verified progress.

**The new defect, found by the R23-built cross-check firing for a reason other than the one it was built for.** `fuzz_generate_oracle.mjs` asserts every row `closure_generator.mjs` predicts "closed" must come out EXACT, and this is the first branch to trip it for a reason OTHER than dialect-name unavailability — the gap R23 found and fixed before that file was ever committed. Of the 127 rows the tool now predicts closed, **70 are not EXACT: 69 hit `PARSE NotPorted` and 1 a genuine `ParseError`** — the port's PARSER, not its generator, has no path for a construct in the row (confirmed by inspection: the errors name `_parse_pipe_syntax_set_operator` (R17's pipe-syntax gap) and `Dialect.to_json_path` (the `jsonpath.py` gap), both pure P3/P5 territory this branch never touched). Root cause: `tools/harvest/trace_generate_demand.py` instruments CPython's *generator* only, so `closure_generator.mjs`'s demand trace has no model of whether the port's OWN parser can reproduce the row's AST at all — a readiness axis entirely orthogonal to generate-time unit demand. That axis was vacuously satisfied at R23's 27-row baseline (every row that small a set touched happened to already be parseable) and stopped being vacuous the moment a real keystone group multiplied the predicted-closed set by 4.7x. **Not fixed here.** Teaching `closure_generator.mjs` to also gate on parseability means running the real parser over the corpus inside a tool that today does pure set arithmetic over a pre-harvested demand trace — a change to the tool's own architecture, not a two-line patch, and the gap it would be modeling (parser stubs) is P3-shaped work, not P4's. Recorded so the next round starts from "the honest EXACT number" rather than re-discovering the same 70-row gap from scratch. *Generalisable takeaway:* a closure/demand tool built for one subsystem's readiness (generate) will silently over-claim the moment its predicted set depends on a DIFFERENT subsystem's readiness (parse) that it was never wired to check — worth asking of any future closure tool built the same way (a parser-closure tool would have the mirror-image blind spot: it could predict a row "parseable" while its GENERATE side is still all stubs).

*Probe status.* `spike/run_all.sh`'s "P4: generate oracle (honest)" probe is expected to go RED on this branch for the reason above — a third pre-existing-red probe from here forward, joining "P3: check_command_warning strings" and "P3: AST oracle coverage (honest)" — and should not be "fixed" by loosening the cross-check's `must be 0` standard, which is doing exactly its documented job. 79/79 unit tests (numbers in `test/generator_dispatch.test.mjs` updated deliberately, per R13's demand that a burndown live in an assertion someone has to touch on purpose).

**R26 — `src/transforms.js` lands (`preprocess`, `eliminate_distinct_on`, `eliminate_qualify`, `eliminate_semi_and_anti_joins`), and re-measuring the task's own stale "5,028/4,168/2,908-row blocker" brief before writing a line of it saved a wasted session: the honest marginal for all three, today, is 0.** Landed 2026-09-08 on branch `p4-transforms`. The brief that scoped this branch quoted `closure_generator.mjs`'s "rows BLOCKED" column from before the P4 keystone group (R25) landed — the tool's own header calls that column "NOT the marginal", and R13/R21's whole lesson is to trust the marginal instead. `--brief transforms.eliminate_distinct_on` measured **0** real marginal against the pre-branch tree (`src/transforms.js` did not exist, so every consuming row was vacuously blocked by the file's absence too), and the reason survives the file landing: upstream moved every dialect `Generator` OVERRIDE out of `dialects/*.py` into its own `sqlglot/generators/*.py` package sometime after this port's plan was written — grepping the pin finds all 22 call sites of these three functions in `generators/{hive,snowflake,bigquery,postgres,mysql,...}.py`, ZERO in `generator.py` or `dialects/*.py`. Base `Generator.TRANSFORMS` (`src/generator.js`'s `static TRANSFORMS`, 143 entries) maps no class to any of them, upstream or in this port, so the task brief's step 3 ("wire into `src/generator.js`'s `TRANSFORMS` table") does not apply to these three functions at all — doing it anyway would be inventing behavior upstream does not have. The only two transforms.py calls base `Generator` makes on its own (`ensure_bools`, `move_ctes_to_top_level`, from `preprocess()`/`_move_ctes_to_top_level()`, already landed as guarded `NotPorted` throws) stay correctly unreachable, confirmed with `--brief` on both: `ENSURE_BOOLS`/`EXPRESSIONS_WITHOUT_NESTED_CTES` are empty at the base-class level and demanded by 0 corpus rows. Reconfirmed post-landing: `node tools/closure_generator.mjs` now reads `transforms.py 45/45 available` (`hasTransformsModule` is a per-FILE existence check, R14/R17/R19's family again — it cannot see that only 4 of upstream's ~30 functions actually have a JS body) while `generate-oracle rows closed` stayed at exactly **127**, unmoved from R25. `spike/p4/fuzz_generate_oracle.mjs`: **EXACT 62, MISMATCH 0, ERROR 0** — unmoved from the pre-branch baseline, because reachability, not correctness, is what is missing.

*What shipped instead of a corpus-closure number, and why that is still the right deliverable.* Every builder primitive these three functions need — `Select.select`/`.from_`/`.where`/`.subquery`, `alias_`, `column`, `Condition.eq`/`.not_`, `Expr.pop`/`.replace`/`.set`/`.findAll`, `Window`/`RowNumber`/`Order`/`Qualify`/`Distinct`/`Exists` — already existed from P1–P3 and needed zero changes, so this is a faithful, self-contained port with no invented API. Correctness has no corpus signal to lean on (R26's whole point), so it is established the way `fuzz_generator_base.mjs` established the P4 blocking step's: a differential oracle built for this file specifically. `spike/p4/gen_transforms_ref.py` parses 16 hand-picked cases (DISTINCT ON with/without an existing ORDER BY, star selects, pre-aliased projections, and the plain-SELECT/plain-DISTINCT no-op cases; QUALIFY with an aliased window reference, an anonymous one, a star select, an alias already satisfying the filter, and a bare column pulled in from the filter; SEMI/ANTI/mixed/plain JOIN) with CPython's real `sqlglot.transforms` functions and dumps the result losslessly (same `{c,a,m,cm}` shape `tools/astdump.py` uses); `spike/p4/fuzz_transforms.mjs` does the same with the port's parser and `src/transforms.js`, and diffs via `astDump`/`astLoad` byte-for-byte — **16/16 EXACT, 0 mismatch, 0 error.** `test/transforms.test.mjs` (11 tests, no Python needed) asserts the same shapes structurally for `node --test`. Both new probes are wired into `spike/run_all.sh`.

*Generalisable takeaway, sharpening R13/"port gates cannot reach behavior" one step further:* a brief written from a closure tool's raw "blocks N rows" column, taken at face value, describes upstream's call graph — not this port's reachability, and not even upstream's OWN module boundaries, which can move between when a plan is written and when the branch starts (here: `dialects/` splitting into `dialects/`+`generators/`). Always re-run `--brief`/`--curve` against the CURRENT tree before scoping a session, exactly as this branch's own instructions said to — the number that changes the plan is the real marginal, not the blocked-count headline.

**R27 — `src/generators/snowflake.js` lands, Snowflake's `generate()` path closes end to end, and the honest new oracle catches a real bug the harness-routed one structurally could not.** Landed 2026-09-08 on branch `p4-snowflake-generator`. Ports `sqlglot/generators/snowflake.py` (1,222 LOC) whole: 20 class-level settings, ~140 `TRANSFORMS` entries (in upstream order), and the ~38 `*_sql` method overrides, plus the ~19 SHARED cross-dialect generator helpers this file needed that did not exist anywhere in the port yet (`rename_func`, `array_append_sql`, `array_concat_sql`, `date_delta_sql`/`unit_to_var`/`ts_or_ds_add_cast`, `datestrtodate_sql`, `groupconcat_sql`, `if_sql`, `inline_array_sql`, `max_or_greatest`, `min_or_least`, `no_make_interval_sql`, `no_timestamp_sql`, `nth_value_from_sql`, `strposition_sql`, `timestampdiff_sql`, `timestamptrunc_sql`/`weekstart_unit_to_str`/`unit_to_str`, `timestrtotime_sql`, `var_map_sql` — all in `src/dialects/dialect.js`, matching upstream's own file, not `generator.js`), `Generator.prototype.format_time` (wired to the already-real `formatTime` in `time.js`), and `generator.js`'s `unsupported_args` decorator. Wiring required two base-file changes: `Dialect.generator_class` (base) was a hardcoded `null` placeholder since before P4 existed — wired to the real base `Generator`, which is what makes `Expr.prototype.sql()` (no dialect argument) work at all; and `registerDialect`'s py:304-309 JSON-path-pruning guard, which unconditionally threw `NotPorted` the instant ANY dialect set a `generator_class` (R24's live guard), is now real logic — `ALL_JSON_PATH_PARTS` is derived from `_gen/expr_meta.js` entries tagged `traits: ["JSONPathPart"]` (the same policy `generator.js`'s own `SUPPORTED_JSON_PATH_PARTS` getter already stated: never hand-list the ten classes, so an eleventh is never missed) rather than read from the still-unported `sqlglot/jsonpath.py` — a real, verified substitute, not a workaround, and `Generator.SUPPORTED_JSON_PATH_PARTS`'s getter now returns it instead of throwing.

*A branch landed mid-session and changed the scope, for the better — reacted to rather than reverted.* `origin/p4-transforms` (R26) merged into `main` while this branch was in progress, landing `src/transforms.js`'s `preprocess` + 3 functions. Of the six `TRANSFORMS` entries this file's own header originally listed as blocked (`exp.Array`, `exp.Create`, `exp.PercentileCont`, `exp.PercentileDisc`, `exp.Pivot`, `exp.Select`), checking each against the four newly-real functions found exactly one unblocked: `exp.Create` needs only `transforms.preprocess` wrapping this file's OWN `_flatten_structured_types_unless_iceberg` (which has no other unported dependency), so it is ported for real now. The other five stay blocked on named, specific gaps (`transforms.inherit_struct_field_names`, `transforms.add_within_group_for_percentiles`, `transforms.unqualify_columns`, `transforms.eliminate_window_clause` + `transforms.explode_projection_to_unnest` + `sqlglot/optimizer/scope.py` for `exp.Select`), each commented out at its upstream anchor rather than given an unreachable body, matching base `Generator`'s own still-unseeded-`TRANSFORMS` convention. `tools/lint_deny.mjs`'s `isPortedSite` (an `@ported-ranges` directive, same mechanism `dialects/dialect.js` already uses for its own excluded `to_json_path` branch) keeps the five still-excluded module-level helpers from being silently claimed as ported.

*The generate-oracle split, and why a second probe was necessary rather than optional.* `spike/p4/fuzz_generate_oracle.mjs`'s own header says why it constructs `new Generator({dialect: writeDialect, ...})` directly instead of `Dialect.generate`: "`Dialect.generator_class` is unset, and wiring it is a change to a P5-owned file" — true when written, false after this branch, and the probe was never updated to exploit it, so its **EXACT stayed at 62**, completely unmoved by this entire branch, exactly as expected: it BYPASSES per-dialect `generator_class` resolution by construction and only ever exercises the base `Generator`. Measuring the real claim ("Snowflake's actual production `generate()` path works") needed a new probe built the way R24 built `fuzz_dialect_parse.mjs` for the parse side: `spike/p5/fuzz_dialect_generate.mjs`, a dialect NAME resolved through the real registry, `Dialect.get_or_raise(name).generate(ast)`, diffed against `corpus/gen/<dialect>.jsonl`. Measured: **SNOWFLAKE 889/919 of reached rows exact (96.7%)**, up from 0 (the path did not exist before this branch); **DEFAULT 127/127 (100%)**, matching the honest base-oracle's own predicted-closed count exactly. The 30 residual MISMATCH + 8 ERROR were traced individually rather than waved through: 21 are `property_sql`'s pre-existing R21-documented "None=" fallback (`PROPERTY_TO_NAME` missing entries for exotic `DESCRIBE` qualifiers — the same mechanism `test/generator_dispatch.test.mjs` already asserts for `StorageHandlerProperty`), 2 are the unseeded base `AFTER_HAVING_MODIFIER_TRANSFORMS["qualify"]` entry, 1 is the unseeded base `TRANSFORMS[ConnectByRoot]`, 5 are `Union`/`Except`/`Intersect` (`set_operation`, explicitly out of R25's "basic SELECT" keystone scope), 2 are `exp.Put`/`exp.Get` (`get_put_sql`, one of the 19 corpus-invisible base-Generator methods §3.5 already named), and 1 needs the still-blocked `_qualify_unnested_columns` (an anonymous-`UNNEST` alias-naming interaction, confirmed by reading the AST oracle directly rather than guessed). None are Snowflake-generator defects; all are pre-existing, already-documented gaps this branch's new oracle was simply the first thing to actually exercise.

*The one real bug this round found, and how — not from hand-review, from the new oracle disagreeing with a hand-verified fix.* `unsupported_args` (`generator.js`) was first written as a decorator for a bound CLASS METHOD — `_func(expression)`, reading `this.dialect` — because that is the one call site this branch's own code needed (`approxquantile_sql`). But upstream's decorator is also applied directly as a `TRANSFORMS` value (`exp.Levenshtein: unsupported_args(...)(rename_func("EDITDISTANCE"))`), which `src/generator.js`'s dispatcher invokes as a plain 2-arg call, `handler(this, expression)` — no `this`-binding at all. `EDITDISTANCE(col1, col2)` threw `Cannot read properties of undefined (reading 'dialect')` under `fuzz_dialect_generate.mjs` (the real dispatch path) while every unit test stayed green, because no existing test called a TRANSFORMS-value-wrapped `unsupported_args` result through the real `sql()` dispatcher — the harness-adjacent gap this whole branch's step 7 was designed to close. Fixed by matching upstream's ACTUAL decorator shape exactly: `_func(generator, expression)`, two always-explicit parameters (never an implicit `self`), which is what lets Python's one decorator serve both a bound method and a plain function — JS has no such unification, so `approxquantile_sql`'s class body now routes through a same-shaped standalone `(self, expression)` function via a thin `this`-forwarding shim, rather than wrapping the bound method directly. `test/generator_dispatch.test.mjs` gained a replacement test for `SUPPORTED_JSON_PATH_PARTS` (the old one asserted the now-fixed throw).

*Generalisable takeaway:* a Python decorator that is applied to BOTH a class method and a bare multi-arg function relies on Python's uniform "first positional arg is whatever the caller supplies" calling convention — a distinction JS's `this`-binding vs plain-call split makes real. Porting the decorator by matching its one visible use, instead of its actual declared parameter shape, reproduces the SOURCE correctly but not the full CALLING CONTRACT; the mismatch is invisible to `node --test` (no test happened to invoke it through the other convention) and only surfaces by exercising the REAL per-dialect dispatch path end to end — which is the entire reason this branch built `fuzz_dialect_generate.mjs` rather than trusting the harness-routed `fuzz_generate_oracle.mjs` alone.

**R28 — `dialects/postgres.py` lands (`class Postgres(Dialect)`), and the clean result is itself evidence: the two fixes R20/R24 shipped for Snowflake generalise to a second dialect with zero new hazard code needed.** Landed 2026-09-08 on branch `p5-postgres-dialect`. `src/dialects/postgres.js` ports the 143-LOC settings class whole — 11 setting overrides, the 34-entry `TIME_MAPPING`, `DEFAULT_FUNCTIONS_COLUMN_NAMES`, and the nested `Tokenizer` subclass (36 added/overridden keywords, bit/hex/byte/unicode string delimiters, `$`-heredoc strings with `HEREDOC_TAG_IS_IDENTIFIER`/`HEREDOC_STRING_ALTERNATIVE`, `KEYWORDS.pop("/*+")` and `.pop("DIV")`) — plus `registerDialect(Dialects.POSTGRES, Postgres)`. Before this file existed, `Dialect.get_or_raise("postgres")` did not merely score worse than the test harness's synthetic stand-in the way pre-R24 Snowflake did — it **threw `PyValueError: Unknown dialect 'postgres'`**, because nothing had ever registered that name (`src/parsers/postgres.js`'s real `PostgresParser`, landed earlier, is reachable only through the harness's `dialectClassFor`, never through the production registry). Measured on `spike/p5/fuzz_dialect_parse.mjs`'s new POSTGRES row, the real `Dialect.get_or_raise(name).parse(sql)` entry point: **0/946 (registry lookup failure) -> 919/930 of reached rows exact (98.8%)**, now matching `fuzz_ast_coverage.mjs`'s harness-routed number (919/946) exactly rather than falling short of it. `Generator = PostgresGenerator` and `EXPRESSION_METADATA` are the same two deliberate omissions R24 named for Snowflake, for the same still-true reasons (`generators/postgres.js` and `typing/postgres.py` are unported). This branch was rebased onto R27 mid-flight (`src/generators/snowflake.js` landed on `main` while it was in progress, and changed `registerDialect`'s JSON-path guard from an unconditional `NotPorted` throw to real `ALL_JSON_PATH_PARTS` derivation, plus wired base `Dialect.generator_class` to the real base `Generator` instead of a hardcoded `null`) — reverified rather than assumed compatible: `registerDialect(Dialects.POSTGRES, Postgres)` still succeeds post-rebase because `Postgres` (unlike `Snowflake` after R27) declares no own `Generator`, and `Object.hasOwn(klass, "Generator")` false plus `Object.hasOwn(klass, "generator_class")` also false means `klass.generator_class` falls through to `base_generator` — `Dialect.generator_class`, now the real base `Generator` rather than `null`. That makes the JSON-path-pruning branch run for Postgres too (real logic now, not a throw), which is a strict improvement over the pre-R27 behavior this class was written against and needed no code change to benefit from.

*Why zero new hazard code was needed otherwise, checked rather than assumed:* R24's second finding was a registry-write hazard — `spike/p3/dialect_tokenizer.mjs`'s `dialectClassFor` used to synthesize a throwaway stand-in and call `registerDialect` unconditionally, silently downgrading any real dialect class already registered. The fix landed with R24 checks `Dialect.get(dialect)` first and returns the real class if the caller already imported one — a fix keyed on the dialect NAME, not hardcoded to `"snowflake"`. Read before writing any Postgres-specific version of that fix, confirmed it already covers this dialect for free: no `src/dialects/postgres.js`-specific change to `dialect_tokenizer.mjs` was needed. This is the useful negative result R13/R21's family predicts — the generalisable fix, once actually generalisable, stops producing new R-entries for the dialects that follow it.
**R29 — `dialects/duckdb.py` lands, closing the fourth §10 dialect's settings class (parser was already done, R22) behind the real `Dialect.get_or_raise("duckdb")` path instead of the P3 harness's harvested-settings stand-in; the shape worth naming is a METHOD OVERRIDE THAT IS ONLY PARTLY PORTED.** Landed 2026-09-08 on branch `p5-duckdb-dialect`. `src/dialects/duckdb.js` ports the 128-LOC `DuckDB(Dialect)` class whole — 14 setting overrides, the `DATE_PART_MAPPING` merge-then-pop (`{**base, "DAYOFWEEKISO": "ISODOW"}` then `.pop("WEEKDAY")`, same "mutate after the literal" shape as `snowflake.js`'s `KEYWORDS.pop("/*+")`, applied to a settings Map instead of a Tokenizer's), an 11-entry `INVERSE_TIME_MAPPING` override, `to_json_path`, and the nested `Tokenizer` subclass (30 added/replaced keywords, `e'...'`/`E'...'` byte strings with `\`-escapes, `$tag$...$tag$` heredocs whose tag is itself parsed as an identifier, `COMMANDS` minus `SHOW`) — plus `registerDialect(Dialects.DUCKDB, DuckDB)`, which per R20 mirrors the settings onto the prototype so `src/parsers/duckdb.js:523`'s `this.dialect.version` read (the `_versionLt` bracket-syntax gate) resolves against a real instance rather than `undefined`. `Generator = DuckDBGenerator` is the one deliberate omission, for the identical reason Snowflake's is: `generators/duckdb.js` is P4-deferred (R21) and `registerDialect` already throws `NotPorted` the moment any dialect sets `generator_class`, so the gap has a live guard rather than being silent. Measured on `spike/p5/fuzz_dialect_parse.mjs`: **duckdb 999/1017 (98.2%) on the real path vs 996/1014 (98.2%) on the harness-routed `fuzz_ast_coverage.mjs` path it replaces** — three MORE exact, three FEWER stub, mismatch (18) and error (9) counts identical — the same direction and the same-sized effect R24 measured for Snowflake, and for the identical reason: three corpus rows need the real `DuckDBTokenizer` subclass that the harness's harvested-settings base tokenizer doesn't have.

*R21's oracle discipline, applied a third time.* `spike/p5/gen_duckdb_dialect_ref.py` + `fuzz_duckdb_dialect.mjs`: **287/287 checks green**, covering all 105 resolved settings (21 differ from the base), the nested tokenizer's declared and `__init_subclass__`-derived values, and `to_json_path`.

*The new shape: `to_json_path` is a method override that is only PARTLY ported, and a probe built like `can_quote`'s would either fail on realistic input or silently pass on faked output.* Snowflake's one method override (`can_quote`) is fully real — every input it can receive gets a real answer, so `fuzz_snowflake_dialect.mjs` compares every case value-for-value. DuckDB's `to_json_path` is different in kind, not just content: py:58's two fast-path checks (`path_text.startswith("/")`, `"[#" in path_text`) are real ported logic, but the fallthrough — `super().to_json_path(path)` for every other JSON-pointer-shaped string, e.g. `"$.a.b"` or `"a.b.c"` — is upstream's real `sqlglot.jsonpath.parse_json_path`, which the base `Dialect.to_json_path` in this port correctly renders as `NotPorted` (`sqlglot/jsonpath.py` is P4, same gap `Snowflake`'s missing `JSONPathTokenizer` announces). Naively comparing CPython's parsed `JSONPath(...)` output against the port's thrown error would either read as a spurious `FAIL` (masking a fully-expected, already-announced gap) or, worse, tempt writing a probe that only exercises the fast-path inputs and never notices the fallback exists at all. `gen_duckdb_dialect_ref.py` dumps both kinds of case from the same 10-row table; `fuzz_duckdb_dialect.mjs` recognizes CPython's real-parse cases by their `JSONPath(...)`-shaped repr and asserts they throw `NotPorted` rather than comparing values, logging each as a `GAP` note the way `EXPRESSION_METADATA`'s count-only gap already is — 3 of the 10 cases take that branch. *Generalisable takeaway:* before writing a differential probe for ANY dialect method override, check whether the override is a genuine fast-path/fallback split rather than a uniform real implementation — `grep 'super\.' src/dialects/*.js` finds the candidates, and each one needs its oracle to classify cases by which branch they hit, not just compare every case the same way.

**R30 — `dialects/{hive,spark2,spark,databricks}.py` land as `src/dialects/{hive,spark2,spark,databricks}.js`, closing the settings-class side of the four-link chain whose PARSER side (`src/parsers/{hive,spark2,spark,databricks}.js`) had been ported since R15/R16; the hazard worth naming is specific to chains 3+ levels deep and did not exist in any single-file dialect this project had ported before — Ben's corrected priority (§1) made this the FIRST chain-shaped `Dialect`-settings port, so it is also the first time this trap could have fired.** Landed 2026-09-09 on branch `p5-databricks-dialect`. Ports all four files whole — Hive's 8 setting overrides plus its `Tokenizer` (backtick identifiers, `ADD JAR`-style commands, numeric-literal suffixes) and 30-entry `TIME_MAPPING`; Spark2's 3 overrides plus a `Tokenizer` that extends `Hive.Tokenizer`; Spark's 4 overrides plus a `Tokenizer` that extends `Spark2.Tokenizer`, including a nested-comprehension `RAW_STRINGS` derivation (`[(prefix+q,q) for q in Spark2.Tokenizer.QUOTES for prefix in ("r","R")]`, outer loop over quote chars, inner over case) reproduced with `.flatMap`; and Databricks's 2 overrides plus a `Tokenizer` that extends `Spark.Tokenizer` — four separate `registerDialect` calls, one per file, matching how the parser chain registers each link.

*The hazard: a Python nested class that ISN'T redeclared at some link resolves, via ordinary attribute-lookup MRO walk, to whichever ANCESTOR last declared it — not necessarily the immediate parent.* Every dialect ported before this one was a single file with no intermediate links, so `class Tokenizer(SomeBase.Tokenizer)` always meant exactly what it looked like. In a 4-deep chain that stops being true the moment ONE link skips redeclaring a nested class: `databricks.py`'s (unported, jsonpath.py-gated) `class JSONPathTokenizer(Spark.JSONPathTokenizer)` does NOT extend some Spark-specific class — neither `Spark` nor `Spark2` redeclares `JSONPathTokenizer`, so Python's own attribute lookup walks `Spark -> Spark2 -> Hive` and finds it on `Hive`, the one link that DOES declare it (py:82). Writing `class DatabricksJSONPathTokenizer extends Spark.JSONPathTokenizer` in JS — the naive "extend the immediate parent's same-named nested class" pattern that worked for every prior single-hop case — would be silently wrong the instant that field resolves to `undefined` on `Spark` and either throws or (worse) synthesizes an empty subclass of nothing. This port avoids the trap only because `JSONPathTokenizer` stays unported chain-wide (§ Hive/Postgres/Snowflake precedent, `sqlglot/jsonpath.py` is P4) — the `Tokenizer` chain itself happens NOT to hit it, because Hive, Spark2, Spark, and Databricks each independently DO redeclare their own `Tokenizer`, so every extension in this port is a direct, unambiguous own-property reference (verified by reading each upstream file's actual class body rather than assuming the pattern from the file name, which is what caught an earlier draft of this same session's own analysis getting Spark2's `Tokenizer` wrong — see the process note below). *Generalisable takeaway:* in any dialect chain 3+ links deep, do not assume `class X(Parent.NestedName)` means "extends the immediate parent's own nested class" — read the actual upstream source for whether `Parent` itself redeclares `NestedName`; if it doesn't, the JS extension target is whichever ancestor last declared it, found by walking the MRO by hand, not by chaining `.NestedName` off the nearest name in the class hierarchy.

*Process note, for what it's worth beyond this one port:* mid-session, before writing any code, this exact question was worked through by eye against a partial (truncated) read of `spark2.py` and got it backwards — concluding Spark2 declares no own `Tokenizer` and that Spark's `Tokenizer(Spark2.Tokenizer)` therefore skips to `Hive.Tokenizer`. Re-reading the FULL file (the truncation had cut off exactly the `class Tokenizer(Hive.Tokenizer): ...` block) corrected this before any file was written, and the corrected, verified reading is what landed. Recorded because the two failure directions are symmetric and equally live: assume a class DOES redeclare a nested name when it doesn't (the Databricks/`JSONPathTokenizer` case above) or assume it DOESN'T when it does (this session's own near-miss on `Tokenizer`) — both are "read the source, not the shape" failures, and a truncated tool read is exactly the kind of partial evidence that produces the second one.

Also caught and fixed before commit: this session's own `hive.js` draft, when first written, put the literal Python-source control bytes for `INITCAP_DEFAULT_DELIMITER_CHARS` (`\x0b\x1c\x1d\x1e\x1f`, from `hive.py`'s `" \t\n\r\f"`) directly into the committed file instead of the `\u00XX`-escaped JS spelling PORT_PLAN.md §4.2 already mandates for exactly this situation — a live re-occurrence of §4.2's four-branches-bit-us-before pattern, this time via a *literal Python source string containing raw control characters* rather than a hand-typed separator byte. `file src/dialects/hive.js` immediately flagged the corrupted file as `data` instead of text, which is precisely the tripwire §4.2 built for; fixed by locating the exact byte offsets and replacing them with the escape-sequence text before commit. §4.2's existing lint (`tools/lint_control_bytes.mjs`, once wired into `spike/run_all.sh`) is the durable fix; this entry exists because the fifth-ever occurrence of this exact mistake, mid-session, on a file being freshly authored rather than copy-pasted, confirms it recurs by DEFAULT rather than by carelessness whenever an upstream literal itself contains a raw control byte — worth a standing reminder for any future dialect whose settings embed one (search upstream `.py` sources for `\x0` literals before transcribing a string setting verbatim).

Measured on `spike/p5/fuzz_dialect_parse.mjs`'s new HIVE/SPARK2/SPARK/DATABRICKS rows, the real `Dialect.get_or_raise(name).parse(sql)` entry point, against `spike/p3/fuzz_ast_coverage.mjs`'s pre-existing harness-routed numbers for the same four dialects (unchanged by this branch, since the parser side was already real): **hive 489/491 reached exact (99.6%) vs harness 489/502 (2 mismatch, 11 stub, 0 error both) — identical; spark2 41/41 (100%) vs harness 41/41 — identical; spark 667/671 (99.4%) vs harness 667/680 (4 mismatch, 6 stub, 3 error both) — identical; databricks 319/321 (99.4%) vs harness 319/336 (2 mismatch, 11 stub, 4 error both) — identical.** Unlike R24/R28/R29's "marginally above" or "measurably below" results, every count here is EXACTLY equal to the harness-routed baseline — the strongest possible confirmation that the ported `Tokenizer` chain and settings reproduce what the harness's harvested-attribute stand-in was already approximating, with zero behavioral drift in either direction. `Generator = {Hive,Spark2,Spark,Databricks}Generator` and each link's own `EXPRESSION_METADATA`/`COERCES_TO` are the same deliberate omissions R24/R28/R29 already named, for the same still-true reasons (`generators/{hive,spark2,spark,databricks}.js` and the four `typing/*.py`/`optimizer/annotate_types.py` modules are unported) — confirmed, not merely assumed, that omitting `Generator` across all four links still lets `registerDialect` fall through to the real base `Generator` with zero new hazard code, the same negative result R28's own note already established generalises.

*Probe status, verified rather than inherited.* `spike/run_all.sh` has exactly the three pre-existing red probes R17/R21/R25 recorded — "P3: check_command_warning strings", "P3: AST oracle coverage (honest)", "P4: generate oracle (honest)" — re-confirmed on this branch and untouched by it. The new "P5: DuckDB dialect vs CPython" probe is green and wired into `run_all.sh`, alongside `fuzz_dialect_parse.mjs`'s new DUCKDB row. 90/90 unit tests, unmoved (this file needed none — no new expression shapes, no parser change).

**R32 — `generators/duckdb.py` lands as `src/generators/duckdb.js`, but DELIBERATELY SCOPED — the file is 4,788 LOC (~4x every other single-file dialect generator ported so far: Snowflake 1,222, Postgres 599, the whole 4-file Databricks chain combined 1,208), so this session ports a marginal-value SUBSET rather than the whole file, the first generator step in this project not to be a full-file port; the hazard worth naming is that a scattered cherry-pick surfaces base-`Generator` gaps at a much finer grain than a whole-file port does, because each newly-wired `TRANSFORMS` lambda is its own small dependency probe rather than one big one.** Landed 2026-09-09 on branch `p4-duckdb-generator`. `node tools/closure_generator.mjs --brief dialect:duckdb` and `spike/p4/fuzz_generate_oracle.mjs --dialect duckdb --verbose`'s "top stubs" breakdown (49 stub rows over the 225 rows this project's generate-oracle harness can currently reach for DuckDB) drove the cut, not raw upstream line order: of the ~110 `TRANSFORMS` entries and 140 `*_sql` overrides this file does NOT carry, the ones actually reachable in that 49-row sample needed either (a) a shared cross-dialect helper `dialects/dialect.js` already ports unmodified, or (b) a DuckDB-local module-level helper function under ~40 lines with no further unported dependency. `src/generators/duckdb.js` ports: the 33 scalar class settings (`PARAMETER_TOKEN` through `SET_ASSIGNMENT_REQUIRES_VARIABLE_KEYWORD`) that change how ALREADY-PORTED base `Generator` methods render DuckDB SQL even with zero DuckDB-specific `TRANSFORMS`/overrides; **70 of 147 `TRANSFORMS` entries** (Tier 1, ~35: wired verbatim to `rename_func`/`array_append_sql`/`array_concat_sql`/`datestrtodate_sql`/`groupconcat_sql`/`no_make_interval_sql`/`approx_count_distinct_sql`/`no_time_sql`/`no_timestamp_sql`/`timestrtotime_sql` — zero new logic; Tier 2, ~25: a boolean-coercion group (`Boolnot`/`Booland`/`Boolor`/`Xor`/`LogicalOr`/`LogicalAnd`/`BoolxorAgg`, all chaining through a ported `_cast_to_boolean`/`_round_arg`), `ArraySort`/`ArrayContains`, `AnyValue`, the `UnixMicros`/`UnixMillis`/`UnixSeconds` implicit-datetime-cast group, and a batch of self-contained one-line lambdas); the 4-entry `SUPPORTED_JSON_PATH_PARTS`, 19-entry `TYPE_MAPPING`, and 77-entry `RESERVED_KEYWORDS` (all mechanical, real upstream data, zero code-logic risk); plus `registerDialect(Dialects.DUCKDB, DuckDB)`'s `static Generator = DuckDBGenerator` wiring in `src/dialects/duckdb.js`, replacing that file's own stale header claim (written before `SnowflakeGenerator` landed, R27) that `registerDialect` "throws `NotPorted` the moment any dialect sets `generator_class`" — untrue since R27 changed that guard to a defensive `try`/`catch`, so there was never a live block here, just an unclosed TODO. Explicitly NOT ported and named rather than silently skipped: all 140 `*_sql` method overrides (`create_sql`/`createable_sql` — the CREATE-statement properties subsystem is its own multi-session port, not scoped to one dialect's generator; `window_sql`/`tablesample_sql`/`pad_sql`/`parsejson_sql` — DuckDB overrides that `super()` into base methods this pass does not touch; `round_sql`/`ceil_floor` — chained through the unported `_scale_rounding_sql`), the ~77 remaining `TRANSFORMS` entries (the largest excluded cluster, 9 entries, is `_date_delta_to_binary_interval_op` — a local wrapper around the already-ported shared `date_delta_to_binary_interval_op` adding DuckDB's own nanosecond/float-interval handling, judged too large for this pass), `TYPE_PARAM_SETTINGS`, `PROPERTIES_LOCATION`, `UNWRAPPED_INTERVAL_VALUES`, and every `t.ClassVar[exp.Expr] = exp.maybe_parse(...)` SQL-string template (ZIPF/NORMAL/SEEDED_RANDOM/MAPCAT/bitmap-construct-agg) — each reachable only from an excluded method.

**R33 — a base-`Generator` helper can be SILENTLY a no-op, with no exception at all, when it is missing an optional callback its caller never supplies — a hazard class none of R1–R32's "throws `NotPorted`" or "wrong string" categories cover, because there is no error to grep for and no wrong-looking output to eyeball.** Found while porting `generators/postgres.py` (PORT_PLAN.md P4): `Postgres.INDEX_OFFSET = 1` (1-based subscripts) makes `col[0]` (a DuckDB/0-based literal) transpile to `col[1]` in the real corpus, and `Generator.prototype.bracket_offset_expressions` (`src/generator.js`, ported during the Databricks-chain step) is the method responsible — but it calls `exp.applyIndexOffset(this_, expressions, offset, { dialect })` with NO `annotateTypes`/`simplify` options, even though `applyIndexOffset` (`src/expressions/builders.js`) needs BOTH: it must first learn the subscript literal's type via `annotateTypes` (to confirm it's actually an integer) and then fold `literal + offset` via `simplify`, and silently `return`s the expressions UNCHANGED if either callback is absent rather than throwing. Both `annotate_types` and `optimizer.simplify.simplify` are P6+ and unported, so every dialect with a non-zero `INDEX_OFFSET` (Postgres and DuckDB today; `dialects/dialect.js`'s own `Dialect.INDEX_OFFSET` default is `0`, which is why Snowflake never surfaced this — the formula degenerates to a no-op offset regardless) has ALWAYS silently mis-rendered an integer-literal bracket subscript, since the Databricks-chain step landed `bracket_offset_expressions` — this is not a regression this PR introduced, it is the first PR whose dialect's `INDEX_OFFSET` and corpus both exercise the branch, and the honest differential fuzzer (not a human read) is what surfaced it: one measured `spike/p5/fuzz_dialect_generate.mjs` POSTGRES mismatch (`col[0]` where CPython wants `col[1]`), diagnosed down to this exact line. Deliberately NOT fixed here: porting `annotate_types`/`simplify` for real is far outside a single dialect-generator PR's scope, and the SQL manuscript is too easy to get quietly wrong (a wider fix that silently succeeds on the ONE test case is worse than the current honest-if-silent gap, which this entry converts into a documented, greppable one). The general lesson for future sessions: when reviewing or porting a method that calls an already-ported shared helper, check whether that helper takes OPTIONAL dependency-injection-style callbacks (`annotateTypes`, `simplify`, the `hash`/`isExpr` callbacks CONTRACTS.md §8 already tracks) before assuming "it's already ported" means "it works" — a helper can be faithfully transliterated and still go quiet, rather than loud, on the exact inputs that need the callback its caller forgot to pass.

**R34 — the base `FUNCTIONS` table's `"LIKE"` entry was a literal `/* TODO build_like */` comment rather than dead code with no reader, and its own comment misdirected to the wrong file — the honest generate-oracle's `MISMATCH must be 0` invariant was the thing that caught it, not a per-PR review.** Found 2026-09-10 while gathering real numbers for a documentation refresh: `node spike/p4/fuzz_generate_oracle.mjs`, run for its own sake rather than because a PR claimed a number, showed `MISMATCH 1` — a value the tool's own header states must always be zero, and one that had not been explicitly checked in the two most-recent merged PRs (`p4-duckdb-generator` #44, `p4-postgres-generator` #45), both of which verified the real per-dialect `fuzz_dialect_generate.mjs` oracle but not this base one; it stayed hidden because `spike/run_all.sh`'s "P4: generate oracle (honest)" probe was ALREADY red for unrelated STUB/reachability reasons (R25), so a newly-nonzero `MISMATCH` inside an already-red probe produces no visible change in the pass/fail count a per-PR check compares against. The single failing row, `LIKE(x, 'z')`, rendered back as the literal input instead of CPython's `'z' LIKE x` — traced to `src/parser.js`'s base `FUNCTIONS` table, where line ~446 read `// py:434  ["LIKE", /* TODO build_like */]  — \`build_like\` lives in dialects/dialect.py`, an unwired entry whose own comment sent the reader to the wrong function: upstream has TWO same-named-but-different `build_like`s — `dialects/dialect.py`'s curried `build_like(expr_type, not_like=False)` factory (a different call site, already ported), and `parser.py:63`'s own non-curried `build_like(args) -> exp.Escape | exp.Like`, the one the base `FUNCTIONS` table actually needs, with a load-bearing ARGUMENT SWAP (`exp.Like(this=seq_get(args, 1), expression=seq_get(args, 0))` — the pattern is `args[1]`, the subject is `args[0]`, so `LIKE(x, 'z')` means `'z' LIKE x`, not `x LIKE 'z'`). Fixed by adding the correct local (non-curried) `build_like` to `src/parser.js` and wiring `["LIKE", build_like]` in place of the TODO. Measured: honest oracle **EXACT 90 -> 91, MISMATCH 1 -> 0**; full corpus **EXACT 10,876 -> 10,879, MISMATCH 2,930 -> 2,927**; DEFAULT real-parse-path (`fuzz_dialect_parse.mjs`) **1,890 -> 1,891**; 90/90 unit tests unmoved. *Generalisable takeaway:* a TODO comment naming a specific file as the fix's location is a claim, same shape as R22's "a stub's stated blocker is a claim with a timestamp" — this one was simply wrong from the start (upstream has two functions with the same name, and the comment picked the wrong one), and nothing short of actually reading both candidate definitions would have caught it. The other generalisable point is procedural: verifying a PR's OWN claimed numbers is not the same as re-running every probe's full invariant set after each merge — `MISMATCH must be 0` needs to be checked explicitly, every round, independent of which specific oracle a given PR's brief happened to name.

*A scattered cherry-pick keeps finding base-`Generator` gaps one lambda at a time, unlike R31's whole-file port which found them in one batch.* Six base `Generator` methods went from `NotPorted` stub to real body this round, each found by wiring a single DuckDB `TRANSFORMS` entry and running it, not by reading ahead: `anonymous_sql` (`UnixSeconds`'s lambda builds a `self.func("EPOCH", ...)` SQL STRING and feeds it back into `exp.cast`, which round-trips it through the DEFAULT parser — `EPOCH` is not a recognized builtin there, so it comes back as `exp.Anonymous`, and rendering that needs this method); `nullsafeeq_sql`/`nullsafeneq_sql` (`EqualNull`'s lambda builds an `exp.NullSafeEQ` node and renders it); `case_sql`/`if_sql` (`ArrayContains`'s `check_null` branch builds an `exp.If` node, which base `if_sql` renders by delegating to `case_sql`); `arraysize_sql` (the same `check_null` branch also builds an `exp.ArraySize` node). All six are small (7-19 lines), self-contained, and were verified individually against direct AST construction (bypassing this port's own, separately-gapped parser) before being trusted — `test/generator_dispatch.test.mjs`'s burndown count updates 79 -> 85 accordingly, the same "stated, not implied" discipline R13 established. Two small shared additions followed the same path: `dialects/dialect.js` gained `no_time_sql` (DuckDB's `Time` `TRANSFORMS` entry wires directly to it, matching `no_timestamp_sql`'s existing shape, py:1747) and `no_comment_column_constraint_sql` (`CommentColumnConstraint`); `expressions/core.js` gained the `paren` builder (`Paren(this=maybe_parse(expression, copy))`, upstream's `expressions/core.py:2980`), needed by the boolean-coercion group and not exported anywhere in the port before now.

*A newly-found hazard: `generator.js` cannot import `dialects/dialect.js`, and that blocks a real `TRANSFORMS` entry rather than a hypothetical one.* `YearOfWeek`/`YearOfWeekIso` build an `exp.Extract` node and render it, which dispatches to base `extract_sql` (py:3649) — itself small (17 lines) and safe to port (its one risky branch, `weekstart_name`, is unreachable while `EXTRACT_ALLOWS_QUOTES` stays at its base-default `true`, which DuckDB does not override) — except `extract_sql` needs `dialects/dialect.js`'s `map_date_part`, and `dialect.js` already imports FROM `generator.js` (`import { ALL_JSON_PATH_PARTS, Generator, unsupported_args } from "../generator.js"`, `dialect.js:49`) to build its `Dialect` class. Importing the reverse direction would cycle. Python has no such constraint (`extract_sql` does `import sqlglot.dialects.dialect` INSIDE the method body, a lazy import specifically to break this exact cycle) — the JS port cannot reproduce that trick directly, because `extract_sql` is a SYNCHRONOUS method on a fully synchronous call chain (`self.sql()` throughout) and JS dynamic `import()` returns a Promise. Both `TRANSFORMS` entries stay commented out at their upstream anchor rather than given a broken body; closing this needs either a `registerGenerator`-style late-binding indirection (the same shape `registerDialect`/`registerParser` already use for the analogous problem elsewhere in this port) or moving `map_date_part` somewhere both files can reach without cycling — a design decision for a follow-on session, not a one-line fix.

*A pre-existing bug this round's new `anonymous_sql` made VISIBLE rather than caused.* `spike/p4/fuzz_generate_oracle.mjs --dialect duckdb`'s closure cross-check reclassified one row from `STUB anonymous_sql` to `MISMATCH`: `LIKE(x, 'z')` (function-call syntax) parses in this port to `exp.Anonymous(this="LIKE", ...)`, which — now that `anonymous_sql` is real — renders back out faithfully as `LIKE(x, 'z')`, but CPython's own oracle wants `'z' LIKE x` (a real `exp.Like` node). That is a **parser** AST-shape defect — `[(default)]` dialect, nothing to do with DuckDB — that was invisible before this round only because it hid behind a `NotPorted` throw (both counted identically as "not exact" in the oracle's totals: `over-claimed` stayed at 67 before and after). Left unfixed, named for a follow-on parser session rather than pulled into this generator-scoped one.

Measured on the newly-extended `spike/p5/fuzz_dialect_generate.mjs`'s new DUCKDB row, the real `Dialect.get_or_raise("duckdb").generate(ast)` entry point (no such row existed before this session — DuckDB had no `generator_class` of its own, R29's own documented omission): **0 -> 207/336 of reached rows exact (61.6%; 587 total: 207 exact, 129 mismatch, 224 stub, 27 error)**. Every sampled mismatch traced (`--verbose`, 8 shown) falls inside the explicitly-excluded surface named above — `SortArray`/`Decode`/`Struct`/`lambda_sql`/`_unix_to_time_sql`/`_arrow_json_extract_sql`/`Select`'s `connect_by_to_recursive_cte` preprocessing, `describe_sql`'s pre-existing `"None="` fallback (the same R21-documented gap R27's own writeup already traced for Snowflake) — none from this session's own Tier 1/Tier 2 work. `spike/p4/fuzz_generate_oracle.mjs --dialect duckdb` (which BYPASSES `generator_class` resolution by construction, per its own header, and so cannot see DuckDB-specific work at all) stayed at its pre-existing **84/225 EXACT**, as expected; STUB moved 49 -> 48 and MISMATCH 0 -> 1, both from the `anonymous_sql` reclassification above, not from DuckDB-specific code.

*Generalisable takeaway, for the next dialect this large.* A whole-file port (Snowflake, the Databricks chain) finds its base-`Generator` gaps in one batch, verified together. A deliberately-scoped cherry-pick finds them ONE AT A TIME, each behind a single freshly-wired `TRANSFORMS` entry — cheaper per session, but only safe if each entry is smoke-tested against a directly-constructed AST (bypassing this port's own separately-gapped parser) before being trusted, which is what caught `is_type` vs `isType` (wrong camelCase), `e.unit` vs `e.args.unit` (the R21-documented implicit-`__str__` trap, recurring here because `TimeUnit`'s `unit` GETTER — unlike `DateTrunc`'s — is not installed in this port yet), and the `anonymous_sql`/round-trip-through-the-parser interaction, all before they reached a committed test.

*A fourth red probe, briefly — and a `@ported-ranges` self-inflicted lesson.* `tools/lint_deny.mjs` initially failed with 42 `missing-marker` errors spanning nearly the whole 4,788-line upstream file, not just this session's ported ranges: the `@ported-ranges` directive this file's header declares had a bare `50` token (meant as "module-level line 50, `TIMEZONE_PATTERN`") where the checker's regex requires an `a-b` pair, which silently broke the WHOLE directive's parse (`isPortedSite`'s `rangeRe` needs `(?:\s+\d+-\d+)+` immediately after the filename, and one malformed token fails the entire match) — with zero ranges parsed, `isPortedSite` fell through to its "no seeded skeleton, treat the whole file as ported" default (the exact failure mode PORT_PLAN.md's own memory note for this project already named), demanding a marker at every deny-listed site in the file, including ~4,700 lines this session never touched. Fixed by writing `50-50` instead of `50`; down to one real failure, `TsOrDsDiff`'s `` `'${e.args.unit || "DAY"}'` `` — a genuine, not spurious, instance of the R21 implicit-`__str__` trap (`e.args.unit` is a `Var` Expr when present; upstream's `f"'{e.args.get('unit') or 'DAY'}'"` calls `str(Expr)` -> `.sql()`, but a bare JS template literal calls `.toString()`, which this port's `Expr` overrides to the verbose repr, `Var(this=DAY)`) — fixed to `e.args.unit.sql()` with a `// deny:implicit_str` marker, and reverified directly: `DATE_DIFF('Var(this=DAY)', ...)` -> `DATE_DIFF('DAY', ...)`. `tools/lint_deny.mjs` is clean; `spike/p5/fuzz_duckdb_dialect.mjs` (R29) also needed a one-line update, from a hardcoded `GAP generator_class: ... port has null` note to a real `check(...)` now that `DuckDB.generator_class` resolves to `DuckDBGenerator` — 287/287 -> 288/288. `spike/run_all.sh` re-verified clean end to end after both fixes: exactly the three pre-existing red probes (P3 `check_command_warning strings`, P3 `AST oracle coverage`, P4 `generate oracle`, all `(honest)`-labeled and unrelated to this session), 90/90 unit tests, LINT and every P5 dialect probe green.

**R31 — `generators/{hive,spark2,spark,databricks}.py` land as `src/generators/{hive,spark2,spark,databricks}.js`, the fourth and final file of each dialect (parser: R15/R16; dialect-settings: R30; generator: this), closing the four-link chain end to end; the hazard worth naming is that this chain's OWN generator overrides reach a wide swath of base `Generator` methods no earlier single-file dialect (Snowflake/Postgres/DuckDB) had ever exercised, via `super()` chains this deep never having existed in the port before.** Landed 2026-09-09 on branch `p4-databricks-generator`. Ports all four files whole, in dependency order (Hive, then Spark2 extends it, then Spark extends that, then Databricks extends that) — `HiveGenerator`'s ~62 `TRANSFORMS` entries, ~20 method overrides, and its own `SAFE_JSON_PATH_KEY_RE`/`format_time` overrides; `Spark2Generator`'s build-then-`.delete()` `TRANSFORMS` (five Hive-specific entries removed so `ArraySort`/`ILike`/`Left`/`MonthsBetween`/`Right` fall through to base `Generator`'s real methods or the generic `exp.Func` fallback, since Spark natively supports what Hive had to rewrite) plus its `struct_sql`/`altercolumn_sql`/`renamecolumn_sql` overrides; `SparkGenerator`'s own build-then-delete `TRANSFORMS` (`AnyValue`/`DateDiff`/`With` removed the same way) plus `ignorenulls_sql`/`bracket_sql`/`ifblock_sql`/etc.; and `DatabricksGenerator`'s `TRANSFORMS` (`RegexpLike`/`TryCast` removed) plus `create_sql`/`columndef_sql`/`jsonpath_sql`/`uniform_sql`/`_regr_sql`/`clusterproperty_sql`. `src/transforms.js` grows by ten more `transforms.py` functions these four files import and R26's earlier landing did not carry: `unnest_generate_series`, `unnest_to_explode` (its own ~150-line port, LATERAL VIEW EXPLODE / CROSS JOIN UNNEST rewriting), `remove_within_group_for_percentiles`, `unqualify_columns`, `unqualify_pivot_fields`, `remove_unique_constraints`, `ctas_with_tmp_tables_to_create_tmp_view`, `move_schema_columns_to_partitioned_by`, `move_partitioned_by_to_schema_columns`, `any_to_exists`, `inherit_struct_field_names`. `src/dialects/dialect.js` grows by eighteen more shared cross-dialect generator helpers these four files need and no earlier dialect had: `bracket_to_element_at_sql`, `approx_count_distinct_sql`, `no_ilike_sql`, `no_recursive_cte_sql`, `no_trycast_sql`, `property_sql`, `struct_extract_sql`, `time_format`, `left_to_substring_sql`, `right_to_substring_sql`, `trim_sql`, `regexp_extract_sql`, `regexp_replace_sql`, `is_parse_json`, `arg_max_or_min_no_count`, the `DATETIME_ADD` runtime tuple, `date_delta_to_binary_interval_op`, and `sequence_sql` — the same "shared cross-dialect GENERATOR helpers" section R27 opened for Snowflake, extended in upstream source order.

*The base-`Generator` reach, and why this chain hits it where Snowflake/Postgres/DuckDB didn't.* Single-file dialects call `super().X()` at most one level up, into a base `Generator` that is either real or a `NotPorted` stub with a known, narrow blast radius. A 4-deep chain multiplies this: `Spark2Generator.struct_sql` calls `Generator.struct_sql(self, expression)` **by name**, deliberately skipping `HiveGenerator`'s own override; `Spark2Generator.altercolumn_sql`/`renamecolumn_sql` and `SparkGenerator.ignorenulls_sql` use Python's explicit `super(HiveGenerator, self).X(...)` / `generator.Generator.X(self, ...)` grandparent-skip syntax for the same reason — and every one of those skip targets turned out to still be an unported base stub, because nothing before this session had a 2-hop-deep `super()` chain to expose them. Separately, `transforms.unnest_to_explode`'s `LATERAL VIEW EXPLODE` output (reached by every `HiveGenerator`-descended `exp.Select` TRANSFORMS entry) needed `lambda_sql`/`lateral_sql`/`lateral_op`, and `HiveGenerator`'s own overrides needed `columndef_sql`/`schema_sql`/`schema_columns_sql`/`version_sql`/`exists_sql`/`trim_sql`/`_jsonpathkey_sql`/`_embed_ignore_nulls`/`json_path_part`/`ignorenulls_sql`/`national_sql`/`naked_property` for their `super()` fallback branches to actually work rather than throw the instant a row took that branch. One more shape, distinct from a missing method: `cluster_sql`/`distribute_sql`/`sort_sql` (Hive/Spark's DML-only `CLUSTER BY`/`DISTRIBUTE BY`/`SORT BY`) were TODO-commented in `AFTER_HAVING_MODIFIER_TRANSFORMS` since the P4 keystone group (R25) with no caller until real HIVE corpus rows reached `query_modifiers` here — the same "declared, correctly empty, read by nothing" shape R19 named, one release later, for a *table entry* rather than a whole table. In total, 29 `*_sql`-suffixed base methods and six non-`*_sql` helpers went from `NotPorted` stub to real body this round (`test/generator_dispatch.test.mjs`'s burndown count, 50 -> 79).

*A real, pre-existing bug in shared code, found by this session's own corpus rows and fixed in the same file it lives in.* `dialects/dialect.js`'s `timestampdiff_sql` (landed with R27 for Snowflake) read `expression.unit` — a bare property access that does not exist on the JS `Expr` class; every other caller of this exact upstream property in this file already uses `expression.args.unit` (`unit_to_var`, `weekstart_unit_to_str`), but this one caller was never fixed to match. The bug silently dropped the unit argument (`TIMESTAMPDIFF(MONTH, foo, bar)` rendered as `TIMESTAMPDIFF(foo, bar)`) rather than throwing, so it survived R27's own verification undetected — `SnowflakeGenerator` also wires `exp.DatetimeDiff` through this same function, but apparently no Snowflake corpus row's real generate-path exercised it with a flagged mismatch. `SparkGenerator.TRANSFORMS[exp.TimestampDiff]` is the first TRANSFORMS entry in this port to route a real, verified corpus row through it end to end, and the real `Dialect.get_or_raise("spark").generate()` path caught it immediately: fixing the one-character typo (`.unit` -> `.args.unit`) took SPARK from 301/303 to 303/303 exact with zero other changes, and left SNOWFLAKE's own count unmoved (its corpus apparently never reached the buggy branch at all) — confirmed, not merely assumed, by re-running both rows before and after the fix.

*Two more real defects, both found by running the existing verification suite rather than by additional manual review — `tools/lint_deny.mjs` and `spike/p4/fuzz_generator_base.mjs`, neither of which is new to this round.* The deny-list lint (§4.6) flagged seven newly-ported sites needing acknowledgment markers; six were correct translations that just needed the marker comment (`hive.js`'s `increment.mul(...)`, `spark2.js`'s `args.pivots[0]` plain-array index, two `json_path_part` sites in `generator.js`, `hive.js`'s `_unix_to_time_sql` and `usingproperty_sql` — the latter verified against `parsers/hive.py:245`'s `_parse_using_property`, confirming `kind` is always a plain token-text string, never an Expr, so the bare template literal is correct). The seventh was a REAL bug the lint caught before merge: `SparkGenerator._dateadd_sql`'s `` `CAST(${this_} AS ${return_type})` `` interpolated `return_type` (an `exp.DataType`, from `expression.returnType`) directly, producing `CAST(DATE_ADD(MONTH, 1, a) AS DataType(this=DType.DATE))` instead of `CAST(... AS DATE)` — the exact "implicit `__str__`" trap R21 already named once, here on a site this session wrote rather than inherited; fixed to `self.sql(return_type)` and reproduced/re-verified directly (`TS_OR_DS_ADD(a, 1, MONTH)` now renders `CAST(DATE_ADD(MONTH, 1, a) AS DATE)`). Separately, `spike/p4/fuzz_generator_base.mjs` — the P4 blocking-step probe from R21 — hard-coded `AFTER_HAVING_MODIFIER_TRANSFORMS` as one of two tables required to stay at size 0 "at this step"; populating `cluster`/`distribute`/`sort` for real (this round's own `AFTER_HAVING_MODIFIER_TRANSFORMS` fix, described above) correctly moved that table out of "fully deferred," and the probe's own assertion needed updating to a THIRD state — partially seeded, keys asserted by name (`cluster`, `distribute`, `sort` real; `windows`/`qualify` still not) — rather than either "must be exactly 0" or "must match upstream's full 5-key set," the same "burndown count updates deliberately" precedent `test/generator_dispatch.test.mjs` already follows (R13's demand, made executable).

Measured on the newly-extended `spike/p5/fuzz_dialect_generate.mjs`'s HIVE/SPARK2/SPARK/DATABRICKS rows, the real `Dialect.get_or_raise(name).generate(ast)` entry point, against each dialect's own corpus row set (AST lookups pooled across `hive`/`spark2`/`spark`/`spark, version=3.0.0`/`spark, version=4.0.0`/`databricks`/`_default` — `corpus/ast/spark2.jsonl` alone has **zero** content overlap with `corpus/gen/spark2.jsonl`'s 94 `ast_ref`s, since `atom_id` is a content-addressed hash shared across the whole corpus rather than scoped per dialect file, and pooling is what SNOWFLAKE's/DEFAULT's own single-stem rows don't need but this chain does): **hive EXACT 108/222 (48.6%, base-`Generator` fallback) -> 248/248 (100%, 0 mismatch, 112 stub, 8 error) — Δ+140; spark2 14/35 (40.0%) -> 36/36 (100%, 0 mismatch, 16 stub, 1 error) — Δ+22; spark 138/250 (55.2%) -> 303/303 (100%, 0 mismatch, 159 stub, 24 error) — Δ+165; databricks 85/143 (59.4%) -> 150/156 (96.2%, 6 mismatch, 128 stub, 19 error) — Δ+65.** Total EXACT delta across all four: **+392**. The "before" figures are `new Generator({dialect})` constructed directly against each dialect's resolved settings (what `Dialect.generate` actually fell through to while `generator_class` was unset, R30's own documented gap) — not a synthetic baseline. DATABRICKS's remaining 6 mismatches are all one root cause, confirmed by inspection and left unfixed as out of this session's scope: `CAST(x AS INTERVAL <unit> TO <unit>)` parses its `DataType.this` to an `exp.Interval` EXPRESSION instance instead of `DType.INTERVAL` plus an `IntervalSpan` in `expressions` — a base **parser** type-parsing defect, not a generator one, and unrelated to the four files this session ports; `datatype_sql`'s `type_sql = type_value` branch then implicitly stringifies the wrong object via template-literal interpolation, producing a debug repr (`"Interval(\n  unit=IntervalSpan(...))"`) instead of `INTERVAL HOUR TO SECOND`. Every other reachable gap (`create_sql` — DDL, explicitly out of scope per R25's own header; `Union`/`Except`/`Intersect` — `set_operation` unported; `JSONPathKey`/`JSONPathRoot` bare dispatch — `sqlglot/jsonpath.py`'s `JSON_PATH_PART_TRANSFORMS` stays intentionally unseeded, per `generator.js`'s own "Fill from `src/jsonpath.js`'s own table when it lands, never by hand" comment) is a pre-existing, announced base-`Generator` gap, confirmed rather than assumed to be out of scope, and none of it silently masked as this round's own work.

**R35 — `delete_sql`/`drop_sql`/`update_sql`/`insert_sql`/`alter_sql`/`create_sql` land in `src/generator.js`, closing the DML/DDL gap that blocked `contrib/gatewaySqlMetadata.js`'s `standardizedSql` field for every statement type except SELECT/MERGE; the hazard worth naming is that landing a shared base-`Generator` method can turn a dialect's pre-existing, already-scoped-out gap from an invisible STUB into a newly-visible MISMATCH, and the aggregate mismatch COUNT staying flat is not a safe signal that nothing changed — only a full atom_id diff is.** Landed 2026-09-13 on branch `generator-dml-ddl-keystone`. Ports six base methods whole (`delete_sql` py:1809, `drop_sql` py:1829, `update_sql` py:2691 plus its `_update_from_joins_sql` helper py:2659, `insert_sql` py:2288, `alter_sql` py:4333 plus its `add_column_sql` dependency py:4384) and, once the gateway smoke test showed a real `INSERT INTO t VALUES (...)` still failed downstream of a correct `insert_sql`, `values_sql` (py:2709) too — R18's "don't silently skip a stub dependency a ported method calls into", caught by testing the actual downstream consumer rather than stopping at the oracle. `createable_sql`/`create_sql` (py:1316/1319) are DELIBERATELY SCOPED to CREATE statements with no `properties` arg: every properties-location branch in `create_sql` (`POST_SCHEMA`/`POST_WITH`/`POST_ALIAS`/`POST_INDEX`/`POST_CREATE`/`POST_EXPRESSION`) is gated on `properties_locs.get(...)`, which is an empty `Map` when `expression.args.properties` is unset, so the full properties subsystem (`locate_properties`/`properties`/`properties_sql`/`root_properties`/`with_properties`) stays `NotPorted` and is never reached for a plain `CREATE TABLE t (a INT)` / `CREATE VIEW v AS ...` — confirmed with `node tools/closure_generator.mjs --add create_sql,createable_sql,locate_properties,properties,properties_sql,root_properties,with_properties` before committing to the cut (+1 marginal on the base oracle, the properties subsystem itself gated on 197 further distinct units per `--brief create_sql`, matching R21/R23/R25/R26's standing lesson to check the real marginal before scoping). A CREATE that DOES carry properties throws `NotPorted` rather than silently dropping them. Measured: `spike/p4/fuzz_generate_oracle.mjs` **EXACT 91 -> 92, MISMATCH 0 -> 0, ERROR 0 -> 0** (R34's own invariant, checked explicitly); `spike/p5/fuzz_dialect_generate.mjs` EXACT **DEFAULT 173->174, SNOWFLAKE 1036->1066, HIVE 260->274, SPARK2 38->38, SPARK 332->357, DATABRICKS 156->173, DUCKDB 220->235, POSTGRES 233->266** — every dialect strictly improved or held flat, zero new mismatches confirmed by diffing the full atom_id set (not just the aggregate count) for all eight populations, **except DuckDB, which gained exactly one new mismatch** (`8bd107b105e0e8be`, a `GENERATE_SERIES`-in-`FROM`-clause row that renders `GENERATE_SERIES(...)` where CPython wants the context-sensitive `RANGE(...)` rewrite `generate_series_sql`'s dynamic dispatch provides upstream). That row was previously invisible as a STUB (blocked earlier on the still-unported `values_sql`, since the row's SQL also contains a `(VALUES (42))` clause) and only became reachable — and therefore able to fail on ITS OWN, unrelated, already-known R32-scoped gap — once `values_sql` shipped; it is not a defect this PR introduced, and R32's own header already named DuckDB's generator as "a SCOPED subset... not full TRANSFORMS/*_sql coverage." *Generalisable takeaway:* when a PR ports a method several dialects share, "mismatch count identical before/after" is not sufficient evidence of "no effect on dialect X" — a shared dependency landing can let a previously-STUB row travel further down dialect X's OWN already-scoped-out code path and surface a DIFFERENT, pre-existing gap as a mismatch instead. The only reliable check is a full atom_id diff of the mismatch (and, ideally, error) sets per dialect, not just the aggregate counts — this round's single-line DuckDB delta would have read as a silent regression under a count-only check, and was only distinguishable as "expected, pre-existing, unrelated" by tracing the specific row.

**R36 — `setitem_sql`/`set_sql`/`command_sql`/`use_sql` land in `src/generator.js`, closing the SET/USE gap in `contrib/gatewaySqlMetadata.js`'s `standardizedSql` field for the session-state statements airbrx-gateway's own tracking cares about most; the hazard worth naming is that "session state" is not one AST shape but three unrelated ones (`exp.Set`, `exp.Use`, `exp.Alter`-wrapped `exp.AlterSession`), each gated by its own independent generator method, so a task brief's own smoke-test list can include a case that LOOKS covered by this round's methods and isn't.** Landed 2026-09-13 on branch `generator-set-use-command`. Ports four base methods whole (`setitem_sql` py:2958, `set_sql` py:2971, `command_sql` py:4169, `use_sql` py:4635) — all four are pure functions of already-ported `this.sql`/`this.expressions`/`expression.text`/a static class field (`SET_ASSIGNMENT_REQUIRES_VARIABLE_KEYWORD`, read via `this.constructor.X` per R20/R33's standing lesson, not bare `this.X`), with no dialect-hosted-state dependency, so all four run "for real" on a bare `new Generator()` (no resolved Dialect required), same class as R35's ten. The task brief's own smoke-test list included `ALTER SESSION SET QUERY_TAG = 'reporting'` on Snowflake alongside `SET .../USE ...`, on the (reasonable-looking, wrong) assumption that "session state" is one feature; it is not — Snowflake's `ALTER SESSION SET ...` parses to `exp.Alter` (kind `SESSION`) wrapping an `exp.AlterSession` action, dispatched to the SEPARATE, still-`NotPorted` `altersession_sql` (py:4379), unaffected by anything landed here or in R35's `alter_sql`. Confirmed directly: `extractSqlMetadata("alter session set QUERY_TAG='reporting'", {dialect:"snowflake"}).extractionError` is still set (`altersession_sql is not ported yet`) after this round, while the two real cases (`SET .../USE ...`) show `extractionError: null` and whitespace/case-normalized `standardizedSql` (`"set   x  =  1"` -> `"SET x = 1"`), which is what actually distinguishes "real regeneration happened" from "the fallback happened to echo the input back unchanged" for statements whose canonical form often equals their input. Measured: `spike/p4/fuzz_generate_oracle.mjs` **EXACT 92 -> 92, MISMATCH 0 -> 0, ERROR 0 -> 0** (unchanged — the 225-row reachable set this probe covers has no SET/USE atoms; R34's invariant re-checked and holds); `spike/p5/fuzz_dialect_generate.mjs` EXACT **DEFAULT 174->174 (no SET/USE atoms reached on this path either), SNOWFLAKE 1066->1090, HIVE 274->276, SPARK2 38->38, SPARK 357->364, DATABRICKS 173->185, DUCKDB 235->250, POSTGRES 266->276** — every dialect strictly improved or held flat, and a full atom_id diff of the mismatch AND error sets (not just aggregate counts, per R35's own standing lesson) confirms EVERY exact gain is a pure STUB->EXACT transition with zero new or resolved mismatches/errors in any of the eight populations. `test/generator_dispatch.test.mjs`'s burndown assertions updated deliberately (101 -> 105 bodied, 100 -> 104 running on the stand-in), per R13's demand. *Generalisable takeaway:* when a task brief names several "obviously related" statement forms as a single smoke-test group, verify each one lands on the SAME generator method rather than assuming shared surface vocabulary ("session state", "SET-like") implies a shared code path — `extractionError`/`NotPorted` on the specific case is the check, not "the SQL text round-tripped unchanged," which a raw-SQL fallback also produces for any statement whose canonical form matches its input.

**R37 — `create_sql`'s properties subsystem (`locate_properties`/`properties`/`properties_sql`/`root_properties`/`with_properties`, plus `columnconstraint_sql`/`datatypeparam_sql`) lands in `src/generator.js`, closing the `CREATE ... WITH (...)`/`USING ... LOCATION ...` gap R35 deliberately scoped out; the hazard worth naming is that porting a small, well-defined DISPATCH CLUSTER makes it USABLE only once its own fan-out is also filled — 64 `Generator.TRANSFORMS` entries the cluster newly routes to, and two independent pre-existing bugs the cluster's own new reachability surfaced (one in `create_sql` itself, one in `SnowflakeGenerator`), all found only by tracing a full atom_id diff rather than trusting aggregate counts.** Landed 2026-09-14 on branch `generator-create-properties`. Ports the 7 declared methods whole (`columnconstraint_sql` py:1203, `datatypeparam_sql` py:1693, `properties_sql` py:2046, `root_properties` py:2071, `properties` py:2076, `with_properties` py:2091, `locate_properties` py:2094, ~70 LOC total) — `create_sql`'s existing `properties_locs.get(...)` gates needed no code change to start working, since `locate_properties` now returns a real (possibly non-empty) `Map` instead of throwing. That alone was not enough for a real property-bearing CREATE to regenerate correctly: `locate_properties`/`properties_sql` route each property class through `Generator.TRANSFORMS` (or, failing that, the generic `property_sql`/`naked_property` fallback), and 64 of the ~114 classes in `PROPERTIES_LOCATION` were still bare `// TODO lambda` placeholders — the exact intersection of "in `PROPERTIES_LOCATION`" and "still a TODO lambda," computed by diffing the two class lists rather than guessed. Without them, e.g. `TemporaryProperty` (no dedicated `*_sql` method, no TRANSFORMS entry) fell through to `property_sql`'s `Unsupported property X` fallback and rendered `CREATE None= TABLE t (...)` for `CREATE TEMPORARY TABLE t (a INT)`, and `LocationProperty` (which IS in `PROPERTY_TO_NAME`) rendered the wrong `LOCATION='/x'` shape instead of `naked_property`'s `LOCATION '/x'`. All 64 are one-line ports of their CPython lambdas (a literal keyword, `self.sql(e, "this")`, `self.naked_property(e)`, or `self.expressions(...)`); `Generator.TRANSFORMS.size` moves 14 -> 78, `test/generator_dispatch.test.mjs`'s burndown assertions updated deliberately (105 -> 108 bodied *_sql methods, 104 -> 107 running on the stand-in, 431 -> 495 total dispatch entries), per R13's demand. A property class that reaches neither TRANSFORMS nor the generic fallback (i.e. still routes to its OWN dedicated, still-`NotPorted` `*_sql` method — `clusterproperty_sql`, DuckDB's own unported `PROPERTIES_LOCATION` override, ~30 more) still throws `NotPorted` rather than silently dropping — a long tail of individually-small, mostly rare/dialect-specific methods, not a structural gap in the dispatch cluster itself, deliberately left for ordinary port work. Two independent, pre-existing bugs were found (not introduced) by tracing the newly-reachable code paths, both fixed here since each was a one-line-class, high-confidence, oracle-traced fix directly in a call site this round touches: (1) `create_sql`'s six `properties_locs.get(loc)` truthiness checks used bare JS-array truthiness (`[]` is truthy in JS), where Python's `if properties_locs.get(loc):` treats an empty `list` as falsy — invisible until `SnowflakeGenerator.createable_sql`'s in-place `.splice()` of `CopyGrantsProperty` out of its `POST_SCHEMA` bucket (for materialized views, where COPY GRANTS renders before the column list, not after) left a present-but-empty array behind, producing a spurious extra space (`FOO COPY GRANTS (A, B)  AS SELECT...`, double space before AS); fixed with a local `locHas(loc)` helper checking `.length` at all six sites. (2) `generators/snowflake.js`'s `generatedasidentitycolumnconstraint_sql` (already real, not touched by this round's 7 methods) interpolated raw `${start_val}`/`${increment_val}` Expr args directly into a template literal instead of `this.sql(expression, "start"/"increment")` — an implicit-`__str__` site (see `static-analysis-misses-implicit-str` in project memory) that CPython's own automated `gen_implicit_str.py` census MISSED (verified: `corpus/deny/implicit_str.json` has no entry for `snowflake.py:895-898`), rendering `AUTOINCREMENT START Literal(this=10, is_string=False)` instead of `AUTOINCREMENT START 10` for any Snowflake `AUTOINCREMENT START n INCREMENT m` column. Both fixes are annotated in place rather than silently folded in. Measured: `spike/p4/fuzz_generate_oracle.mjs` **EXACT 92 -> 93, MISMATCH 0 -> 0, ERROR 0 -> 0** (R34's invariant, checked explicitly); `spike/p5/fuzz_dialect_generate.mjs` EXACT **DEFAULT 174->175, SNOWFLAKE 1090->1154 (mismatch 30->4), HIVE 276->298, SPARK2 38->38, SPARK 364->383, DATABRICKS 185->192, DUCKDB 250->258 (mismatch 130->137), POSTGRES 276->284 (mismatch 1->1, same atom)** — every dialect strictly improved or held flat on EXACT, and a full atom_id diff of the mismatch sets (not aggregate counts, per R35's own standing lesson) confirms: SNOWFLAKE's 26 fixes plus 4 unrelated pre-existing mismatches held over unchanged from baseline (zero newly-introduced); DUCKDB gained exactly 7 new mismatches, all traced to two pre-existing, unrelated gaps this round's `datatypeparam_sql`/`locate_properties` unblocked reachability into rather than caused — 2 rows where `DECIMAL(p, s)`-typed `BIT_OR`/`BIT_XOR` calls were previously blocked on `datatypeparam_sql` and now surface DuckDB's own `BITWISE_OR_AGG`-vs-`BIT_OR` function-name gap, 3 rows where `MAP(TEXT, DECIMAL(38, 0))` casts were likewise blocked and now surface a pre-existing `MAP`-literal-vs-`STRUCT` rendering gap, and 2 rows (`CREATE TABLE IF NOT EXISTS t (...) WITH (FORMAT=ICEBERG, PARTITIONED_BY=(...))`) where CPython's own oracle records `unsupported_messages: ["Unsupported property fileformatproperty", ...]` — DuckDB overrides `PROPERTIES_LOCATION` to mark every base property `UNSUPPORTED` by default and re-enable five (`LikeProperty`/`TemporaryProperty`/`ReturnsProperty`/`SequenceProperties`/`IcebergProperty`), an override `src/generators/duckdb.js`'s own header already names as an unported "follow-on stub queue" item, unrelated to this round and out of scope (DuckDB is a deliberately-scoped R32 subset, not one of `contrib/`'s three target dialects). `node --test` 175/175 green throughout (one pre-existing assertion updated for the new bodied-method counts, no new tests needed — the properties cluster is exercised end-to-end via the dialect-generate oracle, not a hand-written unit test). `bash spike/run_all.sh` reduced from 5 red probes to the 3 known pre-existing ones (confirmed identically red on `main` baseline in this environment, unrelated to this diff: `P3: check_command_warning strings`/`P3: AST oracle coverage (honest)` need `pip install -e .` on the pinned `/tmp/sqlglot-ref` checkout, which this environment lacks; `P4: generate oracle (honest)` is RED on the same pre-existing 66-row closure-tool-over-claim, unaffected by this PR's EXACT/MISMATCH/ERROR numbers) — the other 2 (`LINT: deny-lists routed through _py`, `P4: base Generator (settings/prims/gen)`) were this PR's own responsibility to fix, not pre-existing: the LINT failure was a stale `deny:implicit_str` marker this PR itself introduced pointing at a manifest entry that does not exist (rephrased to avoid the recognized marker syntax, since the site was never in the automated census to begin with); the P4 failure was `spike/p4/fuzz_generator_base.mjs`'s own hardcoded 14-entry "seeded TRANSFORMS subset" assertion, stale the moment `TRANSFORMS.size` grew to 78 (updated to the new 78-entry list, same fix pattern as `test/generator_dispatch.test.mjs`'s counts). Downstream smoke test: `extractSqlMetadata("CREATE TABLE t (a INT) USING DELTA LOCATION '/x'", {dialect:"databricks"}).standardizedSql` now returns real regenerated SQL (`extractionError: null`) instead of falling back to the raw input; same for Postgres `WITH (...)` and Databricks `CREATE TEMPORARY TABLE`/`CREATE OR REPLACE VIEW`. *Generalisable takeaway:* a "port these N declaratively-small dispatch methods" task can be code-complete while still being USELESS for its stated purpose if the dispatch cluster's own fan-out (here, `Generator.TRANSFORMS`) was scoped out of the original size estimate — the fix is not to guess at the fan-out's size but to compute the exact intersection of "what this cluster can now route to" against "what is still a placeholder," and verify with real property-bearing input across every target dialect before declaring done; and unblocking one previously-gated code path can simultaneously fix real bugs (surfacing them for the first time as a traceable MISMATCH instead of a STUB) and surface unrelated ones (equally traceable, equally not-yours-to-fix-here) — a full atom_id diff is what tells them apart, an aggregate count never does.
**R38 — Redshift lands the FULL stack (Parser + Dialect settings + Generator) in one round, the first dialect after the Databricks chain to do so in a single pass rather than split across P3/P4/P5 sessions; getting the parser's own AST-coverage MISMATCH to zero required finding and fixing two pre-existing base-`Parser` bugs and one base-`Generator` bug that predate this branch and affect every dialect, not just Redshift — the hazard worth naming is that a small, single-link `extends Postgres` dialect can still surface base-layer defects that a decade of prior dialect work never exercised, because it is the first caller to combine two specific, previously-uncombined features (`DISTKEY`/`SORTKEY` properties + a parser-set EXCLUDE list that is empty-but-not-null).** Landed 2026-09-14. `src/parsers/redshift.js` (`RedshiftParser extends PostgresParser`, 160-LOC upstream), `src/dialects/redshift.js` (`Redshift extends Postgres` — the dialect-SETTINGS class also extends its Postgres counterpart, same shape `databricks.js` uses for `Databricks(Spark)`, not `extends Dialect`), and `src/generators/redshift.js` (`RedshiftGenerator extends PostgresGenerator`, 384-LOC upstream) all land together, closing `dialects/redshift.js`'s own previously-deferred `generator_class` gap in the same round it was written. Three base-layer defects found while chasing the parser's own residual MISMATCH/ERROR count to zero (none Redshift-specific — Postgres's own numbers improved too): (1) `_parse_wrapped_id_vars(optional)` called `_parse_wrapped_csv(fn, optional)` positionally, landing `optional` in the `sep` parameter slot instead of `TokenType.COMMA` — upstream passes it as `optional=optional`, a KEYWORD arg skipping `sep` entirely (PORT_PLAN.md R18's defect class) — which silently broke comma-splitting for EVERY wrapped id-var list in the port (`ALTER ... SORTKEY (c1, c2)` parsed as a single item then choked on the comma); (2) `_parse_distkey` called the csv-returning `_parse_wrapped_id_vars()` instead of upstream's single-item `_parse_wrapped(self._parse_id_var)`, producing a one-element ARRAY for `DistKeyProperty.this` instead of a bare `Identifier`; (3) `Generator.prototype.select_sql`'s two `exclude` truthiness checks used bare JS truthiness on a value that upstream's own `_parse_projections` sets to `[]` (not `None`) when there is no EXCLUDE clause — a Python empty list is falsy, a JS empty array is not, so EVERY select on a parser whose `_parse_projections` takes this shape (Redshift's own override, ported this round) rendered a spurious `EXCLUDE ()` suffix; fixed with `pyTruthy()`, the project's standing idiom for this exact divergence class. None of the three is Redshift's own grammar — DISTKEY/SORTKEY and the general SELECT-EXCLUDE machinery are BASE `Parser`/`Generator` code shared by every dialect — but Redshift's own test corpus was the first to combine "a wrapped id-var list with more than one element" with a dialect whose `_parse_projections` populates `exclude` unconditionally, so no prior dialect's corpus had exercised either path. Also completed, because Redshift's own `TRANSFORMS[exp.Select]` pipeline and settings needed them: three previously-unported `transforms.js` functions with no unported dependency once `optimizer/scope.js`'s Tier A `findAllInScope` was confirmed sufficient (`unnest_generate_date_array_using_recursive_cte` py:72, `unqualify_unnest` py:278 — its `find_all_in_scope` call is real, not a stub, contrary to what a cursory read of the surrounding upstream code might suggest — and `eliminate_window_clause` py:1001, already inside `transforms.js`'s declared `741-1084` `@ported-ranges` window from a prior round, so no range edit was needed for it, only for the first two); and four module-level `dialects/dialect.js` helpers (`no_tablesample_sql`, `concat_to_dpipe_sql`, `concat_ws_to_dpipe_sql`, `generatedasidentitycolumnconstraint_sql`), the same "port the shared helper the dialect generator needs" pattern every prior generator round used. Measured: `spike/p3/fuzz_ast_coverage.mjs --dialect redshift` (parser layer) **EXACT 121 -> 262, MISMATCH 170 -> 0, ERROR 23 -> 4** (the 4 residual errors are all `_parse_copy`'s pre-existing base-`Parser` `NotPorted` stub on `COPY ... FROM 's3://...'` — a gap upstream's own `RedshiftParser` does not override either, so it is out of this round's scope, not a new gap); `spike/p5/fuzz_dialect_parse.mjs`'s new REDSHIFT row (dialect-settings layer, the real `Dialect.get_or_raise("redshift").parse()` path) matches the harness exactly, **262/262 of reached rows exact (100%)**, with STUB inflated to 60 by `SUPPORTS_IMPLICIT_UNNEST = true` (faithful to upstream) routing every FROM-clause SELECT through the still-`NotPorted` `_implicit_unnests_to_explicit` — a real, pre-existing, honestly-counted limitation, not something this round invented or masked, and deliberately NOT exercised by `fuzz_dialect_parse.mjs`'s own unguarded `SELECT * FROM t WHERE x = 1` assertion loop for exactly that reason (see the import-site comment); `spike/p5/fuzz_dialect_generate.mjs`'s new REDSHIFT row (generator layer) is a brand-new population, **100/100 of reached rows exact (100%), 0 mismatch, 0 error, 74 stub**. Full corpus `fuzz_ast_coverage.mjs` (no dialect filter) rose across the same window this round touched shared files in — reported here for completeness rather than as an isolated signal, since two other sessions were concurrently landing TSQL/BigQuery parser work in the SAME working tree; no MISMATCH/ERROR count went up for any dialect checked individually (Postgres's own numbers: EXACT 919 -> 921, MISMATCH 11 -> 10, ERROR 7 -> 6, from the same two base-`Parser` fixes). *Generalisable takeaway, technical:* a small `extends Postgres`-style dialect is not risk-free just because most of its surface is inherited — it can still be the first corpus to combine two previously-uncombined base-layer code paths and surface a defect that every prior dialect's own test corpus missed by construction, so "port a one-link dialect" sessions should still budget time to chase residual MISMATCH/ERROR counts to zero rather than accepting "close enough, it's just a thin wrapper." *Generalisable takeaway, operational:* `git stash`/`git stash pop` is UNSAFE in a working tree shared by concurrent sessions (as this branch's task explicitly was, by design, with parallel BigQuery/TSQL parser work) — a stash operates on the WHOLE tree, so a second `git stash` issued while another session's edits have landed on shared files (here: `src/parser.js`, `spike/p3/dialect_tokenizer.mjs`, both touched by the concurrent TSQL session) can pop back a stale, pre-their-edit version of those files, silently discarding the other session's live work the moment `git stash pop` "succeeds" (or, worse, discarding it silently on a from-scratch re-stash even when the first pop merely reports a conflict and leaves the stash queued) — recovered here by diffing `git show stash@{0}:<file>` against `git diff HEAD` per file to identify exactly which files were untouched by the other session (safe to overwrite wholesale from the stash) versus which had new concurrent content (require re-applying only this round's own specific hunks by hand). Prefer per-file `git diff`/targeted `Edit` calls over `git stash` for any verification step that needs a "before" comparison in a shared working tree.
**R39 — `parsers/tsql.py` is ported as `src/parsers/tsql.js`, the fifth standalone dialect parser (Snowflake R15/R16, Postgres R19, DuckDB R22, this one), and the defect it surfaced is one layer BELOW those four: base `_parse_alter_table_alter`'s `exp.AlterColumn` construction put `exists` second instead of last in every one of its six branches, an arg-ORDER bug invisible to every existing counter because it neither throws nor changes `toS`'s repr, only `astDump`'s field-insertion order.** Landed 2026-09-14 on branch `p3-tsql-parser`, taking SQL Server / Azure SQL Database, Ben's #3 dialect priority (after BigQuery, Redshift, both landing concurrently on sibling branches) for this phase of dialect onboarding. `sqlglot/parsers/tsql.py` (819 LOC) is ported whole into `src/parsers/tsql.js` — 11 module-level builder functions, 15 class-table overrides, 15 methods, nothing stubbed or skipped — extending the base `Parser` directly the way `snowflake.js`/`postgres.js`/`duckdb.js` do rather than chaining. Two of the eleven builders (`_build_formatted_time` for `DATENAME`, `_build_format` for `FORMAT`) do `from sqlglot.dialects.tsql import TSQL` INSIDE their own body upstream — a REAL circular-import workaround (`dialects/tsql.py` imports `TSQLParser` FROM this file, so this file cannot import `TSQL` back at module level even once that file exists) — and `src/dialects/tsql.js` (the `TSQL(Dialect)` settings class carrying `TIME_MAPPING`/`FORMAT_TIME_MAPPING`) is P5/deliberately deferred to a follow-up session per this project's established phased-dialect split, so both builders route through a file-local `_tsqlSettings()` stub that throws `NotPorted` rather than duplicating the two mapping tables here as a second source of truth (same reasoning as `annotate_types` in postgres.js). `src/parser.js` gains one export (`build_coalesce`, needed by `ISNULL`, R18's `build_trim` shape again) and one field-order fix: `_parse_alter_table_alter`'s `opts = {this: column, exists: exists || null}` spread-first pattern put `exists` in POSITION 2 in every branch, where upstream's own kwarg order (`parser.py:9104-9151`) puts `exists=exists or None` LAST, after the branch-specific fields — found because TSQL's own `_parse_alter_table_alter` override calls `super()` then `.set("allow_null", ...)` on the result for `ALTER TABLE a ALTER COLUMN b INTEGER NOT NULL`, and the appended field only lands in the CORRECT position relative to `exists` if `exists` was already last. Fixing the six-branch spread order (not just adding the TSQL-specific field) is a strict, dialect-agnostic correctness fix — reflected in the full-corpus numbers below, not just TSQL's. Measured: **tsql EXACT 347 -> 915 of 973 (94.1% of REACHED rows), MISMATCH 303 -> 20, ERROR 323 -> 13, STUB 0 -> 25** (the harness-routed `fuzz_ast_coverage.mjs --dialect tsql` path, matching the Snowflake/Postgres/DuckDB parser-only-PR precedent of measuring harness-routed first; the real `Dialect.get_or_raise("tsql")` path is next session's, once `dialects/tsql.js` is real); **full corpus EXACT 10,879 -> 11,467, MISMATCH 2,927 -> 2,624, ERROR 1,178 -> 868, STUB 494 -> 519** (the `_parse_alter_table_alter` fix's own cross-dialect effect, plus TSQL's marginal). The 20 residual MISMATCH and 13 residual ERROR rows were triaged individually and confirmed to route through constructs `parsers/tsql.py` itself never touches — `STRING_AGG`'s missing `on_overflow` field (base `_parse_string_agg`), `OPENJSON`'s unwired `FUNCTION_PARSERS` entry (base, commented out pending the still-`NotPorted` `_parse_open_json`), `OUTER`/`CROSS APPLY`'s missing `sample` field (base `_parse_table`/`_parse_lateral`), `PERIOD FOR SYSTEM_TIME` temporal-table columns, `COPY INTO`, and XML `.value()` method calls — all genuine, pre-existing, out-of-file gaps, not regressions this port introduced; left named here rather than fixed, matching R33's "an honest gap documented beats a wide fix risked" standard. 175/175 unit tests throughout. Dialect settings (`src/dialects/tsql.js`) and Generator (`src/generators/tsql.js`) are deliberately deferred to follow-up sessions, matching the phased split every prior standalone dialect used. *Process note, not a code finding:* this branch's worktree briefly picked up another concurrent session's uncommitted edits to shared files (`src/parser.js`, `spike/p3/dialect_tokenizer.mjs`) during a `git stash`-based before/after measurement, traced to (per independent investigation) a stray absolute-path file write rather than a confirmed systemic bug — but `refs/stash` is in fact a single ref shared across every `git worktree` of one repo, so a push/pop from one worktree can plausibly race a concurrent one in this multi-session environment regardless of root cause here. Recovered by resetting the two shared files to `HEAD` and reapplying only this session's own two verified edits, rather than trusting whatever `git stash pop` returned. *Generalisable takeaway:* prefer `git show <ref>:<path>` / `git diff <ref> -- <path>` for before/after measurement in this environment over `git stash`, and treat any unexpected file in `git status` after a stash operation as a reason to stop and diff against `HEAD` before trusting it, not as a reason to `git stash pop` again.

**R40 — `parsers/bigquery.py` is ported whole into `src/parsers/bigquery.js`, the first dialect in Ben's corrected 2026-09-14 priority order (BigQuery, Redshift, TSQL, then the rest) and PARSER ONLY, matching the exact phased pattern R15/R16 (Snowflake), R18 (Databricks chain) and R22 (DuckDB) each set; the hazard worth naming is a raw NUL byte landing in freshly-authored source through a copy artifact, not through any upstream Python content — a live reproduction of the control-byte failure mode PORT_PLAN.md §4.2 warns about, caught only because `grep` silently stopped matching a line it should have found.** Landed on branch `p3-bigquery-parser`. `sqlglot/parsers/bigquery.py` (800 LOC: ~189 lines of module-level builders/constants, then `class BigQueryParser(parser.Parser)` at ~611 LOC) is ported whole — 22 module-level builder functions (`_build_contains_substring`, `_build_date`, `_normalize_bare_week`, `build_date_diff`, `_build_datetime`, `_build_extract_json_with_default_path`, `_build_format_time`, `_build_json_strip_nulls`, `_build_levenshtein`, `_build_parse_date`/`_build_parse_timestamp`/`_build_parse_datetime`, `_build_regexp_extract`, `_build_time`, `_build_timestamp`, `_build_to_hex`, `_split_qualified_name`), all 9 class-table overrides (`ID_VAR_TOKENS`/`ALIAS_TOKENS`/`TABLE_ALIAS_TOKENS`/`COMMENT_TABLE_ALIAS_TOKENS`/`UPDATE_ALIAS_TOKENS` each `{*parent, GRANT} - {ASC, DESC}`, `FUNCTIONS` FILTERing out SEARCH before adding ~70 entries, `FUNCTION_PARSERS` FILTERing out TRIM before adding 14, `NO_PAREN_FUNCTIONS`/`NESTED_TYPE_TOKENS`/`PROPERTY_PARSERS`/`CONSTRAINT_PARSERS`/`STATEMENT_PARSERS` EXTENDing, `RANGE_PARSERS` FILTERing out OVERLAPS, `DASHED_TABLE_PART_FOLLOW_TOKENS`/`BRACKET_OFFSETS` REPLACing outright since neither has a base-`Parser` counterpart), and all 17 methods (`_parse_for_in`, `_parse_table_part`, `_parse_table_parts`, `_parse_column`, `_parse_cluster_property`, `_parse_property`, `_parse_json_object`, `_parse_bracket`, `_parse_unnest`, `_parse_make_interval`, `_parse_ml`, `_parse_generate`, `_parse_translate`, `_parse_forecast`, `_parse_features_at_time`, `_parse_vector_search`, `_parse_export_data`, `_parse_column_ops`) — nothing stubbed or skipped, extending the base `Parser` directly the way `snowflake.js`/`postgres.js`/`duckdb.js` do rather than chaining. `src/dialects/dialect.js` grows by one shared builder BigQuery's `DATE_ADD`/`DATE_SUB`/`DATETIME_ADD`/`DATETIME_SUB`/`TIME_ADD`/`TIME_SUB`/`TIMESTAMP_ADD`/`TIMESTAMP_SUB` all need — `build_date_delta_with_interval` (py:1676, the INTERVAL-literal sibling of the already-ported unit-token `build_date_delta`) — with its `@ported-ranges` gap closed accordingly; every other dependency (`binary_from_function`, `build_formatted_time`, `build_extract_json_with_path`, `setDiff`/`setUnion`, `pyReGroups`/`PyReError` for the Python-regex group-count in `_build_regexp_extract`, `toIdentifier`/`alias_`/`Dot.build`, and all ~40 BigQuery-specific `exp.*` classes) already existed, unusually cleanly for a dialect this size — the only genuinely new base-layer surface this port needed was the one `dialect.js` function. Two methods stay effectively unreachable behind pre-existing, unrelated base-`Parser` gaps rather than anything this PR introduces: `_parse_json_object`'s `super()._parse_json_object()` call reaches `parser.js:5485`'s `NotPorted` stub (the SAME stub R16's residual already tracks for Snowflake), and its dispatch table entry doesn't even exist — `parser.js:1598-1599` deliberately OMITS `FUNCTION_PARSERS["JSON_OBJECT"/"JSON_OBJECTAGG"]` with a comment naming the same stub, so `JSON_OBJECT(...)` falls through to anonymous-function parsing regardless of which dialect reads it; and `_parse_unnest`'s `annotate_types` call is a file-local `NotPorted` stub, the same P6+-deferred shape `postgres.js` already documents for its own `annotate_types` import. The raw-NUL-byte incident: `_DOMAIN_DOT`, upstream's `"\0"` placeholder sentinel (never serialized, purely a runtime string-masking value inside `_split_qualified_name`), landed in the committed source as an ACTUAL 0x00 byte rather than the JS escape sequence `"\u0000"` — confirmed with `python3 -c "open(...).read().count(b'\\x00')"` (1 hit) after a `grep _DOMAIN_DOT` on the freshly-written file returned nothing despite the string visibly being there, exactly PORT_PLAN.md §4.2's predicted failure mode ("`file`/`grep` silently classify the whole source file as binary the moment one lands... without any error"). Fixed with a byte-level Python replace before any lint or test ran; `tools/lint_control_bytes.mjs` (part of `spike/run_all.sh`) would have caught it had it not been caught by inspection first. Measured (BigQuery-only, `node spike/p3/fuzz_ast_coverage.mjs --dialect bigquery`, harness-routed per R15/R16/R18/R22's own precedent — `parserClassFor` in `spike/p3/dialect_tokenizer.mjs` gains one new map entry, `["bigquery", BigQueryParser]`; the real `Dialect.get_or_raise("bigquery")` path via `fuzz_dialect_parse.mjs` stays out of scope until the `BigQuery(Dialect)` settings class lands in a follow-up session, matching every prior parser-only PR): **EXACT 693 -> 1,088 of 1,486 (98.0% of REACHED rows, up from 47.7%), MISMATCH 685 -> 11, ERROR 75 -> 11, STUB 33 -> 376.** Full corpus: **EXACT 10,879 -> 11,274, MISMATCH 2,927 -> 2,253, ERROR 1,178 -> 1,114, STUB 494 -> 837** — the four full-corpus deltas are IDENTICAL to the four BigQuery-only deltas (+395/-674/-64/+343 respectively), confirming by construction that zero other dialects were touched, without needing a separate per-dialect atom_id diff. The remaining 11 MISMATCH are all `JSON_OBJECT(...)` (the base-parser gap above); the remaining 11 ERROR are 7x `GapFill` (base `_parse_gap_fill` `NotPorted`, already documented at `parser.js:1596`) plus 3x `CAST(... AS ... FORMAT ... AT TIME ZONE ...)` and 1x named-window `ROWS BETWEEN` framing — both base `Parser` grammar BigQuery's own file never touches, confirmed pre-existing by their absence from any override in `bigquery.py`. 175/175 unit tests held throughout. Dialect settings (`dialects/bigquery.py`, ~211 LOC) and the Generator are deliberately deferred to follow-up sessions, the same phased split R30/R31 (Databricks) and every other completed dialect used.

**R41 — `sqlglot/schema.py` (813 LOC, column-type-aware table/column metadata) is ported whole into `src/schema.js`, the first genuinely greenfield module since the P0 spikes: nothing else in the port references it, nothing was blocked on it, and — unlike every dialect PR above — there was no existing corpus/atoms.jsonl row to reach it with, so its own scenario-driven oracle (24 scenarios / 71 checks) is the first differential signal it has ever had; building it immediately found a real, pre-existing bug in the shared `helper.js` dependency this file was told already "worked."** Landed on branch `p6-schema`, Ben's ask to start building toward the (still unported) optimizer's `qualify`/`annotate_types` passes. `Schema` (the abstract interface, py:24), `AbstractMappingSchema` (trie-based shared lookup logic, py:153), and `MappingSchema` (the real concrete implementation, py:287) land alongside all six module-level functions (`normalize_name`, `ensure_schema`, `ensure_column_mapping`, `flatten_schema`, `nested_get`, `nested_set`) — nothing stubbed, `grep -c NotPorted src/schema.js` is 0. Two upstream shapes collapse cleanly rather than needing a literal port: `@trait` (py:12) is upstream's own `def trait(f): return f` — a pure identity decorator with no runtime effect, confirmed by reading `sqlglot/helper.py` directly rather than assumed — so there is nothing to port for it; and `class MappingSchema(AbstractMappingSchema, Schema)`'s real multiple inheritance collapses to single JS inheritance (`MappingSchema extends AbstractMappingSchema`, since `MappingSchema` overrides every member `Schema` itself defines) plus a `Schema[Symbol.hasInstance]` override so `ensureSchema`'s `schema instanceof Schema` check still recognizes a `MappingSchema` — the identical idiom `expressions/core.js`'s `defineExpr` already established for faking Python's MRO (`bases`/`traits` arrays consulted by a custom `hasInstance`, rather than copying prototypes). The load-bearing JS-specific design choice is storing the whole nested schema mapping as `Map`, never a plain object: a plain JS object silently reorders integer-LOOKING string keys (a column or table literally named `"123"`) ahead of insertion order, which a Python `dict` — what upstream actually is — never does, generalizing the sibling `trie.js`'s `TRIE_END` lesson (an integer-vs-string key collision) to a full class of "numeric-looking name" bugs; verified directly against CPython with `addTable("t", "2:INT, 1:INT, a:INT")` keeping `["2", "1", "a"]` order on both sides, not JS's native `["1", "2", "a"]`. Building the oracle immediately surfaced a real, PRE-EXISTING bug in `src/helper.js`'s `dictDepth`/`dictValues` (not touched by any prior port task, confirmed working by this project's own prior sessions): `dictValues` treated ANY JS object as dict-shaped (`typeof d === "object"`), so `dictDepth` recursed into a column-type value that happens to be a real `exp.DataType` instance (a legitimate, upstream-typed input to `column_mapping` — `t.Any`, not `str`-only) and walked its OWN internal fields (`args`/`parent`/`comments`/...), reporting a bogus inflated depth (`Invalid mapping shape. Depth: 4` for a plain 1-level schema) instead of Python's real behavior (`d.values()` raises `AttributeError` for a non-`dict` object, caught and treated as depth 0); fixed with the one-line `constructor === Object` guard this codebase already uses everywhere else for "is this a plain dict" (`expressions/core.js`, `expressions/builders.js`), verified against `spike/fuzz_calib.mjs`'s existing `dict_depth` cases (unchanged, still green — none of its fixtures are non-dict objects) and a live differential re-check with `PYTHONHASHSEED=0`. A second self-caught defect, never shipped: an early draft's `Schema` class was documented as needing the `Symbol.hasInstance` override but the actual `Object.defineProperty` call was left out of the code — caught by a plain smoke test (`ensureSchema(existingMappingSchema)` silently re-wrapped it instead of returning it unchanged, throwing `SchemaError` on the resulting garbage input) before any oracle or commit. A third, R40-style incident: two of the file's composite cache-key template literals (`_normalizeTable`/`_normalizeName`'s internal, purely-a-performance caches) were written with a literal `NUL` JSON-escape sequence intended as source text but decoded into an actual embedded NUL byte by the tool call that wrote the file — caught immediately by `grep` reporting "binary file matches" instead of a normal text match (PORT_PLAN.md §4.2's predicted failure mode, a live repeat of R40's `_DOMAIN_DOT` incident, this time in freshly-authored JS rather than a ported Python literal) — fixed with a byte-level Python `.replace(b"\x00", b" ")` before any test or lint ran; `tools/lint_control_bytes.mjs` passes on the corrected file. `MappingSchema.copy`'s Python-only detail (a cache keyed by `dialect.__hash__`/`__eq__`, which upstream defines as `hash(type(self))` / class-only equality — so two differently-configured instances of the SAME dialect class collide onto one cache entry even in real CPython) is reproduced deliberately rather than "fixed," keying the JS-side cache by `dialect.constructor` to match the observable quirk exactly, not just the common case. `index.js` now re-exports `Schema`/`MappingSchema` from the package root, matching upstream's own `sqlglot/__init__.py:54` (`from sqlglot.schema import MappingSchema as MappingSchema, Schema as Schema` — exactly these two names, not the module's other helpers); `docs/api.md` gains a full `## Schema / MappingSchema` section (moved out of "Not yet designed", following the same "real today" / "target design" split the `exp` namespace section already uses), and `README.md` is updated to match. Measured: `spike/p6/fuzz_schema.mjs` (new, `spike/p6/gen_schema_ref.py` the CPython side) **EXACT 71 / 71, MISMATCH 0, ERROR 0** across 24 scenarios covering 1/2/3-level nested schemas, ambiguous-prefix trie lookups (`SchemaError` naming every possibility), `add_table` in its string/list/dict/`Map` column-mapping forms plus depth enforcement and no-op re-add, `only_visible` filtering (including the upstream quirk where a table missing from an otherwise-populated `visible` map RAISES rather than falling back), dialect-aware identifier normalization across the base/Snowflake (`UPPERCASE`)/DuckDB (`CASE_INSENSITIVE`) normalization strategies, UDF resolution (plain and qualified), `ensure_column_mapping`'s four input shapes plus its two error paths, `copy()`/`from_mapping_schema` independence, and a `DataType`-valued (not string) column type passing through `get_column_type` unchanged — the same DataType-instance case that found the `helper.js` bug above. `test/schema.test.mjs` adds 29 structural/error-handling unit tests needing no CPython (178 -> 207 total, `node --test` green throughout — every new failure during development traced to the test's own bad assumption, not a `schema.js` defect: one used a plain-object literal with numeric keys, already reordered by V8 before the test's own code ran, exactly the hazard this file's Map-based design exists to avoid downstream). `bash spike/run_all.sh`: the new `P6: schema.js MappingSchema vs CPython` probe is green; the only red probes are the same 3 pre-existing ones R37 already documented (`P3: check_command_warning strings` / `P3: AST oracle coverage (honest)` need `pip install -e .` on the pinned `/tmp/sqlglot-ref` checkout, unavailable in this environment; `P4: generate oracle (honest)` is red on the same pre-existing 66-row closure-tool-over-claim) — none newly introduced. *Generalisable takeaway:* a module can be "genuinely greenfield" in the sense of nothing depending on it yet, and still immediately exercise and break a shared dependency nobody had reason to doubt — `dictDepth`/`dictValues` had presumably only ever been called on real dicts before this file's `getColumnType` handed it a class instance, an input shape upstream's own type hints (`ColumnMapping = dict[str, Any] | str | list[str]`) explicitly allow and this port's own calibration oracle (`gen_calib_cases.py`) never happened to construct.

### Accepted red-team findings, not fixed

- **"The corpus is a snapshot; upstream moves."** Accepted; Q4 is the trade.
- **"Golden corpora are unreviewable."** Accepted *in part*: the AST oracle carries `repr` (human-readable), and the hand-ported `test/expressions/` and `test/build/` suites are real specifications. But 15,642 atoms remain a regression net; the fix is §8.5's human gate, not automation.
- **`Expr.__lt__` returning a node** — reproduced faithfully in `pySortedTuples`, accepting that the exact permutation on a tie is a CPython Timsort artifact. Divergence surfaces as a corpus failure and goes to `wontfix` with a citation.
- **`test_executor.py` and the 201 `# execute: true` executions** — permanently out, stated in the README.

---

## 10. Effort

### Model

Throughput: **150–250 LOC/agent-hour** of *conformance-verified* transliteration — written, run against an oracle, debugged to green. Midpoint 200. Deliberately below raw generation rates because the binding constraint is the debug loop. **This constant is now measured by the P0 calibration spike (day 3–6) and re-baselined after P1 and P3.** Integration/rework multiplier: **+35%** on code written before its oracle exists (all of P0–P2).

### v0 (P0–P8) — revised 2026-08-24 per Ben's Q2 decision ("We will ship at P8")

**A note on this recomputation.** The previous P0–P6 table's stated total ("1,130–2,025, mid ~1,310") did not actually match the sum of its own rows — summing the printed low-end figures gives 1,330h, not 1,130h, an inherited ~200h arithmetic slip from the original synthesis pass that nobody had caught. Extending the table through P7/P8 was the occasion to fix it properly rather than propagate the error further. The table below is a clean row-by-row sum; treat it as superseding the old total, not adjusting it.

| Phase | LOC | Agent-hours | Notes |
|---|---|---|---|
| P0 Foundation & tooling | ~3,000 | 150–225 | 2 spikes, harvester, 3 oracles + `unsupported_messages`, runner, ratchet, 6 probes, 6 fuzzers, 3 deny-lists, seeder, sync report, container, 2 bridge proofs, licensing. `_py/num.js` 40–70h; Python-regex group counter ~40h; `pyFromIsoFormat` ~10h; `fuzz_depth` + `DepthLimitError` threading ~10–15h (§4.7). Serial, one agent. |
| P1 Tokenizer | 1,809 | 60–100 | + code-point threading |
| P2 Expressions | 11,882 | 280–420 | includes +35% (written blind) and the hand-ported `test/expressions/` suite |
| P3 Parser base + Snowflake parser | 11,920 | 220–330 | ~253/401 base methods reachable (~9,000 effective) + 1,440 dialect + table seeding |
| P4 Generator base + Snowflake gen + transforms + simplify | 10,573 | 200–300 | ~230/484 base methods reachable |
| P5 dialect.js + optimizer Tier B + bridge + `test/build/` | 6,754 | 150–220 | bridge ~40h. No interim release (§9 Q3) |
| P6 DuckDB | 5,394 | 120–180 | mostly `generators/duckdb.js`. Does not ship as v0 (§9 Q2) |
| P7 Databricks chain | 2,357 | 80–130 | now part of v0 per Q2 |
| P8 Postgres + Redshift | ~1,742 | 50–80 | now part of v0 per Q2 — **v0 ships here** |
| Cross-cutting triage, fuzzer burndown | — | 150–250 | |
| **v0 agent-hours (P0–P8)** | **~55,400** | **1,460–2,235** (mid **~1,850**) | up from 1,310 mid at the old P0–P6 boundary — the +540h mid is P7+P8's own effort, not a correction |
| **v0 human review-hours** | ~34,100 LOC reviewed | **~170–285** (mid **~230**) | §8.5; separate budget. P7+P8 add ~20–35h to the old P0–P6 figure of 150–250h, at the same blended LOC/review-hour rate. If agents review instead, this row goes to ~0 and R1's residual rises correspondingly. |

### Wall clock (agent-hours only, midpoint ~1,850)

**Approximate — linearly extrapolated from the P0–P6-boundary wall-clock figures using the same implied throughput, not a fresh model run.** The original wall-clock figures were derived from phase-blocking-step reasoning (P0/P3/P4/P6 serial bottlenecks), not pure hours-divided-by-concurrency arithmetic, so extending them precisely requires rerunning that reasoning through P7/P8. P7's internal structure is itself partly serial (4 sequential sub-phases: hive→spark2→spark→databricks) but doesn't introduce a *new* project-wide bottleneck beyond what P0/P3/P4/P6 already impose, and P8 has no inheritance chain at all — so linear scaling by the ~1.41× effort increase (1,850/1,310) is a reasonable first-order estimate. **Treat these as placeholders; the P0 calibration spike (§7 P0, day 3–6) is what actually re-grounds this table.**

| Concurrency | 8h/day | 24/7 |
|---|---|---|
| **2** | **~58 weeks** (~13.4 months) | **~20 weeks** (~4.6 months) |
| 4 | ~28 weeks | ~10 weeks |
| 8 | ~17 weeks | ~6 weeks |

Sublinear above 4 — P0 is serial (~4–5 weeks of one agent regardless) and P3/P4/P6 each open with a blocking step. Realistic ceiling ~6 effective agents before the critical path dominates.

### Post-v0 (P9–P10 — everything after v0 ships at P8)

| Phase | LOC | Agent-hours |
|---|---|---|
| P9 Long tail (24 dialects + 14 versioned keys) | 17,680 | 900–1,500 |
| P10 Optimizer Tier C + lineage/diff/anonymize + full `_py/datetime.js` | ~9,000 | 400–700 |
| **On top of v0** | **~26,700** | **1,300–2,200** (mid **~1,750**) + ~165–320 human review-hours |

**Grand total (P0–P10, unchanged by where the v0 line is drawn): ~2,760–4,435 agent-hours** (mid ~3,600) **+ ~335–605 human review-hours.** At concurrency 2, 8h/day, extrapolated the same way as above: **~15–17 months from P0 to 100% closure.**

### Ongoing maintenance (separate budget, starts at P5 per Q4)

Measured steady state: ~85 LOC/day of upstream churn across `sqlglot/`, ~7.4% corpus growth per 4 months, ~98% additive. Re-transliterating changed hunks plus burning down quarantine: **~8 agent-hours/week** — one agent at ~20% capacity. Concentrated in `parsers/` + `generators/` (386 of 600 commit-touches), `parser.py`/`generator.py` (147), and `typing/` (144). Note that two of those three are *dialect* files for dialects that land in P9, so maintenance load runs ahead of build order.

### The two largest estimation risks

1. **P0 is serial and it is 150–225 hours.** If P0 slips, everything slips 1:1. It is also where the go/no-go lives — and that signal now arrives on **day 3**, not day 40. If the numeric and Unicode spikes cannot be made green, byte-exactness is not achievable at this budget, the corpus strategy is invalid, and the project should be rescoped to "semantically correct, not byte-exact" via a full re-plan rather than a schedule slip.
2. **The LOC/hour constant.** Every number above is linear in it. It is measured on day 3–6 and re-measured twice. Publish the measurement; if it lands below 150, bring Q2 back to the table before starting P3.

---

## Appendix — objection disposition

### Review A (execution/architecture)

| # | Objection | Disposition |
|---|---|---|
| A1 | Q1's Databricks marginal (+333) is the single-dialect figure, not the chain; conclusion reverses | **Fixed.** Recomputed: chain **+1,790**, #1 by marginal and by atoms/kLOC. Swap-and-insert-BigQuery recommendation withdrawn; churn line deleted. Q1 narrowed to the BigQuery-vs-Postgres question only — *partial reject of "delete Q1," because a real #3 question remains.* §9 Q1 |
| A2 | P6 "test_snowflake 100%" and P7 "222 TestDatabricks" are arithmetically impossible; R3's mitigation is false | **Fixed.** All exits restated as machine-computed closure. P6 = 2,086/2,437 (85.6%); P7 = 203/222. R3's secondary metric replaced with "% of in-scope atoms" (100% by construction). Full per-phase table published at P0. §3.2, §7, R3 |
| A3 | `.gitattributes merge=ours` is a no-op and inverts under rebase | **Fixed** (driver line deleted; `_gen/.manifest.sha256` + regeneration gate; `merge=union` for JSONL). **Primary remedy rejected:** making `corpus/**` untracked breaks the JS-only contributor path in Q5. §8.1 R4 |
| A4 | Pre-push hook + claims-on-`main` is not enforcement, and serializes pushes | **Fixed.** Claim overlap is a required CI check via GitHub API; hook is local convenience only. §8.1 R1 |
| A5 | Stub-seeding misses the class-level dispatch tables — where grammar shards actually collide | **Fixed.** Rule 2′: per-line table seeding with anchors + CI-asserted order. Re-measured (parser class body 2,337 LOC, generator 1,374). §8.1 R2′ |
| A6 | Review gate mis-sized by median and uncosted | **Fixed.** Measured distribution (p90 42, max 285, 72 methods >30 LOC = 56% of parser method LOC); budgeted by LOC; two-reviewer items named; human-hours row added; LLM-reviews-LLM alternative stated plainly. §8.5, §10 |
| A7 | `test_build.py` cannot run through the bridge (38 operator lambdas); bridge narrowed to its worst workload | **Fixed.** Removed from bridge, hand-ported at P5. Two end-to-end bridge proofs (`test_errors.py`, `test_transpile.py`) moved to P0; per-module satisfiability in `CONTRACTS.md`. §3.8 |
| A8 | LOC/hour constant is uncalibrated | **Fixed.** Calibration spike is P0 day 3–6; re-baselined after P1 and P3. **Target changed** from the suggested `tokens.py` to `time.py` + `helper.py`, which are differential-testable at P0 with no oracle infrastructure. §7 P0, §10 |
| A9 | Go/no-go sits at the end of a 4–6 week serial phase | **Fixed.** Numeric + Unicode spike is P0 day 1–3. §7 P0 |
| A10 | Pinning CPython protects today's corpus and destroys upstream tracking | **Fixed.** Container built from the pinned tag's own `requires-python`, rebuilt each resync, fails loudly on floor moves; patch version in `PROVENANCE.json`. §5.2 |
| A11 | No license/attribution plan for a derivative work | **Fixed.** `LICENSE-sqlglot`, `NOTICE`, README attribution, CI commit-match check, corpus coverage — a P0 blocker. §5.3 |
| A12 | `NEEDS_BASE` queue on the critical path with no owner or latency bound | **Fixed.** Named owner per phase, drain-first rule, `foundation-additive` in-place escape, published queue depth. §8.1 R3 |
| — | "§1–§6 not supplied; not approving on the appendix" | **Fixed.** This document is complete. |

### Review B (semantics/verification)

| # | Objection | Disposition |
|---|---|---|
| B1 | `subsecond_precision` is CPython-version-dependent and reaches output SQL; corpus encodes the harvester's interpreter | **Fixed — the most serious finding in either review.** `PROVENANCE.json` + hard-fail, `pyFromIsoFormat` promoted to P0 against a pinned CPython grammar, targeted differential test. New risk R6. §4.6, §5.2 |
| B2 | `too_wide` / `sanitize_comment` are corpus-invisible; a `.length` port passes 15,642/15,642 | **Fixed.** `fuzz_toowide.py` and `fuzz_comments.py` as named mandatory P0 targets with purpose-built generators; P4 exit criteria. §3.4, R4 |
| B3 | 497 `unsupported()` calls are discarded under `ErrorLevel.IGNORE`, yet P4 asserts on them with no oracle | **Fixed.** Generate-oracle row schema captures `gen.unsupported_messages` (two-line harvester change); P4 exit asserts it. §3.1(D) |
| B4 | Three concrete Python-semantics sites missing (`encode` UTF-8 length, `chr` beyond BMP, `:04d` format spec); `_py/` surface derived from briefs, not a census | **Fixed.** All three shimmed (`utf8Len`, `pyChr`, `pyFormatInt`); `corpus/deny/py_builtins.json` generated census + lint, regenerated on every bump. §4.6 |
| B5 | Decimal blast radius is 63 invocations, not 11,921; 60–100h sized for a general `decimal` clone | **Accepted with a floor.** `_py/num.js` scoped to the reachable op set; budget 60–100h → **40–70h**. **Rejected:** any move of the Decimal gate off P0 — `str(Decimal)`/`str(float)` round-tripping is the hard part regardless of op count, and it is the go/no-go signal. §4.6, §10 |
| B6 | 19 base `*_sql` methods never exercised by the dialect suite | **Accepted with modification.** Hand-written native tests at the porting phase rather than `wontfix` — they are reachable from user code even though upstream's suite misses them. §3.5, P9 exit |
| — | Corpus adequacy (405/424 dispatch), whitespace analysis, determinism, operator census, `.title()`/`casefold` absence, §8 stub-seeding | **Confirmed; not re-litigated.** |

### Post-review addition

| # | Finding | Disposition |
|---|---|---|
| C1 | **Recursion depth**: JS has a fixed, non-tunable stack where Python has `setrecursionlimit`; a realistic generator frame overflows at **4,225** on Node v22 while upstream ships a test at N=**10,000**, and depth grows **linearly with input size** on binary/`UNION` chains | **Fixed.** New §4.7 + risk R11; `fuzz_depth` added as a sixth mandatory P0 target with its measured max-N in `CONTRACTS.md`; `DepthLimitError` before V8's `RangeError`; trampolines on the two depth-∝-N paths, lint-exempted by name; `--stack-size` rejected with rationale. P0 +10–15h. **Found by the orchestrator after both reviews signed off** — missed by all four architects and both adversarial reviewers because it is corpus-invisible (`test_redshift.py:529` asserts the iterative path). Logged here as evidence for R4's residual. §4.7, R11, P0/P4 exits |

### Ben's decisions (2026-08-24)

| # | Question | Answer | Disposition |
|---|---|---|---|
| Q1 | Priority #3: Postgres or BigQuery? | *"Our product requires Snowflake, Databricks, DuckDB and Postgres. Those go first."* | **No change.** Postgres stays at P8 as written; confirms (does not alter) DuckDB's existing forced-dependency placement at P6. §1, §9 Q1 |
| Q2 | v0 acceptance bar: ship at P6 (21.8%) or hold? | *"We will ship at P8"* | **Architecture-changing.** v0 boundary moves from P6 to P8 (41.9% atom closure). §1, §7 P6/P8, §9, §10 all restated. |
| Q3 | Interim release at P5? | *"We don't need an interim release"* | **No change to build order** — Tier B at P5 was never optional. Removes the "v0-minus" release option, moot now that v0 already extends to P8. §7 P5 |
| Q4 | Pin-and-catch-up vs continuous tracking? | *"Sounds good. Pin first"* | **No change.** Plan proceeds exactly as written (§5.1): pin through P4, continuous resync from P5. |
| Q5 | Build-time Python dependency acceptable? | *"This is acceptable (and needed)"* | **No change.** Plan proceeds exactly as written (§5.2). |

**Recommendation: approve, with answers to Q1–Q5.** Only Q2 changes the architecture; the rest change sequencing or calendar. The go/no-go is now **P0 day 3** — if the numeric and Unicode spikes are not green, come back and re-plan rather than proceed.